<!-- 自動生成。手で編集しない。
     本書は自己完結した実装仕様である。ここに書かれていないことは決めないこと。
     不足があれば実装せずに報告する（推測で埋めない）。
     生成元: SPEC_B1_full_2026-08-26.md（実装者の手元には無い。参照を要求しないこと） -->

# SPEC B1 — 中核（実装用）

**作成日**：2026-08-26
---

## §C.0 この文書の位置づけ

### 範囲

**エージェントに `def(R)` を持たせ、予測・会計・削除を回す部分**を作る。

```
入る   AgentState の層B 欄／P1〜P7（予測）／O1〜O3（採点）／U1〜U8（更新）
       会計（節約・功績・例外費用・V）／m1 の登録／θ′ の削除／縦断ループ
入らない  伝達（B4）／個体測定の集計（B3）／掃引の実行
```

### この文書が守る基本原則

```
原則1  predict は AgentState を変えない                              [D9(b)]
原則2  記憶の変異は三種のみ：m1 の登録／θ′ の削除／台帳統計の更新     [D11]
原則3  削除の判定は一箇所で V < θ′ だけ。免除・床・容量退避を置かない [D10]
原則4  台帳はループから write-only。同期・追記のみ                    [D1 / D23]
原則5  エージェントは G*・研究者側台帳・実績 f を参照しない           [D1]
原則6  p̂ はエージェントごとに一つ。判定用と分析用を分けない          [D13(a)]
```

---

## §C.1 成果物

| ファイル | 内容 |
|---|---|
| `abm/definition.py` | `NamedDefinition` / `Constituent` / 位置と行の管理・墓石 |
| `abm/accounting.py` | `MeritAccumulator` / `ExceptionAccumulator` / `FrequencyTable` / `V` の計算 |
| `abm/filling.py` | P4（充填）と `slot_history`（スロット履歴集合） |
| `abm/abstraction.py` | m1（G4a / G4b / G5） |
| `abm/deletion.py` | θ′ による削除・墓石 |
| `abm/loop.py` | 縦断ループ（A-003） |
| `abm/agent_runtime.py`（改修） | `predict` / `update` の中身。`AgentState` の層B 欄 |
| `abm/ledger.py`（改修） | 腕記述子 7 → 12 本・`coin_t`／`f_realized` の改名 |
| `tests/test_b1_*.py` | §C.9 の受け入れ試験 |

---

## §C.2 型（§1 rev5 からの転記）

### §C.2.1 `AgentState` の層B 欄

```python
@dataclass(frozen=True)
class AgentState:
    # 層A（既存・改変禁止）
    prototype: RelationGraph | None = None
    public_history: tuple[AgentOutput, ...] = ()
    rng_state: ... = None
    # 層B（B1 で追加）
    definitions: Mapping[str, NamedDefinition] = field(default_factory=dict)
    merit: Mapping[tuple[str, int], MeritAccumulator] = field(default_factory=dict)
    embed: Mapping[tuple[str, int], float] = field(default_factory=dict)
    exceptions: ExceptionAccumulator = ExceptionAccumulator(0.0, 0)
    p_hat: FrequencyTable = FrequencyTable.empty()
    slot_history: Mapping[tuple[str, int], frozenset[str]] = field(default_factory=dict)
```

- 層A の 3 欄は改変しない。層B の 6 欄を足すだけ
- `merit`・`embed`・`slot_history` のキーは `(R の名前, slot_index)`
- 各試行末に serialize → deserialize しても軌道が変わらない
- oracle 由来値を `AgentState` に入れない
- 研究者側の分類（段階・窓の開閉・E2/E3・停留・神話／真・①②③・干ばつ中か）をフラグとして持たない

### §C.2.2 位置・行・構成素

| 語 | 定義 |
| --- | --- |
| 位置 | `def(R)` の枠。番号で識別する。削除しても番号を詰めない |
| 行 | ある位置に、ある時刻に登録された一本の主張。同一性 =（位置番号, 登録時刻） |
| 構成素 | `def(R)` の一行（＝上の「行」） |
| `m_live` | 生きている**行**の数。墓石を含まない |
| `m_alloc` | 確保した**位置**の数。墓石を含む。単調非減少 |
| 墓石 | 削除された行の `relation_id`。**関係ノードとしてグラフに残り、照合器から見える**が `m_live` に入らない |

### §C.2.3 参加率の二語（混同禁止）

| 語 | 定義 | 核 | 塔 | 媒介 | 周縁A |
| --- | --- | --- | --- | --- | --- |
| **役割参加率 `π_role`** | その役割スロットが場面に現れる確率 | 1.0 | 1.0 | **1.0** | `π_A` < 1 |
| **述語再現率 `π_pred`** | 登録した特定の述語がその位置に再び来る確率 | 1.0 | 1.0 | **1/N** | `π_A` |

- **種ファイル `U-011_seed_v1.json` の `pi` 欄は `π_pred` である**
- 媒介は置換型（周縁B）。役割スロットは常に埋まり、中身だけが袋（N 語）から一様に入れ替わる
- 周縁A は不在型。`π_role` も `π_pred` も `π_A`

### §C.2.4 記号（改名済み）

| 現行 | 旧 | 意味 |
| --- | --- | --- |
| `u` | — | 生存座標 `u = (1−h̄)·π_pred·a`。種の `u` 欄 |
| `coin_t` | `u_t` | 共通乱数の引き。`coin[agent,t] < f_realized` |
| `tau_acc` | `τ` | 部分照合の採択基準 |
| `lambda_j` | `τ` | σ の時定数。`geomspace(0.3, T×3, 16)` |
| `f_realized` | `f_setting` | その試行で `f(agent, t)` が返した値 |

---

## §C.3 予測（P1〜P7）

### §C.3.1 P1 — 整列（SME）

- 層A の `map_graphs` → `apply_threshold` をそのまま呼ぶ。入出力を変えない
- **base は `prototype`**（A-002）。現行 smoke の配線は `prototype` を読んでいないので、ここを直す
- F-1 の第三分岐（未観測スロット）を変えない。T7b がこれを固定している
- `apply_threshold` の `threshold` は SME の写像受理閾値であり、削除閾値 `θ′` でも採択基準 `tau_acc` でもない

### §C.3.2 P2 — R の同定と `def(R)` の展開

- `definitions` から、整列に噛み合う `R` を選ぶ
- 塔は最新行を読む
- 墓石はグラフノードとして残っているので、展開時に「未観測スロット」として扱う

### §C.3.3 P3 — 採択判定

- `need(m_live) = ⌈tau_acc · m_live⌉`。**比の読みを採る**
- 絶対値の読み `max(m−k, 1)` は**腕として走らせない。事後再構成に留める**
- そのために毎試行 `m_live` と充足数を台帳に残す
- `tau_acc` の主値は 0.67

### §C.3.4 P4 — 充填（**この段が律速**）

- **発火条件は連言**：def 側でスロットが埋まらない **かつ** 場面の可視部にも答えが無い
- 可視部に述語まで一致する相手が実在するなら、充填せず ②／③ の判定へ回す
- 候補プールは `slot_history[(R, slot_index)]`。そこから `p̂` 最頻を選ぶ
- 集合が空なら、署名適合（アリティ＋引数**型**）の `p̂` 最頻へフォールバック
- **`slot_history` には「そのスロットで観測された述語」を入れる**（充足した回だけではない）。充足ベースだと集合が単元集合になり U-079 が解こうとした問題が解けない
- **m1 の登録が使う候補プールは、その整列イベントの対応内容**（＝その回の創設事例の中身）であって、蓄積した `slot_history` ではない。帰結として媒介の `P̂₀ ≥ 0.5`
- `slot_history` は `def(R)` の外に置き、MDL の会計に入れない
- v0 は減衰なし（集合は単調増加）。減衰の実装形は U-085
- 伏せ辺の穴と宙吊り塔スロットは**同じ充填規則**で扱う。由来による例外を置かない
- 充填した内容を `verbatim`・`p̂` に書き戻さない
- 場面を見てから充填の**中身**を決めない
- 発火条件の「かつ」は可視部の**空きの判定**であって、中身を取ることではない
- `T=0` で候補が同点なら棄権する。`α` も加点しない
- 予測出力は**全充填スロット集合**

### §C.3.5 P5 — ②／③ の判定

- **可視部に述語まで一致する相手が実在するかで判定する**
- **写像に載っているかどうかで判定しない**（F-1 の内部判定を下流へ漏らさない）
- ② ＝ その役割スロットが可視部で埋まっており、内容が違う。`f` と無関係に発火する
- ③ ＝ その役割スロットが可視部で空いている。①（隠された）と区別できない

### §C.3.6 P6・P7 — 出力と保留

- 棄権理由は `below_threshold` / `ambiguous_projection` / `no_projectable_relation` の三種に分離して記録する（A-014 の `no_mapping` は実装に存在しないため 2026-08-25 に改名）
- `PendingState`（層A に既存）を予測確定〜フィードバック受領前の保留として使う
- **`predict` は `AgentState` を変えない。**`p̂` 更新・`verbatim` 書き込み・m1 発火・`θ′` 実行を禁じる
- 伏せ位置を参照しない

---

## §C.4 採点（O1〜O3）

- 採点対象は**伏せ辺一本**
- 棄権も全候補一様の満額コストを払う（**研究者側の採点**）
- 結末は**的中／失敗／保留／未解決**の四分類
- オラクルの返答を `def(R)` へ直接書き込まない

---

## §C.5 会計（U3〜U5）

### §C.5.1 節約（登録時に凍結）

```
ℓ_q      = −log₂ p̂(q)
c_q      = 1 + 3·arity − 3·新規スロット数
節約_q   = ℓ_q + c_q
a_q      = 節約_q / ℓ_q
```

- `ℓ_frozen`・`C_hold` は登録時から不変。再計算しない
- 塔の `R.k` 参照価格は `log₂ m_alloc`（墓石を含む）
- 例外の位置価格は `log₂ m_live`（墓石を含まない）

### §C.5.2 功績（二チャネル）

```
S_q = 1.0 × 照合参加  +  α × 充填  +  β × R外使用
P_q = ( σ加重の照合参加 + α × σ加重の充填 ) ÷ σ加重の R 適用回数
```

- 功績の基準は**充足**（`def(R)` が名指した述語がその位置に実在したか）であって写像の成立ではない
- 同一試行で同一構成素に二重加算しない
- **`α` を得る「充填」は採点された充填に限る**（狭い読み）
- `p̂` は書き限定で更新する。照合参加・充填では上げない

### §C.5.3 例外費用と `ΔL`

```
例外費用 E = 2 + log₂ m_live + ℓ_r
ΔL_q(t)   = 節約_q − (σ加重の例外費用の和) / (σ加重の q の使用回数)
V_q(t)    → (M_q·節約_q − C_q·E_q) / ℓ_q
生存条件   P·a + w·(embed/ℓ_frozen) ≥ θ′
```

- **`V_q` の正規化分母は `q` の使用回数**（創設事例 2 件を含む）。経過場面数ではない
- ①と② は同額。同一の累積器 M-051 へ流す。分けない
- 修復圧は**採点された予測に寄与した構成素**とその上の塔にだけ届く。同じ試行の他の充填には伝播させない
- ①の届き先は**広い読み**（照合に参加した構成素すべて）
- 宙吊りスロットの充填が外れたときの課金は、宙吊り参照を持つ構成素（塔・核高階）が受ける
- 単位はビット

### §C.5.4 棄権時の課金（`abstain_charge`）

| 棄権理由 | 課金 |
| --- | --- |
| `below_threshold` | **課金しない**（R を適用していない） |
| `no_projectable_relation` | **腕 `abstain_charge` で切り替える** |
| `ambiguous_projection` | 同上（同じ腕で動かす） |

- 課金する腕では、宛先は写像に載った構成素すべて（判断2 の広い読み）
- 課金する腕では `ℓ_r` を「その位置に実在した述語の符号長」で固定する。全候補一様 `log₂ V_vocab` は感度として記録
- **走行台帳に `arm_abstain_charge` を持つ**

### §C.5.5 初期化（D-cold）

- 行の初期化は C2。創設事例（登録時の整列に使われた場面。通常 2 件）の**在／不在**を、登録時点で既に経過した時間だけ減衰させた強度として分子・分母に継承する
- 例外費用累積器の初期値は分子 0・分母は創設事例 2 件分
- **新規行が登録直後に落ちることはない**

---

## §C.6 登録と削除（U6・U7）

### §C.6.0 U1・U2 — 場面と開示辺の符号化（2026-08-26 復帰）

一試行の手順は P1〜P7・O1〜O3・U1〜U8 の **18 手順**である。U1・U2・O2・O3 を落とさないこと。

- **U1**：現在試行の場面を `verbatim` として書き込む。`predict` 確定後に行う
- **U2**：`RevealedEdge` があれば、**観測と同じ経路で**書く。受信専用ストア・来歴タグを置かない
- 開示事象の分子重みは `γ = α`（開示は充填と同じ扱い）。主格子は α=0 なので実際に走る値は γ=0
- 充填した内容は U1・U2 のどちらでも書かない

### §C.6.0b O2・O3 — オラクルのフィードバックと台帳記録（2026-08-26 復帰）

- **O2**：`coin_t < f(agent, t)` なら `RevealedEdge` を返す。そうでなければ何も返さない（＝保留）
- `f` は定数で書かず `f(agent, t)` の署名で研究者側に持つ。各試行で実際に返した値を記録する
- **O3**：研究者側台帳に記録する。提示事象・伏せ位置・①②③・`coin_t`・`f_realized`・`f_fired` を毎試行
- O1〜O3 はオラクル側の手順であり、エージェントの入力域に入らない

### §C.6.1 順序

- **U6（m1 の登録）を先に、U7（θ′ の削除）を後に走らせる**

### §C.6.2 U6 — m1（登録と更新）

- G4a：既存の整列イベントに便乗する。全ペア比較は不要
- G4b：第二整列イベントで初回登録。固定保持試行数を置かない
- その後の整列でも更新する
- G5：整列の共通部分構造の内側で、サイズ2以上の部分集合を `V` が増える限り貪欲に選ぶ。第一歩だけ対で評価する
- 整列対応位置には署名適合の最頻具体述語を記録する。設計者が `q` を直接選ばない
- 更新は必ず m1 の発火を経由する。修復関数・誤り検出ハンドラからの追加を禁じる
- 既存構成素の内容を上書きしない。同じ位置に別内容を登録する場合は新規行として扱い、旧行は θ′ で独立に落ちる
- 再抽象化は新しい `R′` を作らず既存 `def(R)` を更新する
- 塔の畳み直しは既存塔を書き換えない。新塔を新項目として登録する
- **削除項目の復元をしない。再観測は新項目として登録する**
- 追加イベントの台帳レコードに、発火の根拠となった整列イベント ID を必ず入れる

### §C.6.3 U7 — θ′ による削除

- 削除の判定は一箇所で `V < θ′` だけ
- `V` の形と閾値は全項目種で同一（verbatim・gist・`def(R)` 構成素・塔・出現）
- 免除・床を置かない
- 容量 `C` を執行者にしない。LRU・優先度キュー・周期的掃除を禁じる
- 削除に関する閾値は `θ′` 一つ
- 失敗登録・衝突・宙吊りは `S`/`V` を動かすだけで、直接削除を起こさない
- embed の fan-in 集約 `g(x)=κx/(κ+x)` は親の寄与を足した後に一度だけ適用する
- embed は削除判定の直前時点の生存親で計算する
- `θ′` の 4 点は `0.0410 / 0.1432 / 0.2637 / 0.3842`、天井 0.3842。第5段を置かない
- E2（`R` 自体の忘却）は `θ′` 経由でのみ起きる。`m1` が `R := ∅` を書かない

---

## §C.7 走行台帳（U8）

- 台帳はループから**同期・追記のみ**で書く。集計・後処理をしない
- 台帳を無効化するフラグを作らない
- ループの各試行末に append が一回以上呼ばれる
- 台帳はループから見て write-only。read API を作らない
- 欄数は **固有 87 欄 ＋ run ヘッダ 12 本**。内訳は 層A 38 ／ D23 22 ／ 層B 23 ／ 研究者側 5 ＝ 記載 90、重複 3（`agent_state_snapshot_hash`・`abstain_reason`・`f_realized`）。**見出しの数字ではなく実リストを数え上げて検証する**
- `support_at_adoption` は**採択に失敗した試行でも達成された充足数を書く**。null 回避の 0 を入れない
- `f_realized` を毎試行記録する（`f(agent, t)` が返した値）
- 提示事象と隠された位置も毎試行記録する
- `m_live` の時系列を毎試行記録する（振動の周期を測るため）

### §C.7.1 腕記述子（7 → 12 本）

```python
ARM_DESCRIPTOR_FIELDS = (
    "arm_alpha", "arm_beta", "arm_w", "arm_kappa", "arm_repair_scope",
    "arm_holdout_repr", "arm_f_profile",
    "arm_lambda_mix", "arm_abstain_charge",
    "arm_temperature", "arm_d_shared", "arm_adaptation_table",
)
if len(ARM_DESCRIPTOR_FIELDS) != 12:
    raise RuntimeError(f"腕記述子は12本ではない: {len(ARM_DESCRIPTOR_FIELDS)}")
```

- アサーションを `!= 7` から `!= 12` へ変更する
- 欄を持つことと掃引することは別。置き場は §5 で決めてよいが、欄は B1 と同じ便で確保する
- `theta_prime`・`tau_acc`・`f_realized` は毎試行の欄なので腕記述子に重複させない
- `C`（容量）は執行者ではなく測定量なので腕にしない

---

## §C.8 縦断ループ

- 試行 `t` の `AgentState` を `t+1` へ持ち越す
- 一走行の提示回数は `T ≈ 1,740` 試行
- 共通乱数は `coin[agent,t] < f_realized` で引く。`Bernoulli(f)` を直接引かない
- 世界列はエージェント状態・予測・正誤・掃引パラメータに依存しない。`world_hash` が全腕で一致する
- 実行順は `predict` → `oracle` → `update` の一方向。`update` の中で予測をやり直さない

---

## §C.9 受け入れ試験

**本節は自己完結している。**期待値はすべてここに書かれている。上流文書を参照しないこと。
**試験が落ちたら、原因を特定して直してから先へ進む。直せない（＝本書に書かれていない）なら停止して報告する。**

### 段1 — 型と器（走行不要）

| # | 検査 | 期待 |
|---|---|---|
| 1-1 | `a_q = (ℓ_q + c_q)/ℓ_q` が種の `a` 欄と小数点以下 4 桁まで一致 | 24 構成素すべて |
| 1-2 | `c_q = 1 + 3·arity − 3·新規スロット数` が種の `c` 欄と一致 | 24 構成素すべて |
| 1-3 | `ℓ_q = −log₂(rate_q / Z)`、`Z = 8.134472` が種の `ell` 欄と一致 | 24 構成素すべて |
| 1-4 | `Σ p̂ = 1.000000` | — |
| 1-5 | 媒介・周縁A の `(a,e)` 型で `c = 4` が出る | — |
| 1-6 | `sha256` キーを除いた辞書を `json.dumps(d, sort_keys=True, separators=(',',':'), ensure_ascii=False)` した文字列の sha256 | `ddc0e4ae928c08ce252c6f9456eb74e9e8044716a4b4e4c84d6c36daa974d858` |
| 1-7 | `AgentState` の層B 6 欄が存在する | `definitions` / `merit` / `embed` / `exceptions` / `p_hat` / `slot_history` |
| 1-8 | oracle 由来値が `AgentState` に入っていない | 静的検査 |
| 1-9 | 研究者側の分類（段階1/2/3・窓の開閉・E2/E3・停留・神話／真・①②③・干ばつ中か）がフラグとして無い | 静的検査 |
| 1-10 | `config.theta_prime` の取りうる 4 点が種の `theta_grid` と一致 | `0.0410 / 0.1432 / 0.2637 / 0.3842`。第5段を置かない |
| 1-11 | 走行台帳の欄数 | **固有 87 欄**（層A 38 ／ D23 22 ／ 層B 23 ／ 研究者側 5 ＝ 記載 90、重複 3：`agent_state_snapshot_hash`・`abstain_reason`・`f_realized`） |
| 1-12 | run ヘッダの腕記述子 | **12 本**。`!= 12` でアサート |

### 段2 — 静的会計（走行不要）

```
u（媒介・周縁A）   M1 0.088886 ／ M2 0.095324 ／ M3 0.191062 ／ M4 0.336381
u（核・塔）        1.44 〜 2.67
```

| # | 検査 | 期待 |
|---|---|---|
| 2-1 | 各 `θ′` で落ちる集合 | `0.0410` なし（全モチーフ 6）／`0.1432` M1・M2 の媒介・周縁A／`0.2637` さらに M3／`0.3842` さらに M4 |
| 2-2 | `E(m_live=4) = 2 + log₂ 4 + ℓ_r` の総額 | **9.024 bit** |
| 2-3 | `λ_mix` の床 `λ_mix(1−1/V_vocab)/ln2` が種の最小 `ℓ`（3.02 bit・`allow`）より下 | `λ=0.1` で 0.1416 bit |
| 2-4 | `V_q(t)` が定常値へ**単調に落ちる** | 分子 0 から、分母は創設事例 2 件から |
| 2-5 | `V_q` の正規化分母が `q` の**使用回数**（創設事例 2 件を含む） | 経過場面数を分母にしない |
| 2-6 | 過適用が課金を増やすと同時に**功績も減らす** | 片方だけ動かす実装を弾く |
| 2-7 | **媒介の行の登録直後の `P̂`** | **≥ 0.5。`P̂₀ = 0` の行が出たら、m1 の登録が蓄積した `slot_history` を読んでいる** |
| 2-8 | 媒介が `θ′` を割るまでの適用回数（`P̂₀=0.5`） | M1 `0.1432` 26／`0.2637` 7／`0.3842` 4。M2 は 29／7／4。**最短は M2 周縁A の 3 回** |
| 2-9 | 同一試行での登録と削除が起きないこと | 起きたら `P̂₀` が 0 になっている |

### 段3 — 照合と充填（8,000 場面・`τ_acc = 0.67`・`m = 4`）

**T7 期待表（伏せ辺なし）**

| R | 採択率 | OA | 核一階1 | 核一階2 | 核高階 | 塔 |
|---|---|---|---|---|---|---|
| M1 | 0.250 | 0.000 | 1.000 | 1.000 | 1.000 | 1.000 |
| M2 | 0.500 | 0.500 | 0.500 | 0.500 | 1.000 | 1.000 |
| M3 | 0.500 | 0.500 | 0.500 | 0.500 | 1.000 | 1.000 |
| M4 | 0.250 | 0.000 | 1.000 | 1.000 | 1.000 | 1.000 |

| # | 検査 | 期待 |
|---|---|---|
| 3-1 | **伏せ辺の有無で採択率・OA が変わらない** | 上表 |
| 3-2 | 核の充足率（M1・M4）が `(1−h̄)` を再現する | **0.788 / 0.810**（`1−h̄ = 0.797`） |
| 3-3 | **回帰検査**：次の値が出たら照合器の第三分岐（未観測スロット）が外れている | M1 0.150/0.000、M2 0.345/0.574、M3 0.349/0.564、M4 0.248/0.397 |
| 3-4 | `τ_acc` の感度 | `0.67` 上表／`0.75` **0.67 と同値**／`0.50` 全モチーフ崩壊（OA 0.750・核の充足率 0.250） |
| 3-5 | `m=5` の階段が `m=4` と同一 | 上表と一致。媒介の充足率 ≈ `1/N` で課金の 75〜94% を吸う |
| 3-6 | `m=2` で `no_projectable_relation` が 2,000/2,000 にならない | P4 が発火している |
| 3-7 | `m=2` で予測が出る確率 | **`3·h̄ ≈ 0.61`**（残り ≈ 0.39 は棄権） |
| 3-8 | **`m=4` で伏せられた生存行に予測が出る** | **M1 0.406 ／ M2 0.414 ／ M3 0.384 ／ M4 0.184**（n=2000・τ=0.67） |
| 3-9 | **`m=4` の過剰適用先での予測率** | **M2 0.384 ／ M3 0.414**（M1・M4 は OA=0 なので 0.000） |
| 3-10 | **`m=6` でも `m=4` と同じ予測率** | 周縁A の生死は核の投影に影響しない |
| 3-11 | **M4 の予測率だけ低いこと** | `0.184 ≈ h̄`。`turn(b,c)` の `c` が def(M4) の中でこの行にしか現れないため。**種の構造であってバグではない** |
| 3-12 | **充填経路（宙吊りスロット）からは予測が出ない** | 媒介の `e` が `entity_mapping` に載る率 **0.000**（m=4・m=6 とも） |
| 3-13 | `entity_map_covered` 欄が毎試行埋まる | 伏せられた行の実体引数が全部写像されたかの bool |
| 3-14 | 登録直後の `slot_history_size` | **≥ 1**。0 なら D-cold の継承が効いていない |
| 3-15 | フォールバック（署名適合の `p̂` 最頻）に到達する回数 | v0 では **0 回**。1 回でも立つなら継承が抜けている |
| 3-16 | **②／③ は可視部に述語まで一致する相手が実在するかで判定する。写像に載っているかで判定しない** | 静的検査（コードの分岐を見る） |
| 3-17 | 印が出る側（核・媒介）で ③ が起きない | 8,000 場面で 0 件 |
| 3-18 | `T=0` で候補が同点なら棄権する。`α` も加点しない | — |

### 段4 — 薄化の到達点（縮小走行）

| # | 検査 | 期待 |
|---|---|---|
| 4-1 | `V(定常)`（`f = 0`） | M1・M4 の核・塔 **変化なし**（課金ゼロ）／M2 `carry` **0.230**／M2 `lift` **0.238**／M3 `break` **0.259**／M3 `cut` **0.239** |
| 4-2 | **①率（適用あたり・`f=1`）** | **M1 0.000 ／ M2 0.210 ／ M3 0.216 ／ M4 0.000** |
| 4-3 | ① が立つのは**過剰適用先のみ** | 自モチーフでの予測は的中率 1.000・失敗 0.000。過剰適用先は的中 0.000・失敗 1.000 |
| 4-4 | **M1・M4 では `f` を 0→1 に振っても核の `V` が動かない** | OA = 0.000 なので ① が原理的に立たない |
| 4-5 | M2 `carry` の `V` が `θ′` を割る `f` | `0.1432` で **≈ 0.218**／`0.041` で **≈ 0.475** |
| 4-6 | M3 `break` の `V` が `θ′` を割る `f` | `0.1432` で **≈ 0.306**／`0.041` で **≈ 0.577** |
| 4-7 | `charge_source` 欄で ①／② が分離できる | — |
| 4-8 | `θ′` ごとの到達点（`f = 0`） | `0.0410 / 0.1432` **全モチーフ `m=4` で停止**／`0.2637 / 0.3842` **M1・M4 は `m=4`、M2・M3 は `m=2`** |
| 4-9 | `m=2` で採択率・OA が `m=4` から変わらない | 0.500 / 0.500 のまま |
| 4-10 | `m_live` の時系列と、位置別の登録／削除イベント時系列を毎試行記録する | 欄の存在 |
| 4-11 | **`m_alloc` が有界であること** | 再登録は位置を再利用する。伸び続けたら実装が位置を再利用していない |

### 段5 — 台帳の完全性と再現性

| # | 検査 | 期待 |
|---|---|---|
| 5-1 | `NON_NULL` 欄が毎試行埋まる | `oracle_verdict` / `support_at_adoption` / `m_live` / `constituent_reason_123` ほか |
| 5-2 | `support_at_adoption` が**採択に失敗した試行でも**達成された充足数を書く | null 回避の 0 を入れない |
| 5-3 | `f` は `f(agent, t)` の署名で持ち、**実際に経験した `f` を毎試行記録する** | 数値リテラルで `f` を参照する箇所が無いことの静的検査 |
| 5-4 | 提示事象と隠された位置も毎試行記録する | ①/② の率を期間ごとに復元するため |
| 5-5 | **再走行なしで、通過率・干ばつ判定が台帳だけから再計算できる** | 段4 の出力に対して実行して一致 |
| 5-6 | 同一（run seed, config）で `PYTHONHASHSEED` によらず全 run がビット一致 | — |
| 5-7 | 各試行末に `AgentState` を serialize → deserialize しても軌道が変わらない | 完全一致 |
| 5-8 | 測定関数の前後で `agent_state_snapshot_hash` が不変 | — |
| 5-9 | 削除の呼び出し元が一箇所であることの静的検査 | — |
| 5-10 | `AgentState` の変異点が m1／`apply_theta`／統計更新の三つだけであることの静的検査 | — |
| 5-11 | `ℓ_frozen` が登録時から不変であるテスト | — |

---

## §C.10 この文書で決めた小さな既定

| # | 既定 | 理由 |
|---|---|---|
| 1 | `project` を書き換えず、P4 を後段に足す | T7/T7b の期待表と F-1 の検証を動かさないため  |
| 2 | 墓石はグラフノードとして残す（`relation_id` を保持） | F-1 の第三分岐が「未観測スロット」として扱えるため  |
| 3 | 棄権時の `ℓ_r` は「その位置に実在した述語の符号長」で固定 | 全候補一様との差は 0.15 bit 以下で結論を変えない  |
| 4 | 構成素は「行」で数える（`m_live` は行数、`m_alloc` は位置数） | U-033 の行の同一性から  |
| 5 | `coin_t`・`f_realized` へ改名する | 生存座標 `u` との衝突、および非一様 `f` での意味の分離  |

---

## §C.11 範囲外（ここでは実装しない）

```
B3      個体測定の集計（通過率・神話質量 M / M*・①的中率の時系列）
B4      伝達
§5      掃引の実行（主格子・代表セル・N・シード予算）
非一様 f  f(agent,t) の署名と f_realized の記録は本書で持つが、
          時間的非一様（干ばつ）・空間的非一様（群ごとの f）の腕は置かない
```

- これらのフックは定義だけ置き、呼ばれたら `NotImplementedError` を投げる
- **非一様 `f` は後から腕として足せる形にしておく。**`f(agent, t)` の署名を崩さず、`f_realized` を毎試行記録する

---

## §C.12 未決（B1 の範囲に残るもの）

| 番号 | 内容 | 重さ | 影響 |
|---|---|---|---|
| U-018 残 | `R` の出現の価格、スロット宣言の符号形（H-066） | 中 | gist の価格。符号器を書けば決まる |
| U-085 | `slot_history` の減衰（v1）の実装形 | 軽 | v0 は減衰なしで走る |
| U-035 | `error_direction_prototype_consistent` を残すか ① 的中率時系列へ置換するか | 中 | 台帳欄の去就 |
| 隙間2 | 宙吊り充填の課金先が M-008 で閉じるか（§C.5.3・確信度 70%） | 中 | **U-071 の駆動経路。red team 最優先** |
| U-062 | E3 の操作定義が「薄化」を検査していない（過去への条件） | 中 | 本件とは別問題。§C.6.2 の同定は 2026-08-26 に撤回 |
| **N-E3** | E3 は状態述語であって吸収状態ではない（吸収状態の条件） | 重 | `m_live` の振動を停留と呼べるか。**推定量は試走後に決めるので、B1 では判定を実装しない。振動の周期・振幅と、位置別の登録／削除イベント時系列を記録するだけにする** |
| U-030 | 主張階層 | 中 | 台帳の欄が確定しない。B3 の入口 |
| 主格子・代表セル | 定義が §5 に無い | 中 | B1 の実装は止めないが、`arm_*` の置き場が決まらない |
