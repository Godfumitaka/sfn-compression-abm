<!-- 自動生成。手で編集しない。
     本書は自己完結した実装仕様である。ここに書かれていないことは決めないこと。
     不足があれば実装せずに報告する（推測で埋めない）。
     生成元: SPEC_B1_full_2026-08-26.md（実装者の手元には無い。参照を要求しないこと） -->

# SPEC B1 — 中核（実装用）

**作成日**：2026-08-26
---

## §C.0 この文書の位置づけ

### 範囲

**エージェントに `def(R)` を持たせ、予測・会計・削除を回す部分**を作る。

```
入る   AgentState の層B 欄／P1〜P7（予測）／O1〜O3（採点）／U1〜U8（更新）
       会計（節約・功績・例外費用・V）／m1 の登録／θ′ の削除／縦断ループ
入らない  伝達（B4）／個体測定の集計（B3）／掃引の実行
```

### この文書が守る基本原則

```
原則1  predict は AgentState を変えない                              [D9(b)]
原則2  記憶の変異は三種のみ：m1 の登録／θ′ の削除／台帳統計の更新     [D11]
原則3  削除の判定は一箇所で V < θ′ だけ。免除・床・容量退避を置かない [D10]
原則4  台帳はループから write-only。同期・追記のみ                    [D1 / D23]
原則5  エージェントは G*・研究者側台帳・実績 f を参照しない           [D1]
原則6  p̂ はエージェントごとに一つ。判定用と分析用を分けない          [D13(a)]
```

---

## §C.1 成果物

| ファイル | 内容 |
|---|---|
| `abm/definition.py` | `NamedDefinition` / `Constituent` / 位置と行の管理・墓石 |
| `abm/accounting.py` | `MeritAccumulator` / `ExceptionAccumulator` / `FrequencyTable` / `V` の計算 |
| `abm/filling.py` | P4（充填）と `slot_history`（スロット履歴集合） |
| `abm/abstraction.py` | m1（G4a / G4b / G5） |
| `abm/deletion.py` | θ′ による削除・墓石 |
| `abm/loop.py` | 縦断ループ（A-003） |
| `abm/agent_runtime.py`（改修） | `predict` / `update` の中身。`AgentState` の層B 欄 |
| `abm/ledger.py`（改修） | 腕記述子 7 → 12 本・`coin_t`／`f_realized` の改名 |
| `tests/test_b1_*.py` | §C.9 の受け入れ試験 |

---

## §C.2 型

**本節の `dataclass` 宣言は完全である。**フィールドを足さないこと。足りないと感じたら停止して報告する。

### §C.2.0 前提（層A の既存型・改変禁止）

```
# abm/domains.py に既存。公開契約を変えない
Entity(entity_id: str)
Relation(relation_id: str, predicate: str, arguments: tuple[str, ...], attributes: Mapping = {})
RelationGraph(graph_id: str, entities: tuple[Entity, ...], relations: tuple[Relation, ...])
```

### §C.2.1 層B で新設する型（完全宣言）

```python
from dataclasses import dataclass, field
from typing import Mapping, Any
import math

@dataclass(frozen=True, slots=True)
class FrozenPrice:
    """登録時に確定し、以後いっさい再計算しない価格。"""
    ell_frozen: float        # ℓ_q = −log₂ p̂(q)。登録時点の p̂ で確定
    c: int                   # c_q = 1 + 3·arity − 3·新規スロット数
    hold_cost: float         # C_hold = ell_frozen + 1 + 3·arity
    m_alloc_at_reg: int      # 登録時の m_alloc。塔の R.k 参照価格 log₂ m_alloc に使う

    @property
    def saving(self) -> float:      # 節約_q = ℓ_q + c_q
        return self.ell_frozen + self.c

    @property
    def a0(self) -> float:          # a_q(0) = 節約_q / ℓ_q
        return self.saving / self.ell_frozen

@dataclass(frozen=True, slots=True)
class Constituent:
    slot_index: int          # 位置番号。墓石を含む確保位置の通番。削除しても詰めない
    registered_at: int       # 登録時刻（試行番号）
    relation: Relation       # 述語と引数。高階は arguments に relation_id を持つ
    frozen_price: FrozenPrice
    alive: bool = True
    # 行の同一性 = (slot_index, registered_at)。role を持たせない（D5(b)）

@dataclass(frozen=True, slots=True)
class NamedDefinition:
    name: str                              # R の識別子（不透明トークン）
    constituents: tuple[Constituent, ...]  # 墓石を含む全行
    m_alloc: int                           # 確保位置数（墓石込み）。単調非減少
    registered_at: int                     # 初回登録時刻

    @property
    def m_live(self) -> int:
        return sum(1 for c in self.constituents if c.alive)

@dataclass(frozen=True, slots=True)
class MeritAccumulator:
    slot_index: int
    registered_at: int
    basis: tuple[float, ...]   # 指数基底 16 本の走る和。時刻を持たない（§C.5.3）
    use_count: float           # σ 加重した使用回数（照合参加 + α×充填）。分母
    ext_use_count: int         # R 外使用の回数。V の β 項の分子。S_q には入れない

@dataclass(frozen=True, slots=True)
class ExceptionAccumulator:
    bits: float                # 例外費用の σ 加重和（残高）
    event_count: int

@dataclass(frozen=True, slots=True)
class EmbedState:
    slot_index: int
    fan_out: float             # 参照先の生存本数から。主腕はこちらを使う
    fan_in_raw: float          # 親からの寄与の和（飽和適用前）。κ 腕でのみ使う

@dataclass(frozen=True, slots=True)
class FrequencyTable:
    """p̂。エージェントごとに一つ。書かれた述語だけで更新する。"""
    counts: Mapping[str, int]      # 述語トークン → 書き込み回数
    total: int                     # counts の総和
    lambda_mix: float              # 一様との混合係数（AgentConfig から）
    alive_vocab: frozenset[str]    # V_vocab。生存出現のある述語

    @classmethod
    def empty(cls, lambda_mix: float = 0.1) -> "FrequencyTable":
        return cls({}, 0, lambda_mix, frozenset())

    def prob(self, predicate: str) -> float:
        V = max(len(self.alive_vocab), 1)
        emp = (self.counts.get(predicate, 0) / self.total) if self.total else 0.0
        return (1 - self.lambda_mix) * emp + self.lambda_mix / V

    def code_length(self, predicate: str) -> float:   # ℓ = −log₂ p̂
        return -math.log2(self.prob(predicate))
```

- `FrozenPrice` の 4 欄は登録時に確定し、以後いっさい再計算しない
- `C_hold = ell_frozen + 1 + 3·arity`
- `c = 1 + 3·arity − 3·新規スロット数`。**新規スロット数 ＝ その構成素にしか現れない引数の数**
- 核一階・核高階・塔は新規スロット 0 → `c = 7`。媒介・周縁A・M4 の `turn` は 1 → `c = 4`
- **行の同一性 ＝ `(slot_index, registered_at)` の対**
- 位置は安定させる。削除しても番号を詰めず、墓石を残す。**位置は再利用され `m_alloc` は伸びない**
- `alive=False` が墓石。レコードは消さない。**関係ノードとしてグラフに残り照合器から見える**が `m_live` に入らない
- `Constituent` に `role`（核／媒介／塔／周縁A）を持たせない
- 高階構成素は `Relation.arguments` に他の `relation_id` を入れて表現する。新しいグラフ型を作らない
- `p̂` は場面に**書かれた**述語だけで更新する。照合参加・充填・R による代理符号化では更新しない
- `p̂` の初期状態は生存語彙上の経験分布と一様の線形混合。混合の形は `λ_mix(1−1/V_vocab)/ln2` から一意に決まる
- `V_vocab` の参入は初回の書き込み、退出は最後の生存出現の消失
- `p̂` はエージェントごとに一つ。判定用と分析用を分けない。モジュール級・プロセス級のシングルトンを禁止

### §C.2.2 `AgentState`（完全宣言）

```python
@dataclass(frozen=True)
class AgentState:
    # --- 層A（既存・改変禁止）---
    prototype: RelationGraph | None = None   # 「箱」。単一グラフ。§C.2.6 を見よ
    public_history: tuple[Any, ...] = ()     # 層A の記録。**verbatim をここに入れない**
    rng_state: str | int | tuple[Any, ...] | None = None
    # --- 層B（B1 で追加する 6 欄）---
    definitions: Mapping[str, NamedDefinition] = field(default_factory=dict)
    merit: Mapping[tuple[str, int], MeritAccumulator] = field(default_factory=dict)
    embed: Mapping[tuple[str, int], EmbedState] = field(default_factory=dict)
    exceptions: ExceptionAccumulator = ExceptionAccumulator(0.0, 0)
    p_hat: FrequencyTable = FrequencyTable.empty()
    slot_history: Mapping[tuple[str, int], frozenset[str]] = field(default_factory=dict)
    verbatim_clock: VerbatimClock = VerbatimClock({})   # §C.2.6
```

- 層A の 3 欄は改変しない。層B の **7 欄**を足すだけ（`verbatim_clock` を含む）
- `merit`・`embed`・`slot_history` のキーは `(R の名前, slot_index)` の**二つ組**
- 行の同一性は `(slot_index, registered_at)` なので、同一位置に二行が生きうる場合はキーを**三つ組へ拡張する**。拡張するなら三つとも同時に動かす。**発生するかは検算項目**
- 各試行末に serialize → deserialize しても軌道が変わらない
- oracle 由来値を `AgentState` に入れない
- 研究者側の分類（段階・窓の開閉・E2/E3・停留・神話／真・①②③・干ばつ中か）をフラグとして持たない
- 来歴タグ（観測／充填／開示／受信）を記憶項目に持たせない。ID の名前空間でも区別しない

### §C.2.6 `prototype`（箱）の状態表現（2026-08-26 新設）

**`prototype` は単一の `RelationGraph` である。**未減衰の verbatim（場面の関係）が
そこに蓄積し、gist（`definitions`）と同居する。**枚ごとに分けて持たない。**

```python
@dataclass(frozen=True, slots=True)
class VerbatimClock:
    """prototype の各関係が、いつ書かれたか。σ の計算に使う。"""
    written_at: Mapping[str, int]    # relation_id → 最後に書かれた試行番号

    def sigma(self, relation_id: str, now: int) -> float:
        """σ = (t − tᵢ)^(−0.5)。単回露出なので n = 1"""
        t_i = self.written_at.get(relation_id)
        if t_i is None:
            return 0.0
        dt = now - t_i
        return float("inf") if dt <= 0 else dt ** -0.5

    def surviving(self, now: int, theta_prime: float) -> frozenset[str]:
        return frozenset(
            rid for rid in self.written_at if self.sigma(rid, now) >= theta_prime
        )
```

**`AgentState` に `verbatim_clock: VerbatimClock` を足す**（層B の 7 欄目）。

- **`prototype` は単一の `RelationGraph`。**層A の型を変えない
- **`prototype` は圧縮された gist と未減衰の verbatim を同時に含み、両者は入力域として区別されない**
- **`public_history` を独立の入力域として持たない。verbatim を `public_history` に入れない**
- **P1 の `map_graphs` は 1 試行に 1 回。**`prototype` を分割して複数回呼ばない
- 関係ごとの書き込み時刻を `VerbatimClock` に持つ。**`Relation.attributes` に来歴を書かない**（D5(a)）
- 同じ `relation_id` が再び書かれたら `written_at` を**更新する**（再強化ではなく上書き。単回露出 `n = 1` を保つ）
- `σ < θ′` の関係を `prototype` から取り除く

**`prototype` の定常サイズ（`θ′` ごと）**

```
θ′ = 0.0410  →  約 595 試行ぶん     2026-08-19 の実測（θ′=0.0476 で 400 枚）と一致
θ′ = 0.1432  →  約  49 試行ぶん
θ′ = 0.2637  →  約  14 試行ぶん
θ′ = 0.3842  →  約   7 試行ぶん
```

**`prototype` の扱いで誤りやすい点（実装者向け）**

| 誤り | 正 |
|---|---|
| verbatim を `public_history` に入れる | **`prototype` に入れる。**`public_history` を独立の入力域にしない |
| `prototype` を枚（場面）の列として持ち、枚ごとに `map_graphs` を呼ぶ | **単一グラフに蓄積し、`map_graphs` は 1 試行に 1 回** |
| 減衰を実装せずに書き込みだけ行う | **必ず `σ < θ′` の関係を取り除く。**入れっぱなしだと巨大化する |
| 書き込み経路を除去して巨大化を避ける | **除去してはならない。**巨大化の原因は減衰が無いこと |

```
「1 試行に 1 回」の根拠（過去の計算コスト実測からの逆算。数値は下に全部書いてある）
  96セル × 160シード × 1,740試行 × 10体 / 16並列 = 51 時間
  → 1 agent-trial あたり 10.99 ms
  → 8/19 が測った map_graphs 1 回ぶんの 4 点平均 11.0 ms と一致する
  「SME 写像は prototype の場面数に線形」は、
  【合成グラフの大きさが枚数に比例するから線形】という意味であって、
  【呼び出し回数が枚数に比例する】という意味ではない
  8/19 の「写像が 1 試行に 3 回走る場合は 3 倍」も、回数が一桁であることを示している
```

### §C.2.3 位置・行・構成素### §C.2.3 位置・行・構成素

| 語 | 定義 |
|---|---|
| 位置 | `def(R)` の枠。番号で識別する。削除しても番号を詰めない |
| 行 | ある位置に、ある時刻に登録された一本の主張。同一性 =（位置番号, 登録時刻） |
| 構成素 | `def(R)` の一行（＝上の「行」） |
| `m_live` | 生きている**行**の数。墓石を含まない |
| `m_alloc` | 確保した**位置**の数。墓石を含む。単調非減少。**再登録では伸びない** |
| 墓石 | `alive=False` の行。**関係ノードとしてグラフに残り、照合器から見える**が `m_live` に入らない |

### §C.2.4 参加率の二語（混同禁止）

| 語 | 定義 | 核 | 塔 | 媒介 | 周縁A |
|---|---|---|---|---|---|
| **役割参加率 `π_role`** | その役割スロットが場面に現れる確率 | 1.0 | 1.0 | **1.0** | `π_A` < 1 |
| **述語再現率 `π_pred`** | 登録した特定の述語がその位置に再び来る確率 | 1.0 | 1.0 | **1/N** | `π_A` |

- **種ファイル `U-011_seed_v1.json` の `pi` 欄は `π_pred` である**
- 媒介は置換型（周縁B）。役割スロットは常に埋まり、中身だけが袋（N 語）から一様に入れ替わる
- 周縁A は不在型。`π_role` も `π_pred` も `π_A`

### §C.2.5 記号（改名済み）

| 現行 | 旧 | 意味 |
|---|---|---|
| `u` | — | 生存座標 `u = (1−h̄)·π_pred·a`。種の `u` 欄 |
| `coin_t` | `u_t` | 共通乱数の引き。`coin_t < f_realized` |
| `tau_acc` | `τ` | 部分照合の採択基準 |
| `lambda_j` | `τ` | σ の時定数。`geomspace(0.3, T×3, 16)` |
| `f_realized` | `f_setting` | その試行で `f(agent, t)` が返した値 |

---

## §C.3 予測（P1〜P7）

### §C.3.1 P1 — 整列（SME）

- 層A の `map_graphs` → `apply_threshold` をそのまま呼ぶ。入出力を変えない
- **base は `prototype`**（A-002）。現行 smoke の配線は `prototype` を読んでいないので、ここを直す
- F-1 の第三分岐（未観測スロット）を変えない。T7b がこれを固定している
- `apply_threshold` の `threshold` は SME の写像受理閾値であり、削除閾値 `θ′` でも採択基準 `tau_acc` でもない

### §C.3.2 P2 — R の同定と `def(R)` の展開

- `definitions` から、整列に噛み合う `R` を選ぶ
- 塔は最新行を読む
- 墓石はグラフノードとして残っているので、展開時に「未観測スロット」として扱う

### §C.3.3 P3 — 採択判定

- `need(m_live) = ⌈tau_acc · m_live⌉`。**比の読みを採る**
- 絶対値の読み `max(m−k, 1)` は**腕として走らせない。事後再構成に留める**
- そのために毎試行 `m_live` と充足数を台帳に残す
- `tau_acc` の主値は 0.67

### §C.3.4 P4 — 充填（**この段が律速**）

- **発火条件は連言**：def 側でスロットが埋まらない **かつ** 場面の可視部にも答えが無い
- 可視部に述語まで一致する相手が実在するなら、充填せず ②／③ の判定へ回す
- 候補プールは `slot_history[(R, slot_index)]`。そこから `p̂` 最頻を選ぶ
- 集合が空なら、署名適合（アリティ＋引数**型**）の `p̂` 最頻へフォールバック
- **`slot_history` には「そのスロットで観測された述語」を入れる**（充足した回だけではない）。充足ベースだと集合が単元集合になり U-079 が解こうとした問題が解けない
- **m1 の登録が使う候補プールは、その整列イベントの対応内容**（＝その回の創設事例の中身）であって、蓄積した `slot_history` ではない。帰結として媒介の `P̂₀ ≥ 0.5`
- `slot_history` は `def(R)` の外に置き、MDL の会計に入れない
- v0 は減衰なし（集合は単調増加）。減衰の実装形は U-085
- 伏せ辺の穴と宙吊り塔スロットは**同じ充填規則**で扱う。由来による例外を置かない
- 充填した内容を `verbatim`・`p̂` に書き戻さない
- 場面を見てから充填の**中身**を決めない
- 発火条件の「かつ」は可視部の**空きの判定**であって、中身を取ることではない
- `T=0` で候補が同点なら棄権する。`α` も加点しない
- 予測出力は**全充填スロット集合**

### §C.3.5 P5 — ②／③ の判定

- **可視部に述語まで一致する相手が実在するかで判定する**
- **写像に載っているかどうかで判定しない**（F-1 の内部判定を下流へ漏らさない）
- ② ＝ その役割スロットが可視部で埋まっており、内容が違う。`f` と無関係に発火する
- ③ ＝ その役割スロットが可視部で空いている。①（隠された）と区別できない

### §C.3.6 P6・P7 — 出力と保留

- 棄権理由は `below_threshold` / `ambiguous_projection` / `no_projectable_relation` の三種に分離して記録する（A-014 の `no_mapping` は実装に存在しないため 2026-08-25 に改名）
- `PendingState`（層A に既存）を予測確定〜フィードバック受領前の保留として使う
- **`predict` は `AgentState` を変えない。**`p̂` 更新・`verbatim` 書き込み・m1 発火・`θ′` 実行を禁じる
- 伏せ位置を参照しない

---

## §C.4 採点（O1〜O3）

- 採点対象は**伏せ辺一本**
- 棄権も全候補一様の満額コストを払う（**研究者側の採点**）
- 結末は**的中／失敗／保留／未解決**の四分類
- オラクルの返答を `def(R)` へ直接書き込まない

---

## §C.5 会計（U3〜U5）

### §C.5.1 節約（登録時に凍結）

```
ℓ_q      = −log₂ p̂(q)
c_q      = 1 + 3·arity − 3·新規スロット数
節約_q   = ℓ_q + c_q
a_q      = 節約_q / ℓ_q
```

- `ℓ_frozen`・`C_hold` は登録時から不変。再計算しない
- 塔の `R.k` 参照価格は `log₂ m_alloc`（墓石を含む）
- 例外の位置価格は `log₂ m_live`（墓石を含まない）

### §C.5.2 功績（二チャネル）

```
S_q = 1.0 × 照合参加  +  α × 充填  +  β × R外使用
P_q = ( σ加重の照合参加 + α × σ加重の充填 ) ÷ σ加重の R 適用回数
```

- 功績の基準は**充足**（`def(R)` が名指した述語がその位置に実在したか）であって写像の成立ではない
- 同一試行で同一構成素に二重加算しない
- **`α` を得る「充填」は採点された充填に限る**（狭い読み）
- `p̂` は書き限定で更新する。照合参加・充填では上げない

### §C.5.3 例外費用と `ΔL`

```
例外費用 E = 2 + log₂ m_live + ℓ_r
ΔL_q(t)   = 節約_q − (σ加重の例外費用の和) / (σ加重の q の使用回数)
V_q(t)    → (M_q·節約_q − C_q·E_q) / ℓ_q
生存条件   P·a + w·(embed/ℓ_frozen) ≥ θ′
```

- **`V_q` の正規化分母は `q` の使用回数**（創設事例 2 件を含む）。経過場面数ではない
- ①と② は同額。同一の累積器 M-051 へ流す。分けない
- 修復圧は**採点された予測に寄与した構成素**とその上の塔にだけ届く。同じ試行の他の充填には伝播させない
- ①の届き先は**広い読み**（照合に参加した構成素すべて）
- 宙吊りスロットの充填が外れたときの課金は、宙吊り参照を持つ構成素（塔・核高階）が受ける
- 単位はビット

### §C.5.4 棄権時の課金（`abstain_charge`）

| 棄権理由 | 課金 |
| --- | --- |
| `below_threshold` | **課金しない**（R を適用していない） |
| `no_projectable_relation` | **腕 `abstain_charge` で切り替える** |
| `ambiguous_projection` | 同上（同じ腕で動かす） |

- 課金する腕では、宛先は写像に載った構成素すべて（判断2 の広い読み）
- 課金する腕では `ℓ_r` を「その位置に実在した述語の符号長」で固定する。全候補一様 `log₂ V_vocab` は感度として記録
- **走行台帳に `arm_abstain_charge` を持つ**

### §C.5.5 初期化（D-cold）

- 行の初期化は C2。創設事例（登録時の整列に使われた場面。通常 2 件）の**在／不在**を、登録時点で既に経過した時間だけ減衰させた強度として分子・分母に継承する
- 例外費用累積器の初期値は分子 0・分母は創設事例 2 件分
- **新規行が登録直後に落ちることはない**

---

### §C.5.6 verbatim の保持と減衰（2026-08-26 新設）

**`prototype` は箱である。**その中に **verbatim**（逐語記録）と **gist**（名前つき出現の記録）が
同居し、**verbatim から gist が出てくる。**`prototype` と `definitions` を別物として扱わない。
**箱の実体は単一の `RelationGraph` である**（§C.2.6）。

| 語 | 定義 |
|---|---|
| **verbatim** | 試行ごとに記録される生の一階関係。「試行 37 で `push(石,箱)` を見た」という個別の記録。同じ述語が再登場しても**別件の新しい記録**であり、古い記録の再強化ではない（単回露出・痕跡 1 本） |
| **gist** | 名前つきの出現の記録。「試行 37 で `R(石,箱)` が起きた」。verbatim と**並列に**保存され、**独立に**減衰する。**verbatim のほうが速く落ちる** |
| **def(R) の行** | 「R の定義に `push` が含まれる」という一般的な主張。verbatim とは**別の帳簿** |

- **U1 は現在試行の場面を verbatim として `prototype` へ書く。**特別な初回登録経路を置かない
- **U2 は `RevealedEdge` を、観測と同じ経路で verbatim として書く。**受信専用ストア・来歴タグを置かない
- **verbatim と gist は並列保持。独立に減衰させる。m1 の登録時に verbatim を置換・削除しない**
- 近時加重の事象強度は全項目共通の核 `σ(t) = Σᵢ (t − tᵢ)^(−0.5)`（ACT-R の減衰指数 `d = 0.5`）
- **verbatim の可用性は分母なしの蓄積型 `P = σ/(1+σ)`。**σ が非有界なので、可用性を 0〜1 の量として扱うときは飽和が要る
- **落ちる判定は `σ(t) < θ′`。`P` ではなく `σ` を `θ′` と比べる。**単回露出なので `n = 1`、生存時間 `τ = 1/θ′²`
- **検索閾値は一つだけ。`theta_prime` を verbatim の閾値としても使う。**`theta` という別欄を置かない
- **`prototype` は gist と「未減衰の」verbatim を同時に含む。**落ちた verbatim は `prototype` に残らない
- **段階2 ＝ R と def(R) が生存・使用される一方、そのパターンの最後の verbatim が `θ′` で落ちた時点**
- **A5（走行前検査）**：同一モチーフの提示間隔 `S` ≤ verbatim の生存時間 `τ`。すなわち `θ′ ≤ 1/√S`

**`θ′` の 4 点それぞれで、verbatim の生存時間はこうなる。**

| `θ′` | `τ = 1/θ′²` | A5（`S = 4`） |
|---|---|---|
| 0.0410 | 595 試行 | 満たす |
| 0.1432 | 49 試行 | 満たす |
| 0.2637 | 14 試行 | 満たす |
| 0.3842 | 7 試行 | 満たす（余裕 1.7 倍） |

**落ちる判定の誤りやすい点（実装者向け）**

| 誤り | 正 |
|---|---|
| `P = σ/(1+σ) < θ′` で落ちると判定する | **`σ(t) < θ′` で判定する。**`P` は可用性の表し方であって落ちる判定ではない |
| `theta` という別欄を作る | **`theta_prime` を使う。**検索閾値は一つ |
| `prototype` への書き込みを止める | **書き込みは正しい。**巨大化の原因は減衰が無いこと |

```
P で判定すると生存時間が ((1−θ′)/θ′)² になり、確定済みの二項目と食い違う
  S-005（確定 8/14）  A5：θ′ ≤ 1/√S は閉形式 t* = (n/θ′)² から出る
  T-010（確定 8/18）  T ≈ 1,740 の内訳「段階2 完了 θ=0.050 → (1/θ)² = 400」
                      P で判定すると 361 になり、台帳の 400 と合わない
さらに P で判定すると θ′ = 0.3842 で τ = 2.6 < S = 4 となり、A5 を破る
```

**`θ′` を上げると、構成素が落ちやすくなると同時に verbatim も速く落ちる。**
これは副作用ではなく、**同じ検索閾値だからである。**段階2 が早く来る。

**`prototype` を `None` のまま進めてはならない（2026-08-26 実測）**

| 症状 | 原因 |
|---|---|
| 全試行が `Abstain(no_projectable_relation)` | `base` が場面自身で代用され、自分と自分を整列させている |
| 削除が 0 件（`θ′` を上げても） | 登録した行が必ず当たるので `V` が下がらない |
| 四モチーフの述語が 1 つの `R` に積み上がる | どの場面とも整列してしまう |

```
U1 の書き込み経路を「prototype が巨大化する」という理由で除去してはならない
巨大化の原因は書き込みではなく、減衰が無いことである
prototype が None のまま試行が進むことは実装の誤りである（受け入れ試験 4-12）
```

---

## §C.6 登録と削除（U6・U7）

### §C.6.0 U1・U2 — 場面と開示辺の符号化（2026-08-26 復帰）

一試行の手順は P1〜P7・O1〜O3・U1〜U8 の **18 手順**である。U1・U2・O2・O3 を落とさないこと。

- **U1**：現在試行の場面を `verbatim` として書き込む。`predict` 確定後に行う
- **U2**：`RevealedEdge` があれば、**観測と同じ経路で**書く。受信専用ストア・来歴タグを置かない
- 開示事象の分子重みは `γ = α`（開示は充填と同じ扱い）。主格子は α=0 なので実際に走る値は γ=0
- 充填した内容は U1・U2 のどちらでも書かない

### §C.6.0b O2・O3 — オラクルのフィードバックと台帳記録（2026-08-26 復帰）

- **O2**：`coin_t < f(agent, t)` なら `RevealedEdge` を返す。そうでなければ何も返さない（＝保留）
- `f` は定数で書かず `f(agent, t)` の署名で研究者側に持つ。各試行で実際に返した値を記録する
- **O3**：研究者側台帳に記録する。提示事象・伏せ位置・①②③・`coin_t`・`f_realized`・`f_fired` を毎試行
- O1〜O3 はオラクル側の手順であり、エージェントの入力域に入らない

### §C.6.1 順序

- **U6（m1 の登録）を先に、U7（θ′ の削除）を後に走らせる**

### §C.6.2 U6 — m1（登録と更新）

**文法 G2（入力域）・G4b（発火）・G5（探索規律）・G6（終了）を実装する。**

#### G2 — 入力域

- **m1 の入力域は、`prototype` と現在の場面の整列によって生じた「対応集合」である**
- `prototype` は gist と未減衰 verbatim を同時に含み、**両者は入力域として区別されない**
- **hold-out 位置に対応する部分を候補から除外する**（充填は予測であって記録ではない）
- **`public_history` を独立の入力域として持たない**

**整列の走らせ方**

```
map_graphs(prototype, 場面) を 1 回だけ走らせる
prototype は単一グラフなので、写像結果も 1 つ。複数結果からの選択は起きない
その 1 つの対応集合が、そのまま m1 の入力域である
prototype を枚に分割して複数回呼ばない（§C.2.6）
```

#### G4b — 発火条件

- **初回登録は第二整列イベント。**固定保持試行数 `n` を置かない（`θ′` が決める）
- 整列イベント ＝ `prototype` のいずれかの枚と現在の場面が構造的に噛み合うこと
- **その後の整列でも `def(R)` を更新する**（二度目だけ登録して以後固定、ではない）
- **再抽象化は新しい `R′` を作らず、既存の `def(R)` を更新する**

#### G5 — 探索規律

- **候補プールは G4b の整列対応集合。サイズ 2 以上の部分集合を、`V` が増える限り貪欲に選ぶ**
- **原理は `V` 一本。「最初の一本は特別」という規則を置かない**
- ビーム幅 `k = 1`（貪欲）を主腕とする。`k > 1` はロバストネス腕であり B1 では実装しない
- 全列挙は測定面専用。**エージェント面では実行しない**

**【変遷】**8/13 の「高階優先・支持 S2 最大の核」は M-042（2026-08-18）に上書き済み。
**核の概念を実装しない。**サイズ 2 以上の部分集合を `V` の貪欲で選ぶ単一規則である。

#### G6 — 終了条件

- **G5 の併合が停止した時点（`V` が増える併合が無くなった時点）で終了する。独立した終了条件を持たない**

#### 登録の中身

- **登録が使う候補プールは、その整列イベントの対応内容**（＝その回の創設事例の中身）であって、蓄積した `slot_history` ではない
- 整列対応位置に記録するのは「候補プールの `p̂` 最頻」。**設計者が述語を指名しない**
- `m_alloc` は単調非減少。**再登録では位置を再利用する**
- 同じ位置の再登録にも D-cold を適用する（`P̂₀ ≥ 0.5`）

### §C.6.3 U7 — θ′ による削除

- 削除の判定は一箇所で `V < θ′` だけ
- `V` の形と閾値は全項目種で同一（verbatim・gist・`def(R)` 構成素・塔・出現）
- 免除・床を置かない
- 容量 `C` を執行者にしない。LRU・優先度キュー・周期的掃除を禁じる
- 削除に関する閾値は `θ′` 一つ
- 失敗登録・衝突・宙吊りは `S`/`V` を動かすだけで、直接削除を起こさない
- embed の fan-in 集約 `g(x)=κx/(κ+x)` は親の寄与を足した後に一度だけ適用する
- embed は削除判定の直前時点の生存親で計算する
- `θ′` の 4 点は `0.0410 / 0.1432 / 0.2637 / 0.3842`、天井 0.3842。第5段を置かない
- E2（`R` 自体の忘却）は `θ′` 経由でのみ起きる。`m1` が `R := ∅` を書かない

---

## §C.7 走行台帳（U8）

### §C.7.0 欄の完全一覧（固有 85 欄）

**この一覧が定義である。**欄を足さないこと。足りないと感じたら停止して報告する。
**重複して現れる 3 欄（`agent_state_snapshot_hash`・`abstain_reason`・`f_realized`）は 1 欄として数える。**

**(a) 層A から継承する 38 欄**（`abm/logging_schema.py` の `LOGGING_SCHEMA_FIELDS` をそのまま）

```
run_id, agent_id, partner_id, timestamp, prediction_order, seed_id, target_type,
constitution, threshold, lambda, correction_mode, error_correlation, sending_policy,
agent_state_snapshot_hash, prototype_version, prototype_present_at_presentation,
instance_id, observable_mask_edges, prediction_kind, predicted_edge, abstain_reason,
hit, coverage, frequency_baseline_prediction, frequency_baseline_hit,
flat_matcher_baseline_prediction, flat_matcher_baseline_hit, accuracy,
L_H, L_DgH, description_length, coordination_gain, error_agreement,
alignment_candidates, pruning_events, activated_prototype, total_score_breakdown,
error_direction_prototype_consistent
```

**(b) D23 が必須とする 22 欄**（取り直しがきかない量）

```
coin_t, f_realized, f_fired, feedback_content, held_out_content,
expansion_and_filling_all, constituent_reason_123, type2_fired, predictions_all_slots,
oracle_verdict, registration_event, deletion_event, deletion_ell, tie_event,
dangling_ref_onset, dangling_ref_duration, R_used, def_R_diff,
agent_state_snapshot_hash, state_snapshot, transmission_event, scene_G_star_ref
```

**(c) 層B で追加する 23 欄**（機構側）

```
m_alloc, m_live, theta_prime, tau, constituent_states, entity_map_covered,
slot_history_size, filled_predicate, slot_signature, support_at_adoption,
charge_source, verbatim_written, reg_del_events, exception_bits_charged,
M051_balance, matcher, abstain_reason, n_tie_candidates, candidate_distribution,
enumeration_version, V_vocab, merit_event_times, outcome_category
```

**(d) 研究者側でのみ計算する 5 欄**（台帳には持つが、エージェントは読まない）

```
p1, p0, Sel, OA, f_realized
```

```
記載合計  38 + 22 + 23 + 5 = 88
重複       agent_state_snapshot_hash（a と b）／abstain_reason（a と c）／f_realized（b と d）  = 3
固有       85

※ pending_rate・h_subjective は導出量であって欄を足さない。台帳の他の欄から計算する
```

**主要な欄の中身**

| 欄 | 型・中身 |
|---|---|
| `constituent_states` | 構成素ごとの `{slot_index, registered_at, alive, S_q, S_q内訳(照合参加/充填), ext_use_count, u, embed}`。`ext_use_count` は `S_q` の内訳ではなく `V` の β 項の分子 |
| `entity_map_covered` | 伏せられた行の実体引数がすべて `entity_mapping` に載っていたか（bool） |
| `slot_history_size` | スロット履歴集合の要素数 |
| `filled_predicate` / `slot_signature` | 充填に選ばれた述語 ／ 充填したスロットの署名 |
| `support_at_adoption` | 採択時の支持量。**採択に失敗した試行でも達成された充足数を書く**（null 回避の 0 を入れない） |
| `charge_source` | ① 由来か ② 由来か |
| `verbatim_written` | その試行で verbatim を書いたか |
| `reg_del_events` | 位置別の登録・削除イベント時系列 |
| `coin_t` / `f_realized` / `f_fired` | 共通乱数の引き ／ `f(agent,t)` が返した値 ／ `RevealedEdge` が返ったか |
| `constituent_reason_123` | 構成素ごとの ①②③ の分類（台帳側でのみ計算する） |

- 台帳はループから**同期・追記のみ**で書く。集計・後処理をしない
- 台帳を無効化するフラグを作らない
- ループの各試行末に append が一回以上呼ばれる
- 台帳はループから見て write-only。read API を作らない
- 欄数は **固有 85 欄 ＋ run ヘッダ 12 本**。§C.7.0 の一覧が定義である。**見出しの数字ではなく実リストを数え上げて検証する**
- `support_at_adoption` は**採択に失敗した試行でも達成された充足数を書く**。null 回避の 0 を入れない
- `f_realized` を毎試行記録する（`f(agent, t)` が返した値）
- 提示事象と隠された位置も毎試行記録する
- `m_live` の時系列を毎試行記録する（振動の周期を測るため）

### §C.7.1 腕記述子（7 → 12 本）

**`RunHeader` の完全宣言。**フィールドを足さないこと。**全 12 本が必須で、既定値を持たない。**

```python
ARM_DESCRIPTOR_FIELDS = (
    # 既存 7 本（型・順序を変えない）
    "arm_alpha", "arm_beta", "arm_w", "arm_kappa", "arm_repair_scope",
    "arm_holdout_repr", "arm_f_profile",
    # 2026-08-25 に追加する 5 本
    "arm_lambda_mix", "arm_abstain_charge",
    "arm_temperature", "arm_d_shared", "arm_adaptation_table",
)
if len(ARM_DESCRIPTOR_FIELDS) != 12:
    raise RuntimeError(f"腕記述子は12本ではない: {len(ARM_DESCRIPTOR_FIELDS)}")

@dataclass(frozen=True)
class RunHeader:
    # --- 既存 7 本（改変しない）---
    arm_alpha: float            # 充填の功績割引率。三点 {0, 0.5, 1}
    arm_beta: float             # R外使用の重み。三点 {0, 中点, 1}
    arm_w: float                # embed の重み
    arm_kappa: float            # fan-in 飽和。κ→∞ を含む関数族内
    arm_repair_scope: str       # 修復圧の塔方向の射程（"first" / "second" / "all"）
    arm_holdout_repr: str       # hold-out の表現
    arm_f_profile: str          # f のプロファイル（"constant" / "drought" など）
    # --- 追加 5 本（すべて必須。既定値を置かない）---
    arm_lambda_mix: float       # p̂ の混合係数。二点
    arm_abstain_charge: str     # 棄権時の課金の有無。二点
    arm_temperature: float      # 温度 T
    arm_d_shared: str           # d を共有するか。二点
    arm_adaptation_table: str   # 適応表。二点

    def to_dict(self) -> dict[str, Any]:
        return {field: getattr(self, field) for field in ARM_DESCRIPTOR_FIELDS}
```

- アサーションを `!= 7` から `!= 12` へ変更する
- **12 本すべて必須。既定値（デフォルト引数）を置かない**
- 既存 7 本の**名前・型・並び順を変えない**。追加 5 本は末尾に足す
- 連続量は `float`、選択肢は `str`。`AgentConfig` 側で `Enum` を使う場合も、台帳へは `str` で書く
- **既存の呼び出し側（`tests/test_ledger_b0b2.py` の `RunHeader(0.3, 0.2, 0.1, 4.0, "tower", "removed", "constant")`）は、追加 5 本を明示して渡すよう更新する**
- 欄を持つことと掃引に使うことは別。**掃引で振るかどうかに関わらず、12 本すべてを毎 run 記録する**
- `theta_prime`・`tau_acc`・`f_realized` は毎試行の欄なので腕記述子に重複させない
- `C`（容量）は執行者ではなく測定量なので腕にしない

### §C.7.2 なぜ既定値を置かないのか

**これは実装の裁量ではなく、二つの決定からの帰結である。**

```
D14   α・β・ρ・w・罰の射程・温度・θ・λ_mix・d は AgentConfig の名前つき値
      コード構造で片側に固定しない
      「決めなかった」項目（α・β）を実装が決めない
      → 既定値を置くことは「実装が決める」ことに当たる

D23   台帳だけから再計算できること
      U-088 の理由：λ_mix はエージェント側の値なので world_hash・seed_hash が変わらない
      欄が無い（あるいは既定値で埋まる）と、腕違いの run が台帳上で区別できない
      → 既定値があると、腕を明示しない run が記録できてしまう
```

**したがって呼び出し側は 12 本すべてを明示する。**既存テストの `RunHeader(...)` 呼び出しは、
追加 5 本を書き足して更新する（これは「テストを変更・削除・スキップする」に当たらない。
**公開契約が変わったことへの追随であり、検査内容は変えない**）。

**掃引に使わない腕も記録する。**たとえば主格子で `temperature = 0` に固定していても、
`arm_temperature = 0.0` を毎 run 書く。**固定していたことが台帳から読めなければ、
後から「温度を振っていない run だった」と確認できない。**

---

## §C.8 縦断ループ

- 試行 `t` の `AgentState` を `t+1` へ持ち越す
- 一走行の提示回数は `T ≈ 1,740` 試行
- 共通乱数は `coin[agent,t] < f_realized` で引く。`Bernoulli(f)` を直接引かない
- 世界列はエージェント状態・予測・正誤・掃引パラメータに依存しない。`world_hash` が全腕で一致する
- 実行順は `predict` → `oracle` → `update` の一方向。`update` の中で予測をやり直さない

---

## §C.9 受け入れ試験

**本節は自己完結している。**期待値はすべてここに書かれている。上流文書を参照しないこと。
**試験が落ちたら、原因を特定して直してから先へ進む。直せない（＝本書に書かれていない）なら停止して報告する。**

### 段1 — 型と器（走行不要）

| # | 検査 | 期待 |
|---|---|---|
| 1-1 | `a_q = (ℓ_q + c_q)/ℓ_q` が種の `a` 欄と小数点以下 4 桁まで一致 | 24 構成素すべて |
| 1-2 | `c_q = 1 + 3·arity − 3·新規スロット数` が種の `c` 欄と一致 | 24 構成素すべて |
| 1-3 | `ℓ_q = −log₂(rate_q / Z)`、`Z = 8.134472` が種の `ell` 欄と一致 | 24 構成素すべて |
| 1-4 | `Σ p̂ = 1.000000` | — |
| 1-5 | 媒介・周縁A の `(a,e)` 型で `c = 4` が出る | — |
| 1-6 | `sha256` キーを除いた辞書を `json.dumps(d, sort_keys=True, separators=(',',':'), ensure_ascii=False)` した文字列の sha256 | `ddc0e4ae928c08ce252c6f9456eb74e9e8044716a4b4e4c84d6c36daa974d858` |
| 1-7 | `AgentState` の層B 6 欄が存在する | `definitions` / `merit` / `embed` / `exceptions` / `p_hat` / `slot_history` |
| 1-8 | oracle 由来値が `AgentState` に入っていない | 静的検査 |
| 1-9 | 研究者側の分類（段階1/2/3・窓の開閉・E2/E3・停留・神話／真・①②③・干ばつ中か）がフラグとして無い | 静的検査 |
| 1-10 | `config.theta_prime` の取りうる 4 点が種の `theta_grid` と一致 | `0.0410 / 0.1432 / 0.2637 / 0.3842`。第5段を置かない |
| 1-11 | 走行台帳の欄数 | **固有 85 欄**（層A 38 ／ D23 22 ／ 層B 23 ／ 研究者側 5 ＝ 記載 88、重複 3：`agent_state_snapshot_hash`・`abstain_reason`・`f_realized`）。§C.7.0 の一覧と一致すること |
| 1-12 | run ヘッダの腕記述子 | **12 本**。`!= 12` でアサート |

### 段2 — 静的会計（走行不要）

```
u（媒介・周縁A）   M1 0.088886 ／ M2 0.095324 ／ M3 0.191062 ／ M4 0.336381
u（核・塔）        1.44 〜 2.67
```

| # | 検査 | 期待 |
|---|---|---|
| 2-1 | 各 `θ′` で落ちる集合 | `0.0410` なし（全モチーフ 6）／`0.1432` M1・M2 の媒介・周縁A／`0.2637` さらに M3／`0.3842` さらに M4 |
| 2-2 | `E(m_live=4) = 2 + log₂ 4 + ℓ_r` の総額 | **9.024 bit** |
| 2-3 | `λ_mix` の床 `λ_mix(1−1/V_vocab)/ln2` が種の最小 `ℓ`（3.02 bit・`allow`）より下 | `λ=0.1` で 0.1416 bit |
| 2-4 | `V_q(t)` が定常値へ**単調に落ちる** | 分子 0 から、分母は創設事例 2 件から |
| 2-5 | `V_q` の正規化分母が `q` の**使用回数**（創設事例 2 件を含む） | 経過場面数を分母にしない |
| 2-6 | 過適用が課金を増やすと同時に**功績も減らす** | 片方だけ動かす実装を弾く |
| 2-7 | **媒介の行の登録直後の `P̂`** | **≥ 0.5。`P̂₀ = 0` の行が出たら、m1 の登録が蓄積した `slot_history` を読んでいる** |
| 2-8 | 媒介が `θ′` を割るまでの適用回数（`P̂₀=0.5`） | M1 `0.1432` 26／`0.2637` 7／`0.3842` 4。M2 は 29／7／4。**最短は M2 周縁A の 3 回** |
| 2-9 | 同一試行での登録と削除が起きないこと | 起きたら `P̂₀` が 0 になっている |

### 段3 — 照合と充填（8,000 場面・`τ_acc = 0.67`・`m = 4`）

**T7 期待表（伏せ辺なし）**

| R | 採択率 | OA | 核一階1 | 核一階2 | 核高階 | 塔 |
|---|---|---|---|---|---|---|
| M1 | 0.250 | 0.000 | 1.000 | 1.000 | 1.000 | 1.000 |
| M2 | 0.500 | 0.500 | 0.500 | 0.500 | 1.000 | 1.000 |
| M3 | 0.500 | 0.500 | 0.500 | 0.500 | 1.000 | 1.000 |
| M4 | 0.250 | 0.000 | 1.000 | 1.000 | 1.000 | 1.000 |

| # | 検査 | 期待 |
|---|---|---|
| 3-1 | **伏せ辺の有無で採択率・OA が変わらない** | 上表 |
| 3-2 | 核の充足率（M1・M4）が `(1−h̄)` を再現する | **0.788 / 0.810**（`1−h̄ = 0.797`） |
| 3-3 | **回帰検査**：次の値が出たら照合器の第三分岐（未観測スロット）が外れている | M1 0.150/0.000、M2 0.345/0.574、M3 0.349/0.564、M4 0.248/0.397 |
| 3-4 | `τ_acc` の感度 | `0.67` 上表／`0.75` **0.67 と同値**／`0.50` 全モチーフ崩壊（OA 0.750・核の充足率 0.250） |
| 3-5 | `m=5` の階段が `m=4` と同一 | 上表と一致。媒介の充足率 ≈ `1/N` で課金の 75〜94% を吸う |
| 3-6 | `m=2` で `no_projectable_relation` が 2,000/2,000 にならない | P4 が発火している |
| 3-7 | `m=2` で予測が出る確率 | **`3·h̄ ≈ 0.61`**（残り ≈ 0.39 は棄権） |
| 3-8 | **`m=4` で伏せられた生存行に予測が出る** | **M1 0.406 ／ M2 0.414 ／ M3 0.384 ／ M4 0.184**（n=2000・τ=0.67） |
| 3-9 | **`m=4` の過剰適用先での予測率** | **M2 0.384 ／ M3 0.414**（M1・M4 は OA=0 なので 0.000） |
| 3-10 | **`m=6` でも `m=4` と同じ予測率** | 周縁A の生死は核の投影に影響しない |
| 3-11 | **M4 の予測率だけ低いこと** | `0.184 ≈ h̄`。`turn(b,c)` の `c` が def(M4) の中でこの行にしか現れないため。**種の構造であってバグではない** |
| 3-12 | **充填経路（宙吊りスロット）からは予測が出ない** | 媒介の `e` が `entity_mapping` に載る率 **0.000**（m=4・m=6 とも） |
| 3-13 | `entity_map_covered` 欄が毎試行埋まる | 伏せられた行の実体引数が全部写像されたかの bool |
| 3-14 | 登録直後の `slot_history_size` | **≥ 1**。0 なら D-cold の継承が効いていない |
| 3-15 | フォールバック（署名適合の `p̂` 最頻）に到達する回数 | v0 では **0 回**。1 回でも立つなら継承が抜けている |
| 3-16 | **②／③ は可視部に述語まで一致する相手が実在するかで判定する。写像に載っているかで判定しない** | 静的検査（コードの分岐を見る） |
| 3-17 | 印が出る側（核・媒介）で ③ が起きない | 8,000 場面で 0 件 |
| 3-18 | `T=0` で候補が同点なら棄権する。`α` も加点しない | — |

### 段4 — 薄化の到達点（縮小走行）

| # | 検査 | 期待 |
|---|---|---|
| 4-1 | `V(定常)`（`f = 0`） | M1・M4 の核・塔 **変化なし**（課金ゼロ）／M2 `carry` **0.230**／M2 `lift` **0.238**／M3 `break` **0.259**／M3 `cut` **0.239** |
| 4-2 | **①率（適用あたり・`f=1`）** | **M1 0.000 ／ M2 0.210 ／ M3 0.216 ／ M4 0.000** |
| 4-3 | ① が立つのは**過剰適用先のみ** | 自モチーフでの予測は的中率 1.000・失敗 0.000。過剰適用先は的中 0.000・失敗 1.000 |
| 4-4 | **M1・M4 では `f` を 0→1 に振っても核の `V` が動かない** | OA = 0.000 なので ① が原理的に立たない |
| 4-5 | M2 `carry` の `V` が `θ′` を割る `f` | `0.1432` で **≈ 0.218**／`0.041` で **≈ 0.475** |
| 4-6 | M3 `break` の `V` が `θ′` を割る `f` | `0.1432` で **≈ 0.306**／`0.041` で **≈ 0.577** |
| 4-7 | `charge_source` 欄で ①／② が分離できる | — |
| 4-8 | `θ′` ごとの到達点（`f = 0`） | `0.0410 / 0.1432` **全モチーフ `m=4` で停止**／`0.2637 / 0.3842` **M1・M4 は `m=4`、M2・M3 は `m=2`** |
| 4-9 | `m=2` で採択率・OA が `m=4` から変わらない | 0.500 / 0.500 のまま |
| 4-10 | `m_live` の時系列と、位置別の登録／削除イベント時系列を毎試行記録する | 欄の存在 |
| 4-11 | **`m_alloc` が有界であること** | 再登録は位置を再利用する。伸び続けたら実装が位置を再利用していない |
| 4-12 | **`run_longitudinal` の後、`prototype` が `None` でないこと** | U1 が verbatim を書いていない。`base` が場面自身で代用され、全試行が `no_projectable_relation` になる |
| 4-13 | **`run_longitudinal` の後、予測が出た試行が 0 件でないこと** | 4-12 と同じ根で落ちる |
| 4-14 | **`run_longitudinal` の後、削除イベントが 0 件でないこと**（`θ′ = 0.2637` で 400 試行） | θ′ が発火していない。V の計算か `apply_theta` の接続 |
| 4-15 | **`prototype` の関係数が単調増加し続けないこと** | verbatim が減衰していない。落ちた verbatim が残っている |
| 4-16 | **`len(definitions) ≤ 4` かつ 各 `definition.m_alloc ≤ 6`** | R の同定が四モチーフを 1 つにまとめている／位置を再利用していない |
| 4-17 | **`m_live` が単調増加でないこと**（薄化が起きること） | 登録した行が必ず当たっていると V が下がらない。`base` が場面自身になっていないか |
| 4-18 | **`prototype` の関係数が `θ′` ごとの定常値に収束すること** | 場面 1 枚あたり約 9 関係として、`θ′=0.3842` で約 60、`θ′=0.2637` で約 130（±50%）。伸び続けるなら減衰が効いていない |
| 4-19 | **`public_history` に場面（`RelationGraph`）が入っていないこと** | G2 違反。verbatim は `prototype` に入れる |
| 4-20 | **P1 の `map_graphs` 呼び出しが 1 試行に 1 回であること** | `prototype` を分割して複数回呼んでいる。§C.2.6 の実測（1 agent-trial あたり 11 ms）と合わない |

### 段5 — 台帳の完全性と再現性

| # | 検査 | 期待 |
|---|---|---|
| 5-1 | `NON_NULL` 欄が毎試行埋まる | `oracle_verdict` / `support_at_adoption` / `m_live` / `constituent_reason_123` ほか |
| 5-2 | `support_at_adoption` が**採択に失敗した試行でも**達成された充足数を書く | null 回避の 0 を入れない |
| 5-3 | `f` は `f(agent, t)` の署名で持ち、**実際に経験した `f` を毎試行記録する** | 数値リテラルで `f` を参照する箇所が無いことの静的検査 |
| 5-4 | 提示事象と隠された位置も毎試行記録する | ①/② の率を期間ごとに復元するため |
| 5-5 | **再走行なしで、通過率・干ばつ判定が台帳だけから再計算できる** | 段4 の出力に対して実行して一致 |
| 5-6 | 同一（run seed, config）で `PYTHONHASHSEED` によらず全 run がビット一致 | — |
| 5-7 | 各試行末に `AgentState` を serialize → deserialize しても軌道が変わらない | 完全一致 |
| 5-8 | 測定関数の前後で `agent_state_snapshot_hash` が不変 | — |
| 5-9 | 削除の呼び出し元が一箇所であることの静的検査 | — |
| 5-10 | `AgentState` の変異点が m1／`apply_theta`／統計更新の三つだけであることの静的検査 | — |
| 5-11 | `ℓ_frozen` が登録時から不変であるテスト | — |

---

## §C.10 この文書で決めた小さな既定

| # | 既定 | 理由 |
|---|---|---|
| 1 | `project` を書き換えず、P4 を後段に足す | T7/T7b の期待表と F-1 の検証を動かさないため  |
| 2 | 墓石はグラフノードとして残す（`relation_id` を保持） | F-1 の第三分岐が「未観測スロット」として扱えるため  |
| 3 | 棄権時の `ℓ_r` は「その位置に実在した述語の符号長」で固定 | 全候補一様との差は 0.15 bit 以下で結論を変えない  |
| 4 | 構成素は「行」で数える（`m_live` は行数、`m_alloc` は位置数） | U-033 の行の同一性から  |
| 5 | `coin_t`・`f_realized` へ改名する | 生存座標 `u` との衝突、および非一様 `f` での意味の分離  |

---

## §C.11 範囲外（ここでは実装しない）

```
B3      個体測定の集計（通過率・神話質量 M / M*・①的中率の時系列）
B4      伝達
掃引の実行（主格子・代表セル・N・シード予算）  ← 別ブロック。本書の範囲外
非一様 f  f(agent,t) の署名と f_realized の記録は本書で持つが、
          時間的非一様（干ばつ）・空間的非一様（群ごとの f）の腕は置かない
```

- これらのフックは定義だけ置き、呼ばれたら `NotImplementedError` を投げる
- **非一様 `f` は後から腕として足せる形にしておく。**`f(agent, t)` の署名を崩さず、`f_realized` を毎試行記録する

---

## §C.12 未決（B1 の範囲に残るもの）

| 番号 | 内容 | 重さ | 影響 |
|---|---|---|---|
| U-018 残 | `R` の出現の価格、スロット宣言の符号形（H-066） | 中 | gist の価格。符号器を書けば決まる |
| U-085 | `slot_history` の減衰（v1）の実装形 | 軽 | v0 は減衰なしで走る |
| U-035 | `error_direction_prototype_consistent` を残すか ① 的中率時系列へ置換するか | 中 | 台帳欄の去就 |
| 隙間2 | 宙吊り充填の課金先が M-008 で閉じるか（§C.5.3・確信度 70%） | 中 | **U-071 の駆動経路。red team 最優先** |
| U-062 | E3 の操作定義が「薄化」を検査していない（過去への条件） | 中 | 本件とは別問題。§C.6.2 の同定は 2026-08-26 に撤回 |
| **N-E3** | E3 は状態述語であって吸収状態ではない（吸収状態の条件） | 重 | `m_live` の振動を停留と呼べるか。**推定量は試走後に決めるので、B1 では判定を実装しない。振動の周期・振幅と、位置別の登録／削除イベント時系列を記録するだけにする** |
| U-030 | 主張階層 | 中 | 台帳の欄が確定しない。B3 の入口 |
| 主格子・代表セル | 具体的な格子構成と固定セルが未定 | 中 | **B1 の実装を止めない。**`arm_*` は 12 本すべてを毎 run 記録するので、掃引の設計が決まっていなくても実装できる（§C.7.2） |
