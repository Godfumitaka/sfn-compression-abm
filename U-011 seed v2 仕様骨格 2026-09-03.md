# U-011_seed_v2 仕様 — 骨格（下書き）

作成 2026-09-03 ／ 起草 Claude ／ **承認待ち・空欄あり**

★ 本文書は骨格である。**§4 の 3 件は空欄**で、確認結果とアストラさんの判断を待つ。

---

## §0 何のために作るか（一行）

**「有用で複雑な抽象が薄い神話になる」過程を、模型の中で観測可能にする。**

★ 2026-09-03 の実測により、現行の種では観測できないことが確定した。

```
m_alloc_true   最大 5 ／ 中央 4（def 73 本・repro_seed36）
m_live_birth   全 73 本が m_alloc_true と一致（登録時の切り詰めは無い）
降下の実例     試行 18 で登録（4 本）→ 試行 22 で 2 本  ★ 4 試行で崩落
塔の深さ       1（world.py:110,112。関係を引数に取る関係を引数に取る記述が無い）
```

★ **4 本から 2 本は「過程」ではなく二段の崩落である。**軌跡を持つには段数が要る。

---

## §1 現行（v1）の構造 — 確認済み

`world.py:107-128` より、1 場面の関係は次で構成される。

| 層 | 述語の決まり方 | 試行間で共有されるか | rate |
|---|---|---|---|
| core1 | `MOTIF_ROWS` で固定 | **される** | 0.25〜0.5 |
| core2 | `MOTIF_ROWS` で固定 | **される** | 0.25〜0.5 |
| higher | `MOTIF_ROWS` で固定 | **される** | 0.25（require）／0.75（cause） |
| tower | **常に `allow`** | **される** | 1.0 |
| role_unary | モチーフごと固定 | **される** | — |
| peripheral | モチーフごと固定・確率 π_A で出現 | される | 0.02〜0.30 |
| mediator | `bags` から毎試行 `rng.choice` | **されない** | 0.13 |
| glue | ランダム・1〜3 本 | **されない** | — |

★ **共有されうるのは 5〜6 本。**毎試行 1 本が伏せられるため、実効は 4〜5。

★ **`allow` が rate 1.0 なのは実験上の都合であり、理論的な要請ではない**（アストラさん・2026-09-03）。

---

## §2 v2 の目標（★ 数値つき）

| # | 目標 | 根拠 |
|---|---|---|
| G-1 | **m_alloc_true の中央を 8〜10 にする** | 8→2 の降下に 8 段が要る。現行は 4 |
| G-2 | **深さ 2 以上の塔を成立させる** | T-107（射程軸の縮退）は「高さ2以上の塔が立たない」ことによる。部分採択が構造的に意味を持つために必要 |
| G-3 | **次数と共有頻度の交絡を解く** | 現行は高階＝常時共有、媒介＝毎回変化。図1 の三層が構造の帰結か機構の帰結かを区別できない |

★ G-3 は、`allow` の rate を 1.0 から下げ、代わりに**一階の述語を全モチーフ固定にする**ことで達成する。

---

## §3 変更する箇所（★ 確定部分）

### 3-1　`abm/world.py:20-25` の `MOTIF_ROWS`

```
現行  (core1, core2, higher, peripheral_predicate, core_entity_roles)  ← 5 要素
v2    核一階を 2 本 → 4〜5 本
      深さ 2 の塔を 1〜2 本追加
```

★ **追加する述語はすべて互いに異なること。**`abstraction.py:163` は生存中の述語と
  同じ述語の追加を弾くため、同じ述語を増やしても m_live は動かない。

### 3-2　`abm/world.py:107-114` の `relations`

深さ 2 の塔は、**引数に `higher` または `tower` を取る関係**として書く。

```
現行  tower = allow(mediator, core1)          ← 引数は一階の関係。深さ 1
v2    tower2 = <述語>(tower, core2) のような形  ← 引数に関係を引数とする関係
```

★ `sme.py:296-311` は高階の引数を関係どうしで対応させる。
  **引数側の下部構造も共有されていなければ写像に現れない。**
  したがって tower2 の引数は、共有される層（core / higher / tower）から取ること。

### 3-3　触らないもの（★ 明示）

```
mediator   bags からの毎試行抽選を維持する。意図的に不安定な層であり、変えると理論が変わる
glue       維持
role_unary 維持
π_A        媒介・周縁の設計値。§4-3 の判断がつくまで触らない
```

---

## §4 ★ 空欄（★ 判断待ち・確認待ち）

### 4-1　★ ρ（死んだ述語の再追加を許すか） — **U-107・未決**

```
ρ = 1.0  現行（abstraction.py:163 が生存中の述語のみ除外）
ρ = 0.0  再追加を禁じる（ラチェット）
```

★ **両側に一級の支持がある。**

```
ρ=0 側  SEQL/GEL の累進的整列（Kuehne et al. 2000）は 抽象の単調なスキーマ化を要求する
ρ=1 側  Koriat & Goldsmith の粒度統制は 可逆・戦略的である
```

★ **実測（repro_seed36・def 73 本）**

```
n_increases > 0        57 / 73（78.1%）
m_live_max > m_live_birth  16 / 73（21.9%）
```

★ **埋め戻しは失った分を戻すだけでなく、初期値を超えて積み増す。**
  **種を大きくしても、この機構が働けば厚いまま振動するだけである。**

★ **決められない場合は掃引軸へ格上げする**（S1・G の前例に倣う）。

### 4-2　★ 次数の効果があるか — **確認待ち**

`委任_図1が種の帰結でないかの確認` の結果を待つ。

```
M1 の require（核高階・rate 0.25）と hold（核一階1・rate 0.25）の生存率

  同程度        → 図1 の三層は共有頻度の反映。★ G-3 の優先度が上がる
  require が高い → 次数が独立に効いている。★ 図1 は生き残る
```

### 4-3　★ 追加する述語の具体名と層構成 — **4-1・4-2 の後**

★ 数だけは決まる（§2 の G-1 より、共有されうる層を 5〜6 → 9〜11 本）。
  **どの述語をどの層に置くかは 4-2 の結果に依存する。**

---

## §5 種ファイルの生成手順（★ 全式が復元済み。手で書く箇所は無い）

出所：SPEC §3.2.1（2026-08-23 起草）。**本日、種ファイルの全 24 構成素で検算済み。**

```
1  Z = 核一階の本数 + 平均π_A + 媒介1 + glue平均 + 高階の本数 + 塔の本数 + 役割ユナリー1
     ★ v1 では 2 + 0.134472 + 1 + 2 + 1 + 1 + 1 = 8.134472

2  rate(述語) = 1 試行あたりの期待出現回数
     ★ モチーフ出現確率（1/4）× その層の出現確率 の総和

3  marginal(述語) = rate / Z

4  ℓ = −log₂(marginal)                    ★ 述語の符号長

5  c = 1 + 3·arity − 3·新規スロット数       ★ 節約の構造分
     ★ 検算  核系 arity2・新規0 → 7 ／ 媒介・周縁 arity2・新規1 → 4（ファイルと一致）

6  a = 1 + c/ℓ                             ★ 正規化保持価値

7  u = a × π × (1 − h)                     ★ 生存座標
     ★ π  核 1.0 ／ 媒介 1/bag_size ／ 周縁 π_A

8  θ′ 格子を u の隙間に置く                 ★ 設計余裕 δ_m を確保する
```

### ★ 5-1　重大な帰結 — θ′ 格子は置き直しになる

構成素を足すと **Z が増える → marginal が下がる → ℓ が上がる → a と u が下がる。**

```
★ v1 の θ′ 格子（0.041 / 0.1432 / 0.2637 / 0.3842）は v1 の u の隙間に置いたものである
★ ★ v2 では置き直しが必要
★ ★ ★ したがって v1 と v2 の θ′ 軸の結果は 直接比較できない
```

★ **この点を走行前に台帳へ記録すること。**後から「同じ θ′ で比べた」と読むと誤る。

---

## §6 実装上の障害（★ 先に洗い出す）

### 6-1　試験が種の値を複写している

```
tests/test_b1_acceptance.py:39-44   MOTIFS（MOTIF_ROWS の複写）
tests/test_b1_acceptance.py:46      PI_A
tests/test_b1_acceptance.py:47      PERIPHERAL
tests/test_b1_acceptance.py:48      GLUE
tests/test_b1_acceptance.py:337-347 m_live == 期待値（M1 4 / M2 2 / M3 2 / M4 4）
tests/test_b1_acceptance.py:362     assert all(m_alloc <= 6)
tests/test_b1_acceptance.py:438     assert all(m_alloc <= 6)
```

★ **v2 ですべて落ちる。**実装前に grep で全件を洗い出すこと（規範11d）。

### 6-2　走行費用

`sme.py:288-289` は base 側と partial 側の関係の二重ループである。
場面が 7〜10 本から 14〜18 本になれば、**候補生成は 2〜4 倍**。

```
現行  1 走行 207 秒（Mac・多コア）
v2    400〜800 秒（★ 推定。★ 1 セルで実測して確かめること）
2,560 走行なら K=6 で 47〜95 時間
```

★ **掃引の点数を減らす設計が要る。**実測前に全掃引を組まない。

### 6-3　sweep スクリプトが 3 本ある

```
sweep.py ／ sweep_ldecouple.py ／ sweep_ORIGINAL.py
★ arm_d_shared は後ろ 2 本にしか現れない。★ どれが正か未確認
```

---

## §7 v2 の成否の判定（★ 走行前に登録すること・規範13）

★ **数値を見てから基準を選ばない。**下記は暫定案であり、承認時に確定する。

| # | 判定 |
|---|---|
| P-1 | `m_alloc_true` の中央が **8 以上** |
| P-2 | 深さ 2 の塔を持つ def が存在する（★ 本数の下限は未定） |
| P-3 | `m_live_max ≥ 6` かつ `m_live_final ≤ 2` の def が存在する ★ 本研究の中心 |
| P-4 | 1 走行の実時間が **800 秒以下** |

★ **退化条件**（★ これに当たったら発見ではなく退化として扱う）

```
X-1  神話率が 95% を超える（全 def が最小 m へ落ちる）
X-2  生存する def の数が v1 の 1 割を切る（★ 神話ではなく絶滅）
```

★ `analyze_rev6.py:285-286` により、生存 0 の def は系列から消える。
  **X-2 は `final_def_count` で見ること。**系列からは見えない。

---

## §8 未記入の項目（★ この骨格に無いもの）

```
1  追加する述語の具体名（§4-3）
2  ρ の扱い（§4-1）
3  θ′ 格子の具体値（§5-1。u が決まってから計算する）
4  掃引の点数（§6-2。実測してから）
5  P-2 の本数の下限（§7）
```
