# 走行前の読み方（pre-registration）  2026-09-15

★ この文書の役割
  走行を起動する前に、何が出たら何と読むかをここに書き、**その後で**起動する。
  走行を見てから読み方を決めない（規範13）。

---

## ★★ 規範46 — 「固定」と「転記」を混同しない

  下記の読み方は **すべて 2026-09-15 のチャット上で、各腕の起動前に固定した**。
  ★ ただし **本文書への記録は事後である**。
  ★★ **本文書に書かれた時刻を 固定時刻として扱わないこと。**

  | 群 | 固定 | 転記 | 起動 |
  |---|---|---|---|
  | N 群（N1・N2） | 起動前 | 起動前 | ★ 未起動 |
  | **S 群（区画1）** | **起動前** | ★★ **起動後**（転記時点で 44/120 走行中） | 2026-09-15T09:34:05Z |
  | C 群（区画2） | 起動前 | 起動前 | ★ 未起動 |
  | A 群（区画3・4・5） | 起動前 | 起動前 | ★ 未起動 |
  | F 群（区画6） | 起動前 | 起動前 | ★ 未起動 |
  | V 群（区画7） | 起動前 | 起動前 | ★ 未起動 |
  | 付録A（H 群・X 群） | 起動前 | ★ 事後 | 走行済・判定済 |

  ★ 付録A（H 群・X 群）は **事前登録としては扱わない**。既に判定が出ている。
    記録として残す目的は、当たり外れの型を後から分析できるようにすることである。

## ★ 記入規約

  1  一項＝一つの読み方。「何の腕で」「何の量が」「どちら向きに」「いくつを境に」を書く。
  2  各項に **固定日時** を書く。★ 対応する腕の走行開始より前の日時であること。
  3  修正するときは値を書き換えるだけにせず、**旧値・新値・理由・修正日時** を残す。
  4  ★★ 本文が未記入の項に対応する腕は **起動しない**。
  5  腕の同定は config のパスと **種の sha256** で行う。★ 腕の名前を信じない（仮C-55）。
  6  ★ 規範44  Claude が出したい方向の読みは、事前確率を自分で下げてから書く。

## ★ 腕の割り当て（確定）

  | 区画 | 腕 | config | 読み方 | 状態 |
  |---|---|---|---|---|
  | 区画1 | λ=1 × 種 v2a | `sweep_lam_v2a_2026-09-15.json` | S-1・S-2 | ★ 走行中 44/120 |
  | 区画2 | λ=1 × charge | `sweep_lam_charge_2026-09-15.json` | C-1・C-2 | ★ 未起動 |
  | 区画3 | λ=1 × 会計 α=1 | `sweep_lam_alpha1_2026-09-15.json` | A-1 | ★ 未起動 |
  | 区画4 | λ=1 × 会計 β=1 | `sweep_lam_beta1_2026-09-15.json` | A-2 | ★ 未起動 |
  | 区画5 | λ=1 × w=2 | `sweep_lam_w2_2026-09-15.json` | A-3・A-4 | ★ 未起動 |
  | 区画6 | λ=1 × flat2 | `sweep_lam_flat2_2026-09-15.json` | F-1 | ★ 未起動 |
  | 区画7 | λ=1 × 四階 v3a2 | `sweep_lam_v3a2_2026-09-15.json` | V-1・V-2 | ★ 未起動 |
  | N1・N2 | λ=1 × decay | `sweep_lam_n1/n2_2026-09-15.json` | N-1〜N-6 | ★ 未起動 |

  ★ 区画2〜5 の config は 2026-09-15 に作成した（区画α を雛形に 1 キーだけ変えた。
    ★ `seed_file` は書いていないので 種は既定 `73f17e28…`）。dry-run 済み・未起動。
  ★ 基準腕  **区画α**（`sweep_lam_alpha_2026-09-15.json` ／ λ=1・一様 f=0.5・既定の種）。
    ★ 区画α〜δ（ギリシャ文字）は λ と f を振った別系列であり、区画1〜7（数字）とは別物。
  ★ κ を振った腕は存在しない（全 config で κ=1.0）。★ 読み方も無い。
    ★ 併せて κ は乗る相手（`fan_in_raw`）が常に 0 で、恒等的に効かない（仮C-82）。
  ★★ h2 / k0（二階を伏せる腕）は **V 群に含まない**。付録A の H 群・X 群である。

## ★★ 名前の三系列 — 同じ文字が三つの意味で使われている

  | 系列 | 意味 | 例 | 読み方 |
  |---|---|---|---|
  | **区画α〜δ**（ギリシャ文字） | λ と f を振った系列 | α = λ=1・一様 f=0.5・既定種（**基準腕**）／β = f=0.0 ／γ = f=1.0 ／δ = λ=0.5 | 無し（基準として参照するだけ） |
  | **区画1〜7**（数字） | λ=1 × 既存の腕 の交差 | 1 = v2a ／ 2 = charge ／ 3 = α=1 ／ 4 = β=1 ／ 5 = w=2 ／ 6 = flat2 ／ 7 = 四階 v3a2 | S・C・A・F・V |
  | **会計の α・β**（記号） | `AgentConfig` の係数 | α = 充填の功績割引率 ／ β = R 外での使用を功績に数える | A-1・A-2 |

  ★★ 三つは互いに無関係である。とくに **区画α（基準腕）と 会計 α=1（区画3）は別物**。
  ★ 経緯  前版（commit 8f46c9d）で **区画2〜5 を lam_alpha〜lam_delta に当てる誤り**が起きた。
    ★ 紛らわしい命名をしたのは Claude 側である。★ この表を置いて再発を防ぐ。

---

## N 群 — 非一様フィードバック f（`lam_n1` ／ `lam_n2`）

固定日時  2026-09-15（★ N1・N2 の起動より前）
対象  N1 = `config/sweep_lam_n1_2026-09-15.json` → `runs/lam_n1_2026-09-15`（`decay:0.900000->0.100000`）
      N2 = `config/sweep_lam_n2_2026-09-15.json` → `runs/lam_n2_2026-09-15`（`decay:0.100000->0.900000`）
      種は既定（直下「U-011 seed v2a.json」・sha `73f17e28c463172f…`・★ 中身は v2c）
      f の理論平均  N1 = 0.500230 ／ N2 = 0.499770
      他は区画α と同一  θ′=[2.1, 2.3, 2.45]・λ=1.0・充填 2 点・T=1740・seed 1〜20

★★ 読むときの注意（八件目の予防）
  `f_setting` も `cell` 名も区画α と同じ `0.5` / `f0.5000_…` になる。**腕の区別に使えない。**
  区別できるのは `arm_f_profile` と `f_realized` の実測だけ。解析は
  `--runs runs/lam_n1_2026-09-15` のように **走行根ごとに別々に回し、出力を混ぜない**。

### N-1  ★ 0.55
  ★★ 両腕とも 署名（被覆率↓・的中率↑・的中数↓）が 一様 f 0→1 と同じ向きで出る。
  ★ 判定  区画α（λ=1・一様 f=0.5）を基準に、N1・N2 の f の高い区間と低い区間を比べる。

  ★★ 修正の経緯
    旧値 0.70 → 新値 0.55 ／ 修正日時 2026-09-15（★ N1・N2 の起動より前）
    理由  `runs/fdecay_2026-09-09`（λ=0・decay 0.9→0.1・θ′ 0.1/0.9/1.2・480 走行）を
          事後に解析したところ、署名の三条件が揃うのは 24 セル中 6 セルで、
          最多は「的中率も下がる」9 セルだった。
    ★ ただし この解析は事後であり、確認的な結果として扱えない（規範13）。
      ★ 探索として扱い、事前確率の更新にのみ用いた。
    ★ 割り引きを 0.70 → 0.55 に留めた理由
      (i)   fdecay は λ=0 で、N1・N2 は λ=1 である
      (ii)  θ′ の格子が違う（0.1/0.9/1.2 対 2.1/2.3/2.45）
      (iii) θ′=0.1 の 8 セルは 的中率が両腕とも 1.00000 で飽和しており ↑↓ を判定できない。
            ★ 判定可能なのは 16 セル中 6 セル

### N-2  ★ 0.60
  ★ 神話（越境して生まれた定義が出した誤答）の件数が、N1（減衰）では時間後半に偏り、
    N2（成長）では前半に偏る。

  ★★ 修正の経緯
    旧値 0.65 → 新値 0.60 ／ 修正日時 2026-09-15（★ 走行前・N1・N2 未起動）
    理由  端点 0 を含まないので ①課金が完全には止まらない。
          f1 = 0.1 なら後半でも 10% は開示が立つため、偏りが 0.65 まで振れない。
  ★ 根拠  f が低い区間では `coin.f_fired` が立ちにくく ①課金が減る → 訂正が削除に伝わらない。
    （★ f=0 で λ が生存に触れないことは 測定3 で確認済み。厳50/緩140 完全一致）

### N-3  0.60
  ★ N1・N2 の的中率が、同じ平均 f の一様腕（区画α 0.4835）と ±0.02 を超えて違う。
  ★ 違えば「f の平均ではなく 分布が効く」。

### N-4  0.65
  ★★ N1 と N2 の 的中数が 互いに ±5% を超えて違う。
  ★★ 違えば「効いているのは 時間の順序である」が確定する。
  ★ 同じなら「平均だけが効く」。★ どちらに転んでも結果になる。
  ★ N1 と N2 は平均も分散も同一で、違うのは時間の順序だけである
    （N1 平均 0.500230 ／ N2 平均 0.499770・実測）。
  ★ 規範44 により 0.75 → 0.65 に下げてある。

### N-5  0.55
  ★ N2（成長・訂正が後から来る）のほうが N1 より 神話が多い。
  ★ 根拠  訂正は定義を直さず削るだけである（`loop.py:292-304` は例外費用を積むのみで
    `definitions` を書き換えない）。→ 早期に形成された神話は後からの訂正では修復されず、
    薄いまま生き残るはず。
  ★ 規範44 により 0.65 → 0.55 に下げてある。

### N-6  0.55
  ★ 削除の総件数が N1 と N2 で ±5% 以内。
  ★ 総量が同じで 時間分布だけ違うなら、N-4 の差は 順序の効果と読める。

---

## S 群 — λ × 種 v2a（`lam_v2a`）★★ 最重要

固定日時  2026-09-15（★ 区画1 の起動より前）
★★ 本文書への転記は **起動後**（転記時点で 44/120 走行中）。規範46 の注意を参照。
対象  `config/sweep_lam_v2a_2026-09-15.json` → `runs/lam_v2a_2026-09-15`
      種 = `seeds/U-011_seed_v2a.json`・sha `c9c61796a815427c…`

### S-1  0.70
  ★★ v2a の神話が base 族より多いという向きが λ=1 でも保たれる。
  ★ 判定  λ×v2a の神話件数 ＞ 区画α の 1,812。
  ★ 比較相手  v2a（λ=0）16,967 ／ 0.4775 ／ 0.0813 ／ 148・970 ／ ★ 神話 2,253
  ★★ 外れた場合  「神話は種の設計のアーチファクトではないか」という査読の疑いへの反証が
     λ の下で崩れる。→ ★ ポスターから落とすか、λ=0 の結果として述べる。
  ★ 前提の確認（P-1c で実測済み）
    v2a 腕の種は `c9c61796…`（真の v2a・根述語 allow/depend/prevent/block）
    base 族の種は `73f17e28…`（v2c・根述語 allow/depend/allow/depend）
    → ★ 「v2a は三階の共有を外した腕」という記述と 種の中身は一致している。

### S-2  0.60
  ★ v2a の中心的過程（緩 970）が λ=1 で 増える。
  ★ 根拠  区画α で base 803 → 859（+7.0%）。

---

## C 群 — λ × charge（`lam_charge`）

固定日時  2026-09-15（★ 区画2 の起動より前）
対象  区画2  `config/sweep_lam_charge_2026-09-15.json` → `runs/lam_charge_2026-09-15`
★ 比較相手  charge（λ=0）12,998 ／ 7,949 ／ 0.6116 ／ 0.0623 ／ 198,266 ／ 厳69・緩1,739

### C-1  0.70
  ★ charge の署名（被覆率↓・的中率↑・的中数↓）が λ=1 でも保たれる。
  ★ 意味  charge は f 軸と機構が正反対（f は経路の構成で効き、charge は経路の構成が 0%）
    なのに 同じ署名を出す。★ これが λ の下でも成り立つかを見る。

### C-2  0.55
  ★ 薄い発話の入れ替わり（厚→薄 1,350 → 1,970）が λ=1 で縮む。
  ★ 根拠  λ は充填の選択を決定的にする。★ 薄い定義の生き残り方が変わりうる。

---

## A 群 — λ × 会計（`lam_alpha1` ／ `lam_beta1` ／ `lam_w2`）

固定日時  2026-09-15（★ 区画3・4・5 の起動より前）
対象  区画3  `config/sweep_lam_alpha1_2026-09-15.json` → `runs/lam_alpha1_2026-09-15`
      区画4  `config/sweep_lam_beta1_2026-09-15.json`  → `runs/lam_beta1_2026-09-15`
      区画5  `config/sweep_lam_w2_2026-09-15.json`     → `runs/lam_w2_2026-09-15`
      ★ 3 本とも種は既定（`73f17e28…`）。`seed_file` は書いていない。

### A-1  0.60
  ★ α（充填の功績割引率）は λ の下でも結論を動かさない。
  ★ 対象  区画3（λ=1 × 会計 α=1）
  ★ 判定  下記の量が λ=1 で **±5% 以内**。

  ★ 基準値  alpha1（λ=0）`runs/alpha1_2026-09-13/alpha1`・台帳 120 本・T=1740・種 `73f17e28…`
    ★ 2026-09-15 に台帳から数えた（★ 区画3 の起動より前）。

  | 量 | alpha1（λ=0） | ±5% の許容 |
  |---|---|---|
  | 発話 | **19,100** | 18,145 〜 20,055 |
  | 的中 | **9,721** | 9,235 〜 10,207 |
  | 的中率 | **0.5090** | 0.4836 〜 0.5345 |
  | 被覆率 | **0.0915** | 0.0869 〜 0.0961 |
  | 削除 | **144,621** | 137,390 〜 151,852 |
  | 中心的過程（厳） | ★★ **定義待ち** | — |
  | 中心的過程（緩） | ★★ **定義待ち** | — |
  | 神話 | ★★ **定義待ち** | — |

  ★ 数え方（★ 引用値との検算つき）
    発話 = Σ `coverage` ／ 的中 = Σ `hit` ／ 的中率 = 的中 ÷ 発話 ／ 被覆率 = 発話 ÷ (T × 台帳数)
    削除 = `deletion_event` のうち ★ **`kind=="deletion"` のみ**。
      `verbatim_deletion` は毎試行 1 件出るので T−1 = 1,739 × 120 = 208,680 件を差し引く。
    ★ 検算  charge  12,998 ／ 7,949 ／ 0.6116 ／ 0.0623 ／ 198,266 → **引用値と完全一致**
            flat2   19,054 ／ 0.5039 ／ 0.0913            → **引用値と完全一致**

  ★★ 中心的過程（厳・緩）と 神話 は **定義が特定できなかった**。
    リポジトリ内の実装（`compare_abstain.py` の `central` = 生存最大≥6 かつ 最終≤2、
    `diag_thickness.py` の 方式CS／方式REG、`verify_process_v2.py` ほか）を当てても
    引用値（charge 厳69・緩1,739 ／ flat2 厳69・緩814 ／ v2a 厳148・緩970）を再現できない。
    ★ 定義をもらえれば 同じ手順で数えて埋める。
  ★★ **規約4 により、この 3 欄が埋まるまで 区画3 は起動しない。**

### A-2  0.55
  ★ β=1 の projection 的中率 0.9956 が λ で 下がる。
  ★ 対象  区画4（λ=1 × 会計 β=1）
  ★ 根拠  λ は充填の経路にしか効かないはずだが、β=1 は R 外での使用を功績に数えるので
    定義の生き残り方が変わる。

### A-3  0.50
  ★ w2 の「一階の削除が高階より大きく減る」が λ でも出る。
  ★ 対象  区画5（λ=1 × w=2）
  ★ 経緯  w2 では 削除の減少の 82% が一階だった（一階 −12,612 ／ 二階 −1,818 ／ 三階 −953）。
    ★ 一階に w の直接経路は無い（`fan_out = 0`）。
  ★ 註  「A-3」は 高速化の A-3（`abm/loop.py` の `_diff` 同一物短絡）にも使われている。別物。

### A-4  0.65
  ★★ w2 で 高階のみ群の削除率が λ=1 でも 2.0006 付近に下がる。
  ★ 対象  区画5（λ=1 × w=2）
  ★ 経緯  λ は この量を −0.5% しか動かさない（区画α 2.1113 → 2.1012）が、
    w は −5.2% 動かす（2.1113 → 2.0006）。★★ 両方かけたときに 加算的か 干渉するかは未知。

---

## F 群 — λ × flat2（`lam_flat2`）

固定日時  2026-09-15（★ 区画6 の起動より前）
対象  `config/sweep_lam_flat2_2026-09-15.json` → `runs/lam_flat2_2026-09-15`
      種 = `seeds/U-011_seed_v2cflat2.json`・sha `dc60c3a98be136a4…`
      ★ 2026-09-15 に `analysis_bags_2026-09-12/seeds/` から `seeds/` へコピー。sha 同一。

### F-1  0.75
  ★ flat2 ≒ base という関係が λ=1 でも保たれる。
  ★ 比較相手  flat2（λ=0）19,054 ／ 0.5039 ／ 0.0913 ／ 厳69・緩814 ／ 神話 1,803
  ★ 判定  λ×flat2 の主要量が 区画α と ±5% 以内。
  ★ 意味  袋（種の配り方）の効果が小さいことの再確認。★ S-1 と対になる。
    ★ 差が出るのが v2a だけであることを示す。

---

## V 群 — λ × 四階（`lam_v3a2`）

固定日時  2026-09-15（★ 区画7 の起動より前）
対象  `config/sweep_lam_v3a2_2026-09-15.json` → `runs/lam_v3a2_2026-09-15`
      種 = `seeds/U-011_seed_v3a2.json`・sha `5f3e5601333ed9f6…`
★★ h2 / k0（二階を伏せる腕）は V 群に含まない。★ そちらは付録A の H 群・X 群。

### V-1  0.70
  ★ v3a2 でも λ の向きが 区画α と同じ。
  ★ 判定  的中率・充填の的中率・神話の三つで 符号が区画α と一致する。
  ★ 註  v3a2 は 1 本が base の約 3 倍（1,568〜1,664 秒）、台帳 64MB。
    ★ 高階の語 14 個・subtrees 12 個・constituents 68 件（他の種の約 2 倍）。

### V-2  0.65
  ★ λ×v3a2 の高階のみ群の削除率が、同じ腕の「一階あり m>=3」群の削除率より **高い**。
  ★ 判定  ★★ 群内の順序で判定する（★ 絶対値では判定しない）。

  ★★ 修正の経緯
    旧値  λ×v3a2 の高階のみ群の削除率が 3.3097 と ±0.05 以内        0.60
    新値  同じ腕の「一階あり m>=3」群の削除率より 高い（群内の順序）   0.65
    修正日時  2026-09-15（★ 区画7 の起動より前）
    理由  比較相手 `runs/cliff_v3a2_2026-09-13` は **T=600・21 本**、区画7 は **T=1740・120 本**。
          ★ 削除率は時間の関数なので、絶対値の比較が成立しない（規範42 の同型）。
  ★ 根拠  群内の順序（高階のみ ＞ 一階あり m>=3）は
    base 2.1113 対 0.7458 でも v3a2 3.3097 対 1.8036 でも成立しており、**T に依らない**。
  ★ V-1（λ の向きが区画α と同じ）は 符号の比較なので T の違いに影響されない。そのままでよい。

---

# 付録A  判定済みの読み方（★ 事後転記・記録のみ）

★★ 下記はいずれも 各腕の起動前にチャット上で固定し、既に判定が出ている。
   ★ 本文書への転記は事後である。★★ **事前登録としては扱わない。**
   ★ 記録として残す目的は、当たり外れの型を後から分析できるようにすることである。

## H 群 — 高階を伏せる（`h2`・旗 True × λ=1）

  | 項 | 読み方 | 事前 | 判定 |
  |---|---|---|---|
  | H-1 | ★★ 高階を予測した発話が 0 件でなくなる | 0.85 | ★ **当たり**（938 件） |
  | H-2 | ★★ 高階のみ群の削除率が α の 2.1012 より上がる | 0.70 | ★ **外れ**（2.0027・下がった） |
  | H-3 | ★ 神話が α の 1,812 より減る | 0.55 | ★ **当たり**（484） |
  | H-4 | ★ 全体の的中率が α の 0.4835 より下がる | 0.70 | ★★ **外れ**（0.5973・上がった） |
  | H-5 | ★ 二階の削除件数が α の 48,292 より増える | 0.65 | ★ **外れ**（16,713） |
  | H-6 | ★★ 二階の行への①課金が α より増える | 0.75 | ★ **外れ**（5,114 → 1,890） |
  | H-7 | ★ 二階が伏せられた割合が 20〜26% に収まる | 0.80 | ★ **当たり**（22.15%） |

  ★ H-3  件数では当たり、機構は逆。越境の発話自体が −81.7%。
    越境の誤答率は 0.6490 → 0.9472 と上がっている。
  ★ H-5  件数では外れ、削除「率」では当たり（0.9200 → 0.9462）。
  ★ H-7  理論値 H/(F+H+1+E[Bern]+E[Unif]) ≒ 22.0% と一致。

  ★ 当たり 3 ／ 外れ 4。★ 事前 0.70・0.65・0.75 の読みが 3 本外れた。
  ★★ 「高階は間違えられないから生き残っていた」（H-2・H-5・H-6）は三つとも出なかった。
  ★★ 代わりに出たのは「二階を伏せると 投影の足場が失われ モデルが黙る」
     `7_no_projectable_relation` +755% ／ 発話 −47.9% ／ 被覆率 0.0918 → 0.0478。

## X 群 — 高階と λ の分離（`k0`・旗 True × λ=0）

  | 項 | 読み方 | 事前 | 判定 |
  |---|---|---|---|
  | X-1 | ★★ 的中率が h2 の 0.5973 と ±0.02 以内 | 0.60 | ★ **当たり**（0.5934） |
  | X-2 | ★★ 発話が h2 の 9,986 と ±5% 以内 | 0.65 | ★ **当たり**（9,661・−3.3%） |
  | X-3 | ★ `7_no_projectable` が ±5% 以内 | 0.75 | ★ **当たり**（152,177・−0.13%） |
  | X-4 | ★ `filling_live` が 0 件でない | 0.70 | ★ **当たり**（50 件・0.9600） |
  | X-5 | ★ 神話が h2 の 484 より少ない | 0.55 | ★ **外れ**（484・完全同数） |
  | X-6 | ★ 高階のみ群の削除率が ±0.05 以内 | 0.70 | ★ **未判定**（三分を要確認） |
  | X-7 | ★ 越境の誤答率が h2 の 0.9472 と ±0.03 以内 | 0.70 | ★ **当たり**（0.9546） |

  ★ 当たり 5 ／ 外れ 1 ／ 未判定 1。
  ★★ 結論  h2 の的中率上昇 +0.1138 のうち λ の寄与は +0.0039（3.4%）。
     ★ 残り 96.6% は旗の効果である。
  ★ X-5 の「完全同数」は λ が動かさないのではなく 打ち消し合った結果である。
    ★ 越境の発話 +0.8% × 越境の誤答率 −0.8%。★ 生出力の件数で検算すること。

---

# 付録B  腕の実測（★ 2026-09-15 時点。台帳ヘッダから確認した事実）

## 区画1〜7（λ=1 の本系列）

| 区画 | 腕 | config | 走行ディレクトリ | 種の sha | f | λ | α β w κ | charge | 二階 | 台帳 | 走行開始 |
|---|---|---|---|---|---|---|---|---|---|---|---|
| 1 | v2a | `sweep_lam_v2a_2026-09-15` | `runs/lam_v2a_2026-09-15` | `c9c61796…` | uniform 0.5 | 1.0 | 0 0 0 1 | false | false | 走行中 | 2026-09-15T09:34:05Z |
| 2 | charge | `sweep_lam_charge_2026-09-15` | `runs/lam_charge_2026-09-15` | 既定 `73f17e28…` | uniform 0.5 | 1.0 | 0 0 0 1 | **true** | false | 0 | ★ 未起動 |
| 3 | α=1 | `sweep_lam_alpha1_2026-09-15` | `runs/lam_alpha1_2026-09-15` | 既定 `73f17e28…` | uniform 0.5 | 1.0 | **1** 0 0 1 | false | false | 0 | ★ 未起動 |
| 4 | β=1 | `sweep_lam_beta1_2026-09-15` | `runs/lam_beta1_2026-09-15` | 既定 `73f17e28…` | uniform 0.5 | 1.0 | 0 **1** 0 1 | false | false | 0 | ★ 未起動 |
| 5 | w=2 | `sweep_lam_w2_2026-09-15` | `runs/lam_w2_2026-09-15` | 既定 `73f17e28…` | uniform 0.5 | 1.0 | 0 0 **2** 1 | false | false | 0 | ★ 未起動 |
| 6 | flat2 | `sweep_lam_flat2_2026-09-15` | `runs/lam_flat2_2026-09-15` | `dc60c3a9…` | uniform 0.5 | 1.0 | 0 0 0 1 | false | false | 0 | ★ 未起動 |
| 7 | v3a2（四階） | `sweep_lam_v3a2_2026-09-15` | `runs/lam_v3a2_2026-09-15` | `5f3e5601…` | uniform 0.5 | 1.0 | 0 0 0 1 | false | false | 0 | ★ 未起動 |
| N1 | decay 0.9→0.1 | `sweep_lam_n1_2026-09-15` | `runs/lam_n1_2026-09-15` | 既定 `73f17e28…` | **decay** | 1.0 | 0 0 0 1 | false | false | 0 | ★ 未起動 |
| N2 | decay 0.1→0.9 | `sweep_lam_n2_2026-09-15` | `runs/lam_n2_2026-09-15` | 既定 `73f17e28…` | **decay** | 1.0 | 0 0 0 1 | false | false | 0 | ★ 未起動 |

## 区画α〜δ（λ と f を振った別系列・基準腕を含む）

| 区画 | config | 走行ディレクトリ | 種の sha | f | λ | 台帳 | 走行開始 |
|---|---|---|---|---|---|---|---|
| **α（基準腕）** | `sweep_lam_alpha_2026-09-15` | `runs/lam_alpha_2026-09-15` | 既定 `73f17e28…` | uniform 0.5 | 1.0 | 120 | 2026-09-14T15:49:54Z |
| β | `sweep_lam_beta_2026-09-15` | `runs/lam_beta_2026-09-15` | 既定 `73f17e28…` | uniform **0.0** | 1.0 | 120 | 2026-09-14T18:18:57Z |
| γ | `sweep_lam_gamma_2026-09-15` | `runs/lam_gamma_2026-09-15` | 既定 `73f17e28…` | uniform **1.0** | 1.0 | 120 | 2026-09-14T22:33:44Z |
| δ | `sweep_lam_delta_2026-09-15` | `runs/lam_delta_2026-09-15` | 既定 `73f17e28…` | uniform 0.5 | **0.5** | 120 | 2026-09-15T00:59:29Z |

## H 群・X 群、および λ=0 の比較相手

| 腕 | config | 走行ディレクトリ | 種の sha | λ | α β w κ | charge | 二階 | 台帳 | 走行開始 |
|---|---|---|---|---|---|---|---|---|---|
| h2 | `sweep_h2_2026-09-15` | `runs/h2_2026-09-15` | `573c5c53…` | 1.0 | 0 0 0 1 | false | **true** | 120 | 2026-09-15T04:58:52Z |
| k0 | `sweep_k0_2026-09-15` | `runs/k0_2026-09-15` | `573c5c53…` | **0.0** | 0 0 0 1 | false | **true** | 120 | 2026-09-15T07:18:45Z |
| base（λ=0） | `sweep_redo_base_2026-09-13` | `runs/redo_base_2026-09-13/base` | 既定 `73f17e28…` | 0.0 | 0 0 0 1 | false | false | 120 | ★ 記録なし |
| charge（λ=0） | `sweep_redo_charge_2026-09-13` | `runs/redo_charge_2026-09-13` | 既定 `73f17e28…` | 0.0 | 0 0 0 1 | **true** | false | 120 | ★ 記録なし |
| α=1（λ=0） | `sweep_alpha1_2026-09-13` | `runs/alpha1_2026-09-13/alpha1` | 既定 `73f17e28…` | 0.0 | **1** 0 0 1 | false | false | 120 | ★ 記録なし |
| β=1（λ=0） | `sweep_beta1_2026-09-13` | `runs/beta1_2026-09-13` | 既定 `73f17e28…` | 0.0 | 0 **1** 0 1 | false | false | 120 | ★ 記録なし |
| w=2（λ=0） | `sweep_w2_2026-09-14` | `runs/w2_2026-09-14` | 既定 `73f17e28…` | 0.0 | 0 0 **2** 1 | false | false | 120 | 2026-09-14T08:20:26Z |
| w=4（λ=0） | `sweep_w4_2026-09-14` | `runs/w4_2026-09-14` | 既定 `73f17e28…` | 0.0 | 0 0 **4** 1 | false | false | 120 | 2026-09-14T12:11:52Z |
| v2a（λ=0） | `sweep_seed_v2a_2026-09-13` | `runs/seed_v2a_2026-09-13/v2a` | `c9c61796…` | 0.0 | 0 0 0 1 | false | false | 120 | ★ 記録なし |
| flat2（λ=0） | `sweep_seed_flat2_2026-09-13` | `runs/seed_flat2_2026-09-13/flat2` | `dc60c3a9…` | 0.0 | 0 0 0 1 | false | false | 120 | ★ 記録なし |
| v3a2（λ=0） | — | `runs/cliff_v3a2_2026-09-13/th2.*` | `5f3e5601…` | 0.0 | 0 0 0 1 | false | false | 21（T=600） | ★ 記録なし |

★ 「λ」が 0.0 の腕は 台帳ヘッダに `arm_local_lambda` の欄が無い（`abm/ledger.py` が既定値の
  欄を書かないため）。**欄が無い＝λ=0** と読むこと。

---

# 付録C  起動前に必ず確認すること

  1  この文書に 対応する項の本文と固定日時が入っているか。★ 入っていなければ起動しない。
  2  ★ 対応する config が存在するか。★ 区画1〜7・N1・N2 は 2026-09-15 時点ですべて作成済み。
  3  `config` の `seed_file` が意図した種を指しているか。
     ★ `sweep_config.json` と画面の「種 …」は **常に `U-011_seed_v1.json` の sha**
       （`sweep.py:311` がファイル名を決め打ちしている）。信じない。
  4  走行後、段3-2 で **台帳ヘッダの `seed_file_sha256`** が期待値と一致することを確認する。
     `73f17e28…` が出たら 既定の種に落ちている。止めて報告する。
  5  `output.dir` が他の腕と重複していないか。★ 一つの `cells/` に二つの腕を入れない。
     ★★ 非一様 f（N1・N2）は `f_setting` も `cell` 名も一様 f=0.5 と同一になるため、
       出力を混ぜたら分離できない。★ 走行根ごとに解析する（C(i)）。
  6  ★★ 09-13 の走行（base・charge・alpha1・beta1・mainfig・seed_v2a・seed_flat2・
     cliff_v3a2）は `.done` を持たない。台帳は 1740 試行で完走していることを確認済みだが、
     **sweep.py で同じ出力先を指して再走させると、既存の台帳を消してから走り直す**
     （`.done` が無い＝未完了と見なすため）。しかも当時の config は `seed_file` を
     持たないので、再走すると種が v2c になる。
     ★ 再走させない。必要なら 別の `output.dir` に出す。
