# 探索の依頼 — 誤り訂正はどこまで届かないのか

宛先 Codex ／ 発行 2026-09-05
対象 `GODfumitaka/sfn-compression-abm`（HEAD `05c36bdc605b13a44a1fe98f325e54e7987af892`）

---

## 0. この依頼の性格

**仕様書ではありません。問いを渡します。**

★ **成果物はコードではなく、測定と、そこから見えたことです。**
  探索の過程で書いたコードは残してもらって構いませんが、**本体は測定です。**

★ **自由に進めてください。**下記の提案は叩き台であり、より良い筋があればそちらを採ってください。
  **「その筋は成り立たない」という報告も、同じ価値があります。**

★ 途中で仮説が変わることを歓迎します。**変わった経緯を残してください。**

---

## 1. 中心の問い

> **誤り訂正は、どこまで届かないのか。**

誤り訂正とは、世界からのフィードバックによって、エージェントが自分の世界像を
世界に合わせていく過程を指します。

★ 誤りを明示的に表象する必要はありません（Hopfield 型の引き戻しがそうであるように）。
  **問いは「訂正が届かない範囲がどこに、なぜ生じるか」です。**

この模型は、その限界を見るために作られています。

---

## 2. 訂正が届かない筋、三つ（★ 三つ目が本命かもしれない）

### 2-1　信号が来ない

`f`（フィードバック確率）が低ければ、誤りは返ってきません。**既に掃引の軸です。**

### 2-2　誤りが誤りとして登録されない

```
analyze_rev6.py:567  h_subjective = hits / (hits + misses)
```

★ **棄権した試行は分母に入りません。**黙ったことが「外した」として帳簿に載らない。
  **エージェントの側から見ると、精度は下がらない。**

★ 一方、研究者側の被覆率は棄権を分母に入れます（`:570`）。**同じ会計の中で扱いが違う。**

### 2-3　★ 登録されても、様相を直せない

「様相」とは、「AならB」が**必ず**なのか**たいてい**なのか、という断言の種別です。

```
入力側  ceil(tau_acc × m_live) で 部分照合を許す   ★ 段階的
出力側  EdgePrediction 一本。★「たぶん」が付かない
採点    的中 / 失敗 / 保留。★ 中間が無い
訂正    構成素を削るか、残すか。★「9割方成り立つ」に弱められない
```

★ **世界が 90% の規則を持っていても、def はそれを 100% として断言します。**
  そして訂正の手段は削除しかない。**削れば 0%、残せば 100%。**

★ **これが本命かもしれません。**ただし確信はありません。**否定してくれても構いません。**

---

## 3. 我々が持っている道具（★ 未使用のものを含む）

模型は段階的な量を**二つ内部に持っています。**

```
P（参加率）        accounting.py の participation()。過去どれだけ合ってきたか
support / m_live   agent_runtime.py:105 の support_at_adoption。今回どれだけ合ったか
```

★ **どちらも出力に届きません。**

```
P       → V = P × a → θ′ と比較 → 削除するかどうかだけ
support → _need と比較 → 喋るか黙るかだけ
```

★ **材料は内部にあり、断言のときに捨てられます。**
  ここが本研究の中心的な主張（エージェントは内部量から自分の状態を区別できない）と
  つながる可能性があります。

---

## 4. 実測で分かっていること（★ 前提。誤りがあれば指摘してください）

すべて 2026-09-03〜05 に、コードの引用と走行の再生成で確認したものです。

### 4-1　中心的な過程が観測されていない

```
一度厚くなった def が 薄い状態で終わった例   ★ 0 本 / 73
薄い def の 58.6% は 出生時から薄い
```

### 4-2　沈黙の内訳

```
全 1,740 試行
  433  τ を誰も通らない                  24.9%
1,139  ★ def は使われたが 予測を作れなかった  65.5%
  168  予測を出した                       9.7%
```

★ **本体は 1,139 です。**

```
sme.py:364-365  引数に関係を持つ関係は 投影候補から除外される
sme.py:222      docstring「未観測一階 relation を投影する」
高階の生存率     require 1.000 / cause 0.999 / allow 0.995
```

★ **薄い def は高階ばかりが残る。高階は投影できない。だから何も言えない。**
  実測で、`m_alive = 2` の def は 73 本中一本も予測を出していません。

### 4-3　誤りの内訳

```
失敗 50 件のうち  述語が違う 50 ／ ★ 述語は同じで引数が違う 0
```

★ **述語が合えば引数も合っています。**実体の束縛は緩くない。

### 4-4　★ 一本だけ、神話らしい def がある

```
R_c075204e8c6e5efa   m_alive = 3 ／ 予測 23 回 ／ 的中 0 ／ 生存
外している相手  near / above / behind / under など（★ 毎試行ランダムな接着関係）
```

★ **f = 0 なので、この誤りはエージェントに返っていません。**

### 4-5　凍結価格が現在の構造と乖離する

```
c_reg − c_now  常に 0 以上、最大 2.4。29% の def で 0 でない
```

★ 構成素が死んで束縛が外れても、**価格は厚かった頃の圧縮利得を保持し続けます。**

### 4-6　種が浅い

```
m_alloc_true  最大 5 ／ 中央 4
塔の深さ      1（world.py:110, 112）
repair_scope  配線されているが、深さ 1 のため 1 歩で止まり縮退（loop.py:355-369）
```

---

## 5. 種の拡張案（★ 叩き台。より良い案を求めます）

一階 8 語を 2 語ずつ組にして二階をかぶせ、部分木を 4 つ作る。
各モチーフは隣り合う 2 つを持ち、その上に**モチーフ固有の三階**を置く。

```
hA = cause(hold, push)    hB = require(carry, lift)
hC = enable(break, cut)   hD = avert(turn, press)

M1 =  allow(hA, hB)   M2 = depend(hB, hC)
M3 = prevent(hC, hD)  M4 =  block(hD, hA)
```

★ **`map_graphs` に手で組んだ場面を通して確認済み。**

```
M1 対 M1  →  7 本が対応          厚い def
M1 対 M2  →  require の部分木 3 本  ★ 小さな R
M1 対 M3  →  0 本
三階を共通に戻すと  require ↦ enable という誤対応が起きる
```

★ **これは「厚い def が生まれ、薄くなりうる」ための構造案です。**
  **中心の問い（訂正の限界）に本当に必要かどうかは、検討してください。**

★ 既知の制約（★ 再発見に時間を使わないでください）

```
abstraction.py:163  生存中の述語と同じ述語は追加されない → 述語の「種類」を増やす必要がある
abstraction.py:185  new_slots は def ごとに変わる → 種に書ける量ではない
sme.py:290          写像には述語一致が要る
sme.py:305-311      高階の引数は述語一致なしに対応する（体系性であり、過汎化の経路でもある）
```

---

## 6. お願いしたいこと

```
1  中心の問い（§1）に対して、この模型で何が測れるかを 探索してください
2  §2 の三つの筋のうち、どれが測定可能で、どれが実装を要するかを 判定してください
3  必要なら 最小の変更を加えて 実測してください
4  ★ 「その筋は成り立たない」も 同じ価値のある報告です
```

★ **完成したコードより、測定と、そこから何が言えるかを求めます。**

★ **走行は自由に回してください。**費用の見積りには `--calibrate` が使えます
  （`sweep.py:304-334`。短い走行から本走行へ外挿する機構）。

---

## 7. 壊さないでほしいもの（★ これ以外に制限はありません）

```
1  runs/ 以下の既存走行（5,762 本）を変更・削除しない
2  analyze_rev6.py を書き換えない（★ 複製してください）
     → ビット再現の基盤であり、別 OS・別 Python で再確認されたばかりです
3  U-011_seed_v1.json を書き換えない（★ 新ファイルを作ってください）
     → sweep.py:254 が sha256 を走行ヘッダに記録するため、種の同一性が追跡できます
4  main に直接コミットしない
```

★ ブランチを分けても、案を複数試しても、走らせながら設計を変えても構いません。

---

## 8. 返してほしいもの

```
1  何を測ったか（★ 生の数値・実行したコマンド・台帳の sha256）
2  ★ 走らせる前に何を予想したかと、実際との対照
3  何が分かったか。何が分からなかったか
4  §4 の実測に誤りがあれば、行番号つきの引用で指摘
```

★ **結論の要約より、生の数値と引用を返してください。**

★ 我々は 2026-09-03 に、解析出力の欄名を内容の保証として使ったことで 17 件の撤回を出しました。
  **`per_def` と `per_run` は二次記録です。**「模型が何をするか」の一次記録は `abm/` 以下の実装です。

---

## 9. 補足 — 背景にある理論的関心

この模型は、より大きな問いの一部です。

> 通常の誤り訂正では正解集合が固定されているが、生命では「正常」そのものが履歴と環境で変わる。
> 状態と、正常を定義する地形の共進化をどう扱うか。

★ 本模型はその最小版として、**「訂正が届かない範囲がどこに生じるか」**を見ています。

★ 認知科学の対応物として Koriat & Goldsmith (1996) の量−正確性トレードオフを特定しています
  （報告量を増やすと正確性が下がる）。**本模型では、それが戦略ではなく会計と削除から出てきます。**
