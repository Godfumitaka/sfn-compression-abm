# 台帳追記7 — 2026-09-03（最終版 rev4）

作成 2026-09-03 ／ 起草 Claude ／ **承認待ち**

★ rev3 からの変更：主図の照合結果を C-16 として追加。U-116 を解決へ。

---

## §0 このセッションで起きたこと（一行）

**per_def の `m_alloc` 欄が「定義の厚さ」ではなく「構成素行の累積数」であったため、
同欄に依拠した結論がすべて誤っていた。**走行の再生成により訂正し、
種 v2a の構造設計と写像器での検証、主図の式の確認まで到達した。

---

## §1 撤回（17 件・すべて Claude の発言）

| # | 撤回内容 | 型 |
|---|---|---|
| 1 | **「T-111（m_alloc の上限は 5）は反証された」** | ★ 欄名を内容の保証として使用 |
| 2 | 「撤回86 として T-111 を落とすべき」 | 1 に依存 |
| 3 | 「判断2（種を大きくする）はやらないで決まった」 | 1 に依存 |
| 4 | 「67 は一対一制約が効いていない証拠（0.7）」 | 推測を根拠に機構を断定 |
| 5 | 「厚い def は薄くならない」 | 厚い def を一度も観測していなかった |
| 6 | 「m_alloc ≥ 7 が 28.6% あるので 7→2 は構造的に可能」 | 1 に依存 |
| 7 | 「種の問題は測定の分解能の問題」 | 癖4 の逆 |
| 8 | 「種を大きくすると神話率が下がる（0.6）」 | 打ち切られた統計を率として読んだ |
| 9 | 「abstain は vt 交絡のため主張に使えない」 | 過剰な保留 |
| 10 | 委任書の指示「照合が外れたら停止せよ」 | 主目的と診断を取り違えた |
| 11 | 「`repair_scope` は未配線ではないか（0.7）」 | ★ 実測差ゼロから未配線を推測した |
| 12 | 「θ′ 格子を u の隙間に置くという理解は数値と合わない」 | u の上端と比べた |
| 13 | 「塔2 は 3 モチーフに置くべき」 | 判断材料が無い |
| 14 | 「到着率は研究者側の量でエージェントは計算できない」 | 誤り。分解ができないだけ |
| 15 | 「`freeze_price` の第三引数が `saving` なので `c` は V に届かない可能性」 | 第三は `hold_cost` |
| 16 | **「θ′ 格子は種の u から導出できる」という手順全体** | `new_slots` が設計できない |
| 17 | **「判断1 は種の変更を待たず今すぐ決められる」** | v2 で母集団が変わる |

★ **最頻出型は 1・5・6 の「per_def の欄名を内容の保証として使う」。**

---

## §2 確定候補（16 件）

### C-1　per_def の `m_alloc` は厚さではない

```
analyze_rev6.py:497    "m_alloc": len(constituents)      ← 累積行数
abstraction.py:138     "m_alloc": definition.m_alloc     ← 真の厚さ。解析側が読んでいない
abstraction.py:179     old.m_alloc のまま更新されない
abstraction.py:167,175 _extend_definition は rows.append。墓石は消えない
abstraction.py:163     追加対象は「生存構成素に無い述語」→ 死んだ述語は再追加されうる
```

### C-2　T-111 は支持される

`m_alloc_true`（def 73 本）：`2:6 / 3:14 / 4:37 / 5:16`（中央 4・最大 5）

### C-3　★ 中心的な過程は観測されない（本研究で初の直接観測）

```
m_live_max > m_live_birth かつ m_live_final <= 2   ★ 0 本 / 73
m_live_birth が m_alloc_true と一致               ★ 73 / 73
降下の実例  試行 18 で登録（4本）→ 試行 22 で 2本  ★ 4 試行で崩落
```

### C-4　m_live の天井は「写像に現れる異なり述語の数」（`abstraction.py:163`）

実測（135,328 本）で異なり述語数の最大 11、`m_live_final` の最大 8。

### C-5　T-105 が環境をまたいで確認された

macOS+Python3.14 と Linux+Python3.12 で `R_05975fac0d064dbb` が一字も違わず一致。

### C-6　`repair_scope` は配線されている。縮退の原因は塔の深さ

```
loop.py:359   depth = 1 if FIRST_ORDER else 2 if SECOND_ORDER else None
base_ids={core1} → 1歩目 {higher,tower} → 2歩目 空
```

### C-7　`one_minus_h` は検算専用（`world.py:59-64`）

### C-8　高階は投影候補に入らない（`sme.py:364-365` / `243-244`）

### C-9　V の式（`accounting.py:221-223`）

```
delta_l = price.saving − (charged/denominator) ／ a = delta_l/ell_frozen
V = participation × a + w×(embed/ℓ)
```

### C-10　θ′ 格子の設計意図（SPEC_B1 1357-1361）

```
0.0410 → 0 本 ／ 0.1432 → 4 本 ／ 0.2637 → 6 本 ／ 0.3842 → 8 本
```

★ **一段ごとに生き残り方が質的に変わるよう置かれている。**順序が合えば成立する。

### C-11　★ 共通の三階は誤対応を生む（写像器で実測）

```
三階が異なる  → require ↦ require。部分木 3 本が共有される
三階が共通    → t ↦ t2 が先に成立 → ★ require ↦ enable という誤対応
```

### C-12　★ 高階の引数は述語一致なしに引きずられる（実測）

`map_graphs(cause(hold,push), cause(carry,lift))` → `hold ↦ carry` ／ `push ↦ lift`

★ 体系性であり、**同時に過汎化の経路。台帳に記述が無い。**

### C-13　`c` は保持価値に届いている

`definition.py:17-24`：`saving = ell_frozen + c`。第三引数は `hold_cost`。

### C-14　★ `new_slots` は種で設計できない

```
_new_slot_count  ＝ その関係にしか現れない引数の個数
実測（構成素 463 件）  種の宣言との一致率 27.22%
require / allow / cause  宣言 0 に対し 実測 0 が 一件も無い
break  実測 0:16 / 1:8 / 2:12   ★ 同じ述語でばらつく
```

★ **`c`・`a`・`u`・θ′ 格子を種から導く手順は成立しない。**
★ ただし `u` は実行時に読まれないため、**既存の走行結果は有効。**

### C-15　`--calibrate` は費用の較正（`sweep.py:304-334`）

```
scale = (1740/trial_count)**1.63 ／ size_scale = **1.85
```

★ **v2 の走行費用を推測でなく実測で外挿できる。**

### C-16　★ 主図は量−正確性トレードオフを正しく測っている

```
analyze_rev6.py:536   spoke = predicted_edge が None でない試行
analyze_rev6.py:570   coverage     = len(spoke) / len(trials)      ★ 棄権は分母に入る
analyze_rev6.py:571   accuracy_def = hit(spoke) / len(spoke)       ★ 棄権は分母から落ちる
analyze_rev6.py:567   h_subjective = hits / (hits + misses)        ★ 棄権は分母から落ちる
accounting.py:267-268 棄権は OracleScore("保留", False, cost)
```

★ **完全ケース法ではない。**Koriat & Goldsmith の quantity–accuracy の定義と同型である。

★ **「実際の的中数 −18%」が分母操作の疑いを消す。**精度が上がって絶対数が減るのは、
  分母の取り方では説明できない。

★ **`h_subjective` の分母も棄権を落とす。**エージェントの台帳では黙った試行が
  「外した」に数えられない。**三層乖離の機構的な理由がここにある。**

★ **未照合として残るもの**

```
「実際の的中数」に相当する per_run の欄が特定できていない（hits か否か）
三数値（+8% / −24% / −18%）がどの水準間の比か不明
手元の per_run は f=0 の 1 水準のみ。★ 数値の再現は Mac が要る
```

---

## §3 設計の根拠（★ 主張としては用いない）

### 回転（rotation）

```
n_increases > 0  57/73（78.1%）／ m_live_max > birth  16/73（21.9%）
R_05975fac0d064dbb  3⇄4 を 61 回。試行 1728 まで振動が続き収束しない
```

★ 用途は三つ。**測定の掃除／降下の保存（ρ=0）／ρ 軸の根拠。**

---

## §4 決定（2026-09-03）

| # | 内容 |
|---|---|
| D-1 | **判断2（種を大きくする）は実施する。** |
| D-2 | **ρ は掃引軸とする。ρ ∈ {0,1}、主は 0。** |
| D-3 | **段階分割する。**段階1＝塔2（射程軸）／段階2＝＋核一階（降下）／段階3＝塔の伏せ候補化 |
| D-4 | **三階はモチーフごとに別述語とする**（C-11） |
| D-5 | **判断1（一次指標）は段階2 まで保留する。** |
| D-6 | **θ′ 格子は種から導かず、短い走行で境目を測って置く**（C-14） |
| D-7 | ★ **ポスターの重心を主図（f 軸）に置く。**神話率は段階2 の結果次第で加える |

### D-5 の付帯 — ★ 走行前に登録する選び方の規則（規範13）

```
m_live_max ≥ 6 かつ final ≤ 2 が相当数 → 劣化が観測された。数え上げ系の指標を採る
それが 0 のまま                        → 静的属性は諦め、過程の量（変換率）へ
どちらとも言えない                     → 神話率を一次指標に据えず、主図で出す
```

★ **指標そのものは決めない。選び方だけ先に決める。**

---

## §5 未決

| # | 内容 | 状態 |
|---|---|---|
| U-111 | ~~`new_slots` の設計値と実行時の一致~~ | ★ 解決（C-14）。設計できない |
| U-112 | ~~部分木の実体を共有するか分けるか~~ | ★ U-111 により設計対象から外れた |
| U-113 | 主張の述べ方（「薄化する」を何に書き換えるか） | 段階2 の結果待ち |
| U-114 | 段階3（塔の伏せ候補化）を行うか | 費用 1.5〜3 日 |
| U-115 | **θ′ 格子を置く具体的手順**（T・seed 数・境目の判定） | D-6 による新規 |
| U-116 | ~~主図が一次記録と未照合~~ | ★ **式は確認（C-16）。数値の再現は Mac 待ち** |
| U-117 | ★ **C-12（高階の引数が述語一致なしに引きずられる）は仕様か副作用か** | 台帳に記述が無い |

---

## §6 生成物

```
seed_gen.py            種の生成器。★ v1 の全数値を再現（構成素24件×6量＋marginal54件）
gen_v2a.py             v2a の生成器
U-011_seed_v2a.json    ★ 構造は有効。数値は C-14 により作り直しが要る
U-011_seed_v2_仕様骨格_rev2  段階分割・判定基準・退化条件
委任書 9 通            測定・コード読み・実装前確認
```

---

## §7 本日の走行

```
config           config/repro_seed36.json
code_commit      05c36bdc605b13a44a1fe98f325e54e7987af892
world_hash       73b40ae925351b54187705f8d394e2121969b94180c202517b772b97b1d4e302
elapsed_sec      328.33（1 コア・Python 3.12.3）／ final_def_count 73
```

★ 背景走行は `setsid` と `< /dev/null` が必要。`nohup ... &` のみでは 20 秒後に回収された。

---

## §8 規範への追記提案（4 件）

```
1b-2  ★ per_def・analysis 出力は 二次記録である
      「模型が何をするか」の一次記録は abm/ 以下の実装である
      ★ 欄名を内容の保証として使わない。式まで遡って確認する

10e   ★ 部分集合の統計を母集団の率として読まない（撤回8）

10f   ★ 実測で差が出ないことから 未配線を推測しない（撤回11）
      → 差が出ない理由には「軸が無い」と「軸が噛む構造が無い」がある

19    ★ 設計文書が宣言する値が、実行時に再計算される量かどうかを先に確かめる
      経緯：種の new_slots は設計値だが、実行時は def ごとに再計算される（C-14）
      → 「種に書いてある」ことは「模型がそう振る舞う」ことを意味しない
```
