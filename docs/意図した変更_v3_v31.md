# 意図した変更（v2・v3・v3.1）— 仕様の文面が未更新のもの　2026-09-26

コードを仕様（主に `SPEC_B1_impl_2026-08-28.md`、以下 **B1**）と比べるときの手引き。

- ここに載せた変更は、決定・決裁があって意図して入れたもの。**仕様の文面は更新していない**ので、そのまま比べると食い違いとして出る。
- 「決定」の欄には、番号があれば番号を書いた。番号が無いものは「番号なし」と書き、出所（委任書・決定の日付・コミット）を添えた。番号は作っていない。
- 関連する文書（このディレクトリ）
  - `仕様_走らせた模型_2026-09-25.md`（as-run）：v2（b2 の腕）の模型を、項目ごとに「今のコードの動き・仕様の文面・判定・決定」で書いたもの。
  - `仕様突き合わせ_2026-09-24.md`（突き合わせ）：仕様とコードの食い違いの表。下の「突き合わせ 1-3」などは、この表の番号。
  - 二つとも、コミット `bf7dd90` の abm/・sweep.py を読んで書いた。このブランチの abm/・sweep.py は `bf7dd90` と同じなので、file:line はそのまま当たる。
- v3・v3.1 の旗は、外側のドライバ `tools/v3_run.py`（と `tools/v31.py`・`tools/lowmem.py`）で入れる。**abm/ は変えていない**。旗の既定は v2 の動き。
- 腕の旗
  - v3 ＝ `--nohash --vt 0.3842 --greedy`
  - v3.1a ＝ v3 ＋ `--extend-rule profit --charge1 d32`
  - v3.1b ＝ v3 ＋ `--extend-rule none --charge1 d32`
  - `n80` の腕は、これに `--nsim 0.8` を足したもの。

## 1 v2 の設定（config の fixed。旗ではなく設定値）
| 設定 | 何を変えたか | 決定 |
|---|---|---|
| `identification_graph = "live"` | 同定（NSIM）に使う定義のグラフを、生きている行だけにする（abstraction.py:306-331）。仕様は墓石込み（B1:1035・:515） | 番号なし。コミット `4392c73`（2026-09-10）。アストラさんの意図と確認済み（as-run 2-B、突き合わせ 1-3） |
| `refill_rule = "one_per_slot"` | 同化で足す行は、生きている行のいない位置（墓石の席）に一本だけ。m_alloc は伸ばさない（abstraction.py:204-217） | **案イ′**（2026-09-22、コミット `bf7dd90`「帳簿の件を直す（案イ′）」）（突き合わせ 2-7・2-10） |
| 墓石からの穴埋め（設定の欄は無い） | 充填で墓石の行も埋める（filling.py:168-257）。仕様の文面は飛ばす（B1:564-565） | **仮U-3 を閉じる**（アストラさんの決定と確認済み、as-run 7-B）。コミット `0580058`（2026-09-12「充填で墓石を飛ばす（2-A）を取り消す」）（突き合わせ 5-1） |
| `local_lambda = 1.0` | 充填の候補の重み ＝（その位置での相対頻度）^λ × p̂（filling.py:40-61）。仕様は p̂ 最頻（B1:588） | **仮U-41・U-085 v1**（コードの注記 filling.py:44・:68。決裁の記録そのものは確かめていない）（突き合わせ 5-5） |
| `holdout_include_second_order = true` | 伏せ辺の候補に二階の関係も入れる（world.py:236-238） | **仮U-42・仮D-59**（コードの注記 domains.py:182、world.py:50・:74。決裁の記録そのものは確かめていない）（突き合わせ 7 の表） |
| `theta_prime = 2.1／2.3`（セル） | 行の削除の閾値。仕様の 4 点（0.0410〜0.3842、B1:1129）と尺度が違う | 番号なし。「行の崖を作るため」（アストラさんの記憶による、as-run 5-B）。決裁の記録は見つからなかった（突き合わせ 3-3） |

★ ほかの設定値（nsim_threshold 0.95・self_score_cache off・pricing_rule spec・tau_acc 0.67・α/β 0・w 0・κ 1・λ_mix 0.1・repair_scope first_order・abstain_charge false）は、突き合わせ 7 の表で「一致」。

## 2 v3 の旗（tools/v3_run.py）
| 旗 | 何を変えたか | 決定 |
|---|---|---|
| `--nohash` | 名前の一致の経路を使わない。0.95（または --nsim）に届かず、m1 のハッシュ名が既にある定義と同じなら、既存に入れず `<名>_t<試行>` の新しい定義として登録する。B1:1045「届かなければ新規登録」に沿わせる | 番号なし。委任書 2026-09-24「今夜の対照：名前の一致を使わない版」で作り、委任書 2026-09-25「v3（仕様に沿わせた模型）の組み立て」で v3 に入れた（突き合わせ 1-2） |
| `--vt 0.3842` | 逐語の枚の閾値 verbatim_theta を θ′ から切り離して 0.3842 にする。v2 は θ′（2.1／2.3）に落ちて、枚が 1 試行しか残らない。0.3842 なら約 7 枚残る | 番号なし。委任書 2026-09-25（突き合わせ 2-2：B1:931 と :396-399・:936-943 の矛盾。0.3842 は B1:1129 の天井の値） |
| `--greedy` | 生まれ方：共通構造（土台と今の場面の構造の対で述語一致）の全体から、外すと Σ(V−θ′) が一番増える行を一本ずつ外す。V は組の中で計算した生まれた直後の 1＋c/ℓ。2 本未満なら作らない。B1:1088-1090（G5）に沿わせる | 番号なし。アストラさんの決定 2026-09-25 12:5x（突き合わせ 2-3） |
| `--greedy`（続き） | 構造の絞り（高階の関係とその引数だけを対にする）は残す | 同じ決定（2026-09-25 12:5x）で「構造の絞りは残す」（突き合わせ 2-4） |
| `--greedy`（続き） | 最初から子の欠けた高階の行（連鎖を含む）を、はじめに外す | **Q5**（アストラさん、2026-09-25 14:5x） |
| `--greedy`（続き） | 同点：まとめて外して増えるなら外す、増えないならつながりの弱い方、それでも決まらなければ外さずに記録（tie_unresolved） | 同じ決定（2026-09-25 12:5x）。「決まらなければ止めて記録」は実装の読み |
| `--nsim X` | 同定の基準 nsim_threshold を X にする（`n80` の腕は 0.8） | 番号なし（腕の値）。仕様の既定は 0.95（B1:1070）、較正は U-091 で未決（B1:1076） |
| `--extgreedy` | 比べ用：取り込みで儲けが増える限り一本ずつ足す。本番の腕では使っていない | 番号なし（2026-09-25 12:5x の決定で「比べに取り込みの一本ずつ足す版」） |

## 3 v3.1 の旗（tools/v31.py。v3 の旗と一緒に使う）
| 旗 | 何を変えたか | 決定 |
|---|---|---|
| `--extend-rule profit`（v3.1a） | 同化で行を足すかを「足すと Σ(V−θ′) が増えるか」で一本ずつ決め、増えなくなったら止める。死んだ述語（墓石の述語）も候補。席は、合う墓石の席があればそこ、無ければ新しい席（m_alloc を一つ増やす、上限なし） | **D-31**（2026-09-25 決裁）。候補・席の照合・同点・β＝0 のみの読みは、tools/v31.py の先頭の注と tools/v31_確かめ_2026-09-25.md |
| `--extend-rule none`（v3.1b） | 同化で行を足さない。slot_history の記録・同化の回数など、ほかの同化の処理は今のまま | **D-31** |
| `--charge1 d32` | ① の罰：投影で外したら、予測した行（と repair_scope の親）だけに付ける。穴埋めで外したら行に付けず、その位置の slot_history で予測した述語の回数を一つ減らす（0 未満にしない。履歴に無い述語なら変えない）。① が立つ条件・② は今のまま | **D-32**（2026-09-25 決裁）。「履歴に無い述語なら変えない」はタグ v3.1-verified（556ef79）の前に直した |

## 4 振る舞いを変えない旗（台帳は同じ、または記録だけ）
| 旗 | 何をするか | 決定 |
|---|---|---|
| `--lowmem`（既定。外すときは `--no-lowmem`） | 状態の正準形の控え（loop.py:31-33）を、前の試行で触った物とその子孫に絞る（tools/lowmem.py）。台帳は一字一句同じ（7 本で確かめた） | 番号なし。アストラさんの決定 2026-09-25（as-run 10） |
| `--dump-slot-history` | 走行末の全定義の slot_history（墓石の席も含む）と行を、side の最後の行に書く。台帳には何も書かない | 番号なし（2026-09-26、タグ v3.1-slotdump） |
| `--compare-to`・`--no-compare`・`--trial-count`・`--seeds`・`--cells`・`--workers` | 走らせ方と確かめだけ（`--trial-count` は試しの短い走行用） | — |

## 5 参考：決定の記録を確かめていない食い違い（意図かどうか分からない）
突き合わせで「仕様に無い動き」「食い違い」とした項目のうち、上の表に入れなかったもの。調べで出てきたら、突き合わせの番号の行を見てほしい。
- 2-6 同化で、生きている行に同じ述語がある関係は足さない（abstraction.py:202-203）。v3.1a もこれを保つ（tools/v31.py の読み (1)。アストラさんの決定ではない）。
- 2-8 足す順番とあふれ（v2 の one_per_slot）。
- 1-5 発話の定義選びと投影のグラフが墓石を除く（agent_runtime.py:246-260、コミット `c265fe1`）。
- 1-6 m1 を threshold を見ずに呼ぶ（loop.py:97）。
- 3-11 m_live＝0 で定義を丸ごと消す（deletion.py:77-94、計装1、コミット `dfe0f1d`「計装1・2・4・5 を確定する」）。
- 3-14 p̂ の語彙から述語が抜けない。
- 4-5 予測は一本だけ。
- 5-7 充填の候補を構成素と同じ階数に絞る（filling.py:212-222）。
- 3-5 embed の fan-in は未実装（w＝0 なので効かない）。
- 2-15 記憶の鍵（merit・embed は三つ組、exceptions・slot_history は二つ組）。
- 仕様の中で矛盾（2-2・2-13・3-1・3-3・3-7・1-7）は、突き合わせの表を見てほしい。

★ 二つ目の実験の旗（`--ident-rho`・`--ident-argmax`・`--ident-commons`・`--ident-shadow`）は別のブランチ（v3.2-2026-09-26）にあり、このブランチには無い。
