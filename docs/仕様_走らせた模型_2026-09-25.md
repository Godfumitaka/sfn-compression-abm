# 走らせた模型の仕様（as-run）下書き　b2 の腕　2026-09-25

★ 下書き。読んで書いただけで、コード・設定・走行は変えていない。
★ 対象：b2 の腕（`config/sweep_b2_*_2026-09-22.json` の 8 本。`fixed` はすべて同じで、違うのは f と種だけ）で走ったコード `bf7dd90`。
　設定 8 本は 2026-09-25 にコミット `3140eb8` で git に入れた（abm/・sweep.py は `bf7dd90` と同じ）。
★ 各項目に四つの欄を置く。
- 「今のコードの動き」：事実だけを平易な言葉で書き、ファイル:行を添える。
- 「仕様の文面」：ファイル:行。
- 「判定」：`analysis_astra/仕様突き合わせ_2026-09-24.md` の表の番号。
- 「決定」：空けておく。記入したのは、指示のあった三つだけ（同定のグラフ・墓石からの穴埋め・θ′）。
★ 仕様の略号：**B1** ＝ `SPEC_B1_impl_2026-08-28.md`、**B0B2** ＝ `SPEC_B0B2_impl_2026-08-24.md`、**v3** ＝ `SPEC_v3_0625.md`。
★ 数は、断りがなければ 1 台帳（`runs/b2_hide_s1_2026-09-22`・f=0.5・seed001・セル `f0.5000_th2.1000_first_order`）の値。一般化しない。

---

## 0 一試行の順番（全体像）

「今のコードの動き」（loop.py:76-155）
1. **予測**（agent_runtime.py:66-183）。状態は変えない。
2. **採点**（accounting.py:262-275）：伏せ辺一本と、予測一本の中身（述語と引数）が一致するか。
3. **開示の判定**（loop.py:88-89）：coin_t < f なら伏せ辺を返す。
4. **書き込み**（agent_runtime.py:262-293）：いまの場面の見えている部分を逐語の枚として足し、p̂ を更新する。開示があれば、その辺も同じ枚と p̂ に足す。
5. **会計**（loop.py:182-355）：功績・例外費用の減衰と加算、②・① の課金。
6. **同定と m1**（loop.py:96-118）：直前の枚を土台に、定義を生む・伸ばす。
7. **削除**（deletion.py:12-117）：行を V < θ′ で消す。m_live＝0 の定義を消す。枚を σ < θ′ で消す。

「仕様の文面」：B1:985「18 手順」、:1001「U6 を先に、U7 を後に」、:1297「predict → oracle → update の一方向」
「判定」：一致（6 と 7 の順）
「決定」：

---

## 1 逐語の記憶（枚）と場面の並び順

### 1-A 枚を書く
- **今のコードの動き**
  - 毎試行、いまの場面の見えている部分（伏せ辺を除いたグラフ）を一枚として足す（agent_runtime.py:266-277・:394-401）。
  - 枚の番号は「いまある枚の番号の最大＋1」（agent_runtime.py:270）。枚が毎試行一枚ずつ残る限り、試行番号と同じになる。
  - 開示があれば、その辺を同じ番号の枚に足す（agent_runtime.py:280-290）。
- **仕様の文面**：B1:925-927・:987-990
- **判定**：一致（6-3）
- **決定**：

### 1-B 枚が消える
- **今のコードの動き**
  - 試行の終わりに、σ ＝（いま − 枚の番号）^(−0.5) が θ′ を下回った枚を消す（deletion.py:96-108）。
  - 枚の閾値は、verbatim_theta が無ければ θ′ を使う（domains.py:215-217）。b2 では指定が無い。
  - 1 試行たった枚の σ は 1 で、θ′ 2.1／2.3 を下回る。**そのため、次の試行に残るのは直前の試行の枚一枚だけになる。**
  - 1 台帳の実測：1 試行目のあとは毎試行ちょうど 1 枚消え、登録 1,163 件すべてで「登録の試行 − 土台の枚の番号」＝ 1 だった。
- **仕様の文面**
  - B1:930「落ちる判定は σ(t) < θ′」、:931「theta_prime を verbatim（枚）の閾値としても使う」。
  - 一方で B1:396-399・:936-943 は枚の寿命を τ ＝ 1/θ′² ＝ 7〜595 試行と書き、:934 の A5 は θ′ ≤ 1/√S を求める。
- **判定**：2-2（仕様の中で矛盾）
- **決定**：

### 1-C 場面の並び順（事実の確認。2026-09-25 に計算）
- **今のコードの動き**
  - 試行を 4 つずつの組に分け、組ごとに M1〜M4 の並びを乱数で入れ替える（world.py:306-315）。
  - その結果、同じ組の中では同じモチーフが続かない。同じモチーフが続くのは、組の切れ目で、前の組の最後と次の組の最初が一致したときだけ。
  - 1 台帳（seed 1）の最初の 12 試行：M3 M2 M4 M1 ｜ M2 M1 M4 M3 ｜ M4 M3 M2 M1。
- **連続する二試行が同じモチーフである割合**（`_motif_for_trial` から直接計算、T=1,740）

| 種 | 同じモチーフの組／連続する組 | 割合 | 種ごとの最小〜最大 |
|---|---:|---:|---:|
| s1（seed 1〜20） | 2,139 / 34,780 | **0.0615** | 0.0506〜0.0725 |
| s21（seed 21〜40） | 2,210 / 34,780 | **0.0635** | 0.0558〜0.0707 |

- **同じモチーフが次に出るまでの試行数**（s1）：1 が 6.2%、2 が 12.7%、3 が 18.8%、4 が 24.7%、5 が 18.8%、6 が 12.7%、7 が 6.2%。8 以上は無い。
- **モチーフの移り変わり**：「直前 → いま」の 16 通りのうち、同じモチーフ同士（M1→M1 など）はそれぞれ 519〜558、違うモチーフ同士はそれぞれ 2,662〜2,779（s1・s21 の各 20 種の合計）。
- **1 台帳での、比べられた組と登録の関係**（seed001・θ′2.1）

| 直前といま | 試行 | 登録なし | 新しく登録 | 同化・0.95 | 同化・名前の一致 | below_threshold の棄権 |
|---|---:|---:|---:|---:|---:|---:|
| 同じモチーフ | 110 | 0 | 59 | 42 | 9 | 0 |
| 違うモチーフ | 1,629 | 576 | 82 | 329 | 642 | 417 |

  → この台帳では、登録 1,163 件のうち 1,053 件（90.5%）が、違うモチーフの二場面の突き合わせから生まれた。below_threshold の棄権 417 回は、すべて違うモチーフの組だった。
- **仕様の文面**：B0B2 の場面の生成（B0B2:19・:48）。並び順の規則そのものは見ていない。★ B1:934 の A5 は「同一モチーフの提示間隔 S ≤ 枚の生存時間」を求め、S＝4 を使っている（:938）。
- **判定**：（突き合わせの表には無い。事実の確認として足した）
- **決定**：

### 1-D 場面の中身（参考）
- **今のコードの動き**（world.py:183-296）
  - 場面の関係は次のとおり。
    - モチーフの骨組み：一階はすべて実体 (a, b) の上に立ち、高階は子の関係 ID を引数に取る。
    - 媒介：袋の語から一つ引く。
    - 役割ユナリー。
    - 周縁：π_A の確率で入る。
    - 飾り（glue）：1〜3 本。
  - 伏せ辺の候補は次のとおりで、そこから一本を選ぶ。
    - 一階。b2 では二階も入る（holdout_include_second_order＝true、world.py:237-238）。
    - 媒介・周縁・飾り。
  - 役割ユナリーは伏せない。
- **仕様の文面**：B0B2 §B.4（B0B2:146-181）。二階を伏せ辺にするのはコードの注記で「仮U-42／仮D-59」（domains.py:182-184・world.py:236）
- **判定**：7 の表（holdout_include_second_order）
- **決定**：

---

## 2 同定（どの定義の話か）

### 2-A 0.95 の経路
- **今のコードの動き**
  - 生きている定義を「同化の回数が多い順、同じなら新しい順」に並べ、一つずつ、定義をいまの場面に写した点を、定義を自分に写した点で割る（abstraction.py:31-55）。
  - この比が 0.95 以上になった最初の定義を返す。
  - 返った名前は m1 に渡る（loop.py:98-117）。
- **仕様の文面**：B1:1026-1046（G2b）
- **判定**：1-1 一致
- **決定**：

### 2-B 同定に使う定義のグラフ
- **今のコードの動き**
  - 生きている行だけで作る。関係 ID と実体は、墓石も含めた全行から作る（abstraction.py:306-331、mode="live"）。
  - 既定を "live" にしたのはコミット `4392c73`（2026-09-10）。
- **仕様の文面**：B1:1035・:515「墓石も関係ノードとして含む」
- **判定**：1-3
- **決定**：**同定は生きている行だけ（アストラさんの意図と確認済み）**

### 2-C 自己点数
- **今のコードの動き**：使い回さず、毎回計算する（abstraction.py:36-39、self_score_cache＝off）。
- **仕様の文面**：B1:1048「メモ化してよい」
- **判定**：1-4 一致
- **決定**：

### 2-D 名前の一致の経路
- **今のコードの動き**
  - 0.95 に届く定義が無いとき、m1 は突き合わせの対から名前を作る。名前は、対の土台側の述語を並べ替えてつないだもののハッシュ（abstraction.py:88・:279-281）。
  - その名前の定義が既にあれば、その定義に同化する（abstraction.py:89・:98-105）。
  - 今夜の hide_s1（b2_hide_s1、80 台帳、`analysis_seql_2026-09-24/seql_hide_f00_s1_2026-09-24.md` 表0）：同化 84,850 のうち 0.95 の経路が 43,153、名前の一致が 41,697。0.95 に届かなかった m1 75,814 の行き先は、既存の定義へ 41,697・新規登録 10,102・何もせず 24,015。
- **仕様の文面**：B1:1045「どの def(R) も閾値に届かなければ**新規登録**する」、B1:139・:1028-1029（述語名の集合による同定の撤回）
- **判定**：1-2（仕様に無い動き）
- **決定**：

---

## 3 定義の生まれ方

### 3-A 生まれる条件と中身
- **今のコードの動き**
  - 直前の枚（土台）といまの場面を写した対のうち、次を満たすものを行の材料にする（abstraction.py:74-87・:289-302）。
    - 両側で**述語が一致**する。
    - 土台側で**構造の関係**（高階の関係か、高階の関係の引数になっている関係）である。
  - 材料が **2 本未満なら何もしない**。
  - 名前の定義が無ければ新しい定義を作る（abstraction.py:90-97）。
    - 材料の対の土台側の関係を全部行にする。位置の番号は並び順。
    - m_alloc は行の数（生まれたときで固定）。同化の回数は 1。
  - V を見て部分集合を選ぶことはしない。
- **仕様の文面**
  - B1:1088-1090（G5：サイズ 2 以上の部分集合を V が増える限り貪欲に選ぶ）。
  - B1:1079-1084（G4b）、:895-896（述語一致の対）、:1102-1103。
- **判定**：2-3（食い違い）・2-4（仕様に無い動き：構造の絞り）・2-5（一致：述語一致）
- **決定**：

### 3-B 値段と初期値
- **今のコードの動き**
  - 各行の値段は生まれたときに凍結する（abstraction.py:177-188、accounting.py:42-51）。
    - ℓ ＝ −log₂ p̂（その試行の書き込みの後の p̂）。
    - c ＝ 1＋3·arity−3·新規スロット数（pricing_rule＝spec）。
  - 功績は D-cold で初期化する（accounting.py:80-88、abstraction.py:112-119）。
    - 土台が直前の枚なので、base_age はいつも 1。
    - 例外費用の蓄えはその位置で 0 に戻す。
- **仕様の文面**：B1:308-311・:660-671・:877-899
- **判定**：2-11・2-12（一致）
- **決定**：

### 3-C 生まれた試行で消える行
- **今のコードの動き**
  - 生まれた行は、同じ試行の削除で V ＝ 1＋c/ℓ（生まれた直後）を θ′ と比べられる（deletion.py:26-54）。
  - c が 4 や 1 の行は、θ′ 2.1 でも消えうる。
  - b2 では数えていない。参考：所見記録 §51-1（v3a2_hide、直す前の模型）では、新規スロット 1・2 の行は 100% が寿命 0 だった。
- **仕様の文面**：B1:879「新規行が登録直後に落ちることはない」
- **判定**：2-13（仕様の中で矛盾）
- **決定**：

---

## 4 同化と補充

### 4-A 足すもの・足さないもの
- **今のコードの動き**
  - 材料の対（3-A と同じ作り方）の土台側の関係のうち、**定義の生きている行に同じ述語が無いもの**だけを足す候補にする（abstraction.py:202-203）。
  - 生きている行は、いまの場面と対応していてもいなくても、消さない・変えない（共通部分を取らない）。
  - 同化の回数を 1 増やす（abstraction.py:104）。
  - 今夜の hide_s1（`seql_hide_f00_s1_2026-09-24.md` 表1）：R の生きている行のうち、いまの場面との突き合わせで対応に入らなかった行の割合の中央は、0.95 の経路で 0、名前の一致の経路で 0.5。
- **仕様の文面**：B1:1057-1062（加算的・置換の禁止）、:182（原則2）。「生きている行にある述語は足さない」は文面に無い
- **判定**：2-9（一致：共通部分を取らない）・2-6（仕様に無い動き：足さない述語）
- **決定**：

### 4-B 足す場所・順番・あふれ
- **今のコードの動き**（abstraction.py:204-240、refill_rule＝one_per_slot）
  - 足す場所は、**生きている行がいない位置**の墓石だけ。同じ位置には一本だけ。
  - 並び順は定義の行の並びのまま。
  - 足す候補と場所を先頭から組にして、余った候補は捨てる。m_alloc は伸ばさない。
  - そのため、空いた位置が無い定義には足されない。
- **仕様の文面**：B1:313「位置は再利用され m_alloc は伸びない」、:1104。「一つに一本」「順番」「あふれ」は文面に無い
- **判定**：2-7（決裁で変更済み：案イ′ 2026-09-22）・2-8（仕様に無い動き：順番とあふれ）・2-10（帳簿の件、直し済み）
- **決定**：

### 4-C 位置ごとの履歴（slot_history）
- **今のコードの動き**
  - m1 のたびに（生まれるときも同化のときも）、定義を墓石込みでいまの場面に写す（abstraction.py:122-143、filling.py:266-287）。
  - 各行の引数が写った先と同じ引数を持つ**場面側の関係の述語**を、その位置の履歴に数える（local_lambda＝1.0 なので回数で持つ）。
  - 行が入れ替わっても、位置の履歴は引き継ぐ。定義が消えると履歴も消える（deletion.py:80-92）。
- **仕様の文面**：B1:590「そのスロットで観測された述語を入れる」、:593。いつ記録するかは書かれていない
- **判定**：2-14（仕様があいまい）・2-15（鍵）
- **決定**：

---

## 5 行と定義の削除

### 5-A 行の V と判定
- **今のコードの動き**
  - V ＝ 参加率 ×（節約 − 例外費用の蓄え ÷ Σ功績の基底）÷ ℓ ＋ w·embed/ℓ（accounting.py:201-235）。
  - V ≥ θ′ なら残り、下回れば墓石になる（accounting.py:256-259、deletion.py:26-69）。判定はこの一箇所だけ。
  - w＝0 なので embed の項は 0。fan-in は 0.0 のまま（deletion.py:135）。
- **仕様の文面**：B1:780-786（具体式）、:1109・:1125（一箇所）。★ :769・:772 は ΔL の分母を use_count と書く
- **判定**：3-1（仕様の中で矛盾：ΔL の分母）・3-2（一致）・3-5（embed、未実装、w＝0 で効かない）
- **決定**：

### 5-B θ′ の値
- **今のコードの動き**：θ′ ＝ 2.1 と 2.3 の二つのセル。同じ θ′ が行の削除と枚の削除の両方に使われる（1-B）。
- **仕様の文面**：B1:1129「θ′ の 4 点は 0.0410／0.1432／0.2637／0.3842、天井 0.3842」
- **判定**：3-3（仕様の中で矛盾）
- **決定**：**θ′ 2.1／2.3（行の崖を作るため。アストラさんの記憶による）**

### 5-C 参加率（功績）
- **今のコードの動き**
  - 発話に使われた定義（R_used）の生きている行だけ、次を加える（loop.py:242-260、accounting.py:91-142）。
    - 分子：充足なら 1、α＝0 なので充填では 0。
    - 分母：1。
  - ほかの定義の行は、減衰だけする。減衰は 16 本の梯子（accounting.py:71-77）。
- **仕様の文面**：B1:673-702
- **判定**：3-4・3-10（一致）
- **決定**：

### 5-D 課金
- **今のコードの動き**
  - **②**（loop.py:274-290）：発話に使われた定義の生きている行のうち、写った位置に別の述語の関係が見えている行に、E ＝ 2＋log₂ m_live＋ℓ_r を課す。候補が二本以上なら ℓ_r は平均。
  - **①**（loop.py:292-305）：棄権せずに外れ、かつ開示が出た試行だけ。課金する行は次のとおり。
    - 発話に使われた定義の生きている行のうち、突き合わせの relation_mapping に載った行。引数の写像だけで載った行も入る。
    - そこから repair_scope（first_order）に従って親の行へ伝える。
    - ℓ_r は開示された辺の述語の符号長。
  - **棄権の課金**：b2 では切ってある（abstain_charge＝false、loop.py:308-336）。
  - 例外費用の蓄えは、すべての位置で毎試行減衰する（loop.py:232-236）。
  - 蓄えの鍵は（定義, 位置）の二つ組（domains.py:253）。
- **仕様の文面**：B1:793-861・:863-873。★ ① の届き先は :805-806・:859（広い読み）と :858（狭い読み）で文面が二通り
- **判定**：3-6・3-8・3-9・3-10（一致）、3-7（仕様の中で矛盾）、2-15（鍵）
- **決定**：

### 5-E 定義の削除
- **今のコードの動き**
  - 行の削除の後、生きている行が 0 本になった定義を、功績・embed・例外費用・位置の履歴ごと消す（deletion.py:77-94、計装1）。
  - 名前が空くので、同じ述語の並びの対から次に生まれる定義は新規登録になる。
  - 今夜の旗オフ 1 台帳（`analysis_nohash_2026-09-24/check_off`）：登録 141 のうち 28 が消滅。
- **仕様の文面**：B1:1116-1119（定義全体の削除は実装しない）、:1130（E2 は θ′ 経由でのみ）
- **判定**：3-11（仕様に無い動き）
- **決定**：

### 5-F p̂ の語彙
- **今のコードの動き**：一度書かれた述語は語彙から抜けない（accounting.py:60-68）。
- **仕様の文面**：B1:319「退出は最後の生存出現の消失」
- **判定**：3-14（食い違い）
- **決定**：

---

## 6 発話

### 6-A 門の順番
- **今のコードの動き**（agent_runtime.py:66-183）
  1. 枚が無ければ棄権する（no_prototype）。
  2. 枚ごと（実際は直前の一枚）にいまの場面を写し、点が最大の枚を選ぶ。
  3. その点が threshold（0.0）に届かなければ棄権する（below_threshold）。
     - 1 台帳で 417/1,740 試行。すべて直前と違うモチーフの組（1-C）。
  4. 定義が無ければ棄権する（no_definition）。
  5. 生きている定義をそれぞれいまの場面に写し、支持を数える（6-B）。支持比が最大の一本を選ぶ。
  6. 支持が ⌈0.67·m_live⌉ に届かなければ棄権する（below_tau）。
  7. 投影と充填（7-A・7-B）。
- **仕様の文面**：B1:619-635・:421・:539-556。★ v3:167 は threshold を project の受理に掛ける
- **判定**：1-7（仕様の中で矛盾：threshold の置き場所）・4-2・4-3（一致）
- **決定**：

### 6-B 発話に使う定義の選び方
- **今のコードの動き**
  - 定義のグラフは、**生きている行だけ**で作る（agent_runtime.py:246-260）。
  - 支持 ＝ 生きている行のうち relation_mapping に載った本数、分母は m_live。
  - 同点は m_live が大きい方、新しい方、名前の順（agent_runtime.py:204-243）。
  - 選ばれなかった定義は、τ を通っていても使わず、会計でも加算しない。
- **仕様の文面**：B1:505-537（:515 は墓石を含むグラフ）・:692-702
- **判定**：1-5（食い違い：グラフ）・4-1・4-4（一致）
- **決定**：

### 6-C 予測の本数
- **今のコードの動き**
  - 予測は一本。投影が一本出ればそれを使い、出なければ充填の一本目（位置の番号順）を使う（agent_runtime.py:125-168）。
  - 採点も一本の中身が伏せ辺と一致するかだけを見る（accounting.py:262-275）。
  - 1 台帳：予測 312 本の内訳は、投影 117、生きている行の充填 52、墓石の充填 143。
  - 腕全体（s21、各 80 台帳、`analysis_asrun_2026-09-25/数え上げ_経路と生まれ方_2026-09-25.md`）：墓石の充填の割合は f=0 で 0.616、f=0.25 で 0.489、f=0.5 で 0.424、f=1.0 で 0.378。
    - predictions_all_slots が 2 本以上の試行は、予測 20,913〜29,580 本のうち 13,328〜18,369。
- **仕様の文面**：B1:634「全充填スロット集合を予測として返す」、B0B2:294「予測集合から内容一致」
- **判定**：4-5（食い違い）・4-6（仕様があいまい）
- **決定**：

---

## 7 投影と穴埋め

### 7-A 投影
- **今のコードの動き**
  - 選んだ定義（生きている行だけのグラフ）の行のうち、次を満たす行を候補にする（sme.py:215-249）。
    - 場面に写っていない。
    - 引数が全部写っている。
  - 場面に同じ中身が既にあるものは除く。
  - 構造の支持で順位を付け、首位が同点なら棄権する（ambiguous_projection）。
  - 墓石はグラフに無いので投影されない。
- **仕様の文面**：v3:147、B1:537（墓石を未観測スロットとして扱う）
- **判定**：1-5
- **決定**：

### 7-B 穴埋め（充填）
- **今のコードの動き**（filling.py:118-261）
  - 定義の行を位置の番号順に見る。**墓石の行も見る**。
  - いまの場面に写った先が見えている行と、写った位置に同じ述語の関係が見えている行は、埋めない。
  - 引数が写せない行も埋めない。高階の行は、子を先に埋めてつなぐ。
  - 候補は位置の履歴。無ければ、アリティと引数型が合う語彙。そこから構成素と同じ階数の述語だけに絞る（2-B、filling.py:212-222）。
  - 重みは（その位置での相対頻度）^1.0 × p̂（filling.py:40-61、local_lambda＝1.0）。
  - most_frequent のセルでは最大の重みを選び、同点なら棄権する（ambiguous_projection）。
  - sample のセルでは重みで一本を引く（filling.py:100-117・:230-233）。
  - 1 台帳：予測 312 本のうち 143 本（45.8%）が墓石の行の充填。
  - 腕全体（s21）：墓石の充填のうち、その墓石が寿命0 の行だったものは 0.000〜0.008（f=1.0 で 0/7,898、f=0 で 153/18,221）。
- **仕様の文面**：B1:558-599（:564-565 は墓石を飛ばす）、:1616（U-085 未決）
- **判定**：5-1（墓石）・5-3・5-4（一致）・5-5（決裁で変更済み：重み）・5-6（仕様があいまい：抽出）・5-7（仕様に無い動き：階数）
- **決定**：**墓石からの穴埋め（アストラさんの決定と確認済み。仮U-3 を閉じる）**
  - ★ この決定は 5-1 の墓石の件に当たる。重み・抽出・階数の欄は空けておく。

---

## 8 フィードバック

### 8-A 開示
- **今のコードの動き**
  - 試行ごとの共通乱数 coin_t が f（腕ごとに一定、sweep.py:46-53）より小さければ、伏せ辺をそのまま返す（loop.py:88-89、feedback.py:39-52）。
  - 当たり外れや棄権にはよらない。
  - 返った辺は、いまの場面の枚と p̂ に書く（1-A）。その枚は次の試行の土台になる（1-B）。
  - f_realized を毎試行記録する。
- **仕様の文面**：B1:992-997・:1295
- **判定**：6-1・6-2・6-3（一致）
- **決定**：

### 8-B ① への結びつき
- **今のコードの動き**：5-D の ① を参照。開示が出て、発話が外れた試行だけに課金する。
- **仕様の文面**：B1:802-806
- **判定**：3-7・3-8
- **決定**：

---

## 9 設定値（b2 の 8 本すべて同じ。f と種だけ違う）

| 設定 | 値 | 仕様 | 判定 | 決定 |
|---|---|---|---|---|
| f | 0／0.25／0.5／1.0（腕） | B1:994-996 f(agent,t) | 6-1 | |
| θ′ | 2.1／2.3（セル） | B1:1129 | 3-3 | **2.1／2.3（行の崖を作るため。アストラさんの記憶による）** |
| verbatim_theta | 指定なし（θ′ に落ちる） | B1:931 | 2-2 | |
| fill_selection | most_frequent／sample（セル） | B1:588・:598 | 5-6 | |
| repair_scope | first_order | B1:841-852 | 一致 | |
| threshold | 0.0 | 既定なし（v3:70・:168） | 7 の表 | |
| tau_acc | 0.67 | B1:556 | 一致 | |
| nsim_threshold | 0.95 | B1:1037 | 一致 | |
| identification_graph | live | B1:1035 | 1-3 | **同定は生きている行だけ（アストラさんの意図と確認済み）** |
| self_score_cache | off | B1:1048 | 一致 | |
| refill_rule | one_per_slot | 文面に無い（案イ′） | 2-7 | |
| pricing_rule | spec | B1:310 | 一致 | |
| local_lambda | 1.0 | B1:593・:1616 | 5-5 | |
| holdout_include_second_order | true | 文面に無い（仮U-42／仮D-59） | 7 の表 | |
| α／β | 0.0／0.0 | B1:1238-1239 | 一致 | |
| w／κ | 0.0／1.0 | 腕（D14） | 一致（w＝0 で embed は効かない） | |
| λ_mix | 0.1 | B1:296 | 一致 | |
| abstain_charge | false | B1:868 | 一致 | |
| correction_mode | none | v3:70 | —（見ていない） | |
| 試行数・種 | 1,740／seed 1〜20（s1）・21〜40（s21） | B1:1294 | 一致 | |

---

## 書いていないこと・確かめていないこと
- 台帳の欄（記録専用の欄）の仕様との一致は見ていない。
- 「効き」の数の多くは 1 台帳の値。
- 決裁の記録そのものは、案イ′ を除いて確かめていない。仮U-3・仮U-41・仮U-42／仮D-59 は、コミットとコードの注記だけを見た。

---

## 10 記録の控えの持ち方（2026-09-25 午後に変更。出力は同じ）
- **今のコードの動き**
  - 毎試行、状態全体を正準形にして、指紋と前の試行との差分を台帳に書く（loop.py:467-477）。
  - 正準形を作るときの控え（loop.py:31-33）は、走行の始めに一度空にするだけで、走行のあいだ捨てなかった。
  - そのため、台帳一本で最大 6.0 GB（v2）、18.1 GB（v3 の組み合わせ）を使っていた。
- **変更**（アストラさんの決定、2026-09-25）
  - 今後の走行は、控えを「前の試行で触った物とその子孫」だけに絞る書き直し（tools/lowmem.py、ドライバ tools/v3_run.py の既定）を使う。
  - abm/ は変えていない。ドライバの作業プロセスの中で loop の関数を差し替える。
- **出力**：台帳は一字一句同じ。7 本で確かめた。
  - 比べたもの：全試行の指紋と、見出しを除いた台帳全体。
  - 7 本の内訳：v2 の hide の 4 セル・f00・f10 は runs/ の既存と、v3 の組み合わせ 1 本は書き直し前の台帳と比べた。
  - 最大メモリは 745〜795 MB（v2）、962 MB（v3 の組み合わせ）になった。
- **仕様の文面**：記録の控えの持ち方は、仕様に書かれていない（台帳の欄は B1:1134 以降）。
- **判定**：—（出力を変えない実装の変更）
- **決定**：**控えを絞る書き直しを既定にする（アストラさんの決定、台帳が同じと確かめた）**
