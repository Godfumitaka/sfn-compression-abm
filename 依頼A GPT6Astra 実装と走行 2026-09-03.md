# 依頼 — ABM の生成世界の再設計と、その実装・走行

宛先 GPT-6 Astra ／ 発行 2026-09-03 ／ リポジトリ `GODfumitaka/sfn-compression-abm`
HEAD `05c36bdc605b13a44a1fe98f325e54e7987af892`

---

## 0. この依頼の性格

**助言ではなく実装と走行を含む依頼です。**Codex を使える環境を想定しています。

★ 我々は現在 Mac にアクセスできず、走行を回せません。**そちらで回してください。**

★ **§3 の機構記述は前提として渡します。**課題ではありません。ただし**誤りがあれば
指摘してください。**行番号をつけてあります。我々は本日、この記述の一部を半日以上
まちがえていました。まちがった記述の上に設計を提案されると、無駄が生じます。

---

## 1. 研究の目的

計算認知科学のエージェントベースモデル。中心的な主張はこうです。

> エージェントは、自分の内部量だけを用いて、
> 自分の定義（def(R)）が「真の抽象」なのか「神話」なのかを区別できない。

**神話とは、名前のついた関係定義が構成素を失って過度に一般化し、
ほぼ何にでも当てはまるようになったものです。**「空」ではなく「薄い」。
具体的な予測はするが、反証されにくい。

理論的背景は SME（構造写像）、MDL（最小記述長）、ACT-R の記憶動態の統合です。
Koriat & Goldsmith (1996) の量−正確性トレードオフが直接の対応物として特定されています。

---

## 2. ★ 現在いちばん困っていること

**「有用で複雑な抽象が薄い神話になる」過程が、模型の中で一度も起きていません。**

```
m_live_max > m_live_birth かつ m_live_final ≤ 2   ★ 0 本 / 73
（★ 一度厚くなった定義が、薄い状態で終わった例）
```

理由は二つ、どちらも特定済みです。

**① 種が浅い。**共有されうる関係が 5〜6 本しかなく、定義は 4 本前後で生まれます。
そして 4 試行で 2 本に落ちます。**それは過程ではなく崩落です。**

**② 埋め戻しが降下を打ち消す。**78% の定義で、死んだ構成素が再追加されます。
ある定義は m_live 3⇄4 を 61 回繰り返し、1,740 試行の最後まで収束しませんでした。

---

## 3. 現在の機構理解（★ 前提。誤りがあれば指摘してください）

すべて本日、コードの引用と走行の再生成で確認したものです。

### 3-1　定義の生成と削除

```
abstraction.py:78-84    二場面の構造写像で対応がついた関係が構成素になる
abstraction.py:163      追加対象は「生存構成素に無い述語」
                        ★ 死んだ述語は再び追加されうる（＝埋め戻し）
abstraction.py:167,175  _extend_definition は rows.append。★ 墓石は消えない
abstraction.py:179      NamedDefinition の m_alloc は初回のまま更新されない
deletion.py:50-53       V < θ′ なら alive=False。★ 行は残る
```

### 3-2　保持価値

```
definition.py:17-24     FrozenPrice(ell_frozen, c, hold_cost, m_alloc_at_reg)
                        saving = ell_frozen + c
accounting.py:221-223   delta_l = saving − (例外費用/機会) ／ a = delta_l/ell_frozen
                        V = participation × a + w×(embed/ℓ)
accounting.py:256-259   survives = V >= θ′
accounting.py:42-51     freeze_price: c = 1 + 3·arity − 3·new_slot_count
abstraction.py:185-189  _new_slot_count ＝ その関係にしか現れない引数の個数
```

★ **`new_slot_count` は def ごとに変わります。**同じ述語でも 0・1・2 が混在します
（`break` は 16 / 8 / 12 件）。**種に書ける量ではありません。**

### 3-3　構造写像

```
sme.py:288-290      候補は「述語一致かつアリティ一致」で生成
sme.py:126-137      一対一制約あり（relation_mapping / used_relations）
sme.py:305-311      ★ 高階の引数どうしは 述語が違っても対応する（体系性）
sme.py:326-327      候補は -arity で並ぶ。★ 高階が先に処理され一階を連れていく
sme.py:364-365      ★ 引数に関係を持つ関係は 投影候補から除外される
```

### 3-4　修復の射程

```
loop.py:355-369     _repair_targets。depth = 1(FIRST_ORDER) / 2(SECOND_ORDER) / None(ALL)
domains.py:42-45    RepairScope は 3 値。★ SECOND_ORDER は掃引で使われていない
```

★ **現在この軸は縮退しています。**base={core1} から 1 歩で {higher, tower} に届き、
2 歩目が空になるため。**塔の深さが 1 だからです。**

### 3-5　現在の生成世界

```
world.py:20-25      MOTIF_ROWS。4 モチーフ
world.py:107-128    1 場面 = 固定 6 本 ＋ 周縁(確率π_A) ＋ glue 1〜3 本
world.py:112        ★ 塔は 4 モチーフとも "allow" で固定
world.py:115-131    伏せ候補は core1 / core2 / mediator / peripheral / glue
                    ★ 塔・高階・役割ユナリーは 一度も伏せられない
```

★ **共有されうるのは 5〜6 本。**mediator は毎試行 bag から抽選されるため共有されません。

### 3-6　種ファイル

`U-011_seed_v1.json`（9,529 バイト）。**実行時に読まれるのは
`pi_A` / `one_minus_h` / `bags` / `role_unary` / `glue` のみ。**
`u` / `u_target` / `theta_grid` は読まれません（検算とドキュメント）。

★ 我々は種の全数値の生成式を復元し、**v1 を完全に再現するコードを持っています**
（`seed_gen.py`。構成素 24 件 × 6 量 ＋ marginal 54 件が一致）。

```
Z = 1 試行あたりの関係トークン期待数
marginal = 期待出現回数 / Z ／ ℓ = −log₂(marginal)
c = 1 + 3·arity − 3·new_slots ／ a = 1 + c/ℓ ／ u = a × π × (1−h)
```

★ ただし **`new_slots` が設計できないため、`c`・`a`・`u`・θ′ 格子は
種から導けません**（3-2 参照）。

---

## 4. 我々の改善案 v2a（★ 叩き台。より良い案を求めます）

### 4-1　構造

一階 8 語を 2 語ずつ組にして二階をかぶせ、**部分木**を 4 つ作ります。

```
hA = cause(hold, push)    hB = require(carry, lift)
hC = enable(break, cut)   hD = avert(turn, press)
```

各モチーフは**隣り合う 2 つ**を持ち、その上に**モチーフ固有の三階**を置きます。

```
M1 =  allow(hA, hB)     M2 = depend(hB, hC)
M3 = prevent(hC, hD)    M4 =  block(hD, hA)
```

### 4-2　狙い

```
共有されうるのが 9 本      → 厚い定義が生まれる（現在 5〜6 本）
三階→二階→一階の 3 層      → 射程軸に噛む深さができる
三階がモチーフごとに別語    → 三階が写像を横取りしない（4-3 参照）
rate 1.0 の述語が消える     → 次数と共有頻度の交絡が緩む
```

### 4-3　★ 検証済み（`map_graphs` に手で組んだ場面を通した）

| 照合 | 事前に登録した期待 | 結果 |
|---|---|---|
| M1 対 M1 | 7 本が対応 | **7 本** ✓ |
| M1 対 M2 | `require` の部分木 3 本のみ | **carry / lift / hB** ✓ |
| M1 対 M3 | 0 本 | **0 本** ✓ |

**反対条件も試しました。**三階を共通（両方 `allow`）に戻すと、`hA ↦ hB2` が先に成立し、
**`require ↦ enable` という誤対応**が起きます。意味の違う二階が構造の位置だけで同一視される。

★ **`world.py:112` の `allow` 共通は、v1 で既にこれを起こしていた可能性が高い。**

### 4-4　★ 添付する `U-011_seed_v2a.json` について

**構造（モチーフ割り当て）だけが有効です。数値は無効です。**
`new_slots` を全構成素 0 と置いていますが、実測との一致率は 27.22% でした。
**算術の検証に時間を使わないでください。**

---

## 5. 依頼すること

### 5-1　種の設計（★ v2a を叩き台に、より良い案を）

**満たすべき条件**

```
A  厚い定義が生まれうる（m_alloc_true の中央 8 以上を目標）
B  厚い定義が薄くなりうる（段階的な降下。2 段の崩落ではなく）
C  深さ 2 以上の塔が立ち、修復の射程軸が縮退から抜ける
D  次数（高階/一階/媒介）と共有頻度が交絡しない
```

**既知の制約（★ 再発見に往復を使わないでください）**

```
abstraction.py:163  同一述語の二本目は追加されない。★ 述語の「種類」を増やす必要がある
sme.py:290          写像には述語一致が要る
sme.py:305-311      高階の引数は述語一致なしに対応する
sme.py:364-365      高階は投影候補に入らない。★ 高階を伏せ候補にするには改修が要る
共通の三階は誤対応を生む（4-3）
```

### 5-2　ρ（埋め戻し）の扱い

我々は **ρ ∈ {0, 1} を掃引軸とし、主を 0（再追加を禁じる）と決めました。**

```
ρ=0 側の支持  SEQL/GEL の累進的整列は 抽象の単調なスキーマ化を要求する
ρ=1 側の支持  Koriat & Goldsmith の粒度統制は 可逆・戦略的である
```

★ **この判断への批判も歓迎します。**

### 5-3　θ′ 格子の置き方

**種から導けません（3-2）。走行して測る必要があります。**

v1 の意図は「一段ごとに生き残り方が質的に変わる」ことでした。

```
0.0410 → 0 本が死ぬ ／ 0.1432 → 4 本 ／ 0.2637 → 6 本 ／ 0.3842 → 8 本
```

★ **v2 で同等の性質を持つ 4 点を決める手順を設計し、実行してください。**

### 5-4　実装と走行

```
1  世界生成（world.py の MOTIF_ROWS ほか）を新しい種に合わせる
2  試験 59 箇所の修正（★ 一覧を添付）
3  --calibrate で走行費用を実測（sweep.py:304-334）
4  段階1 の走行  repair_scope 3 水準 × θ′ 4 × seed 20 ＝ 240 走行
```

### 5-5　★ 実装の自由度について

**実装は自由に進めてください。**ブランチを分けても、案を複数試しても、
走らせながら設計を変えても構いません。

★ **種の良し悪しは走らせないと評価できません。**m_alloc_true が 8 に届くか、
降下が段階的になるか、射程軸が噛むか——**すべて走行の結果です。**
  設計を先に確定させる必要はありません。

**守っていただきたいのは、次の四つだけです。**

```
1  runs/ 以下の既存走行（5,762 本）を変更・削除しない
2  analyze_rev6.py を書き換えない（★ 複製してください）
     → T-105（ビット再現）の基盤であり、本日 別 OS・別 Python で再確認されたばかりです
3  U-011_seed_v1.json を書き換えない（★ 新ファイルを作ってください）
     → sweep.py:254 が sha256 を走行ヘッダに記録するため、種の同一性が追跡できます
4  main に直接コミットしない
```

★ **これ以外に制限はありません。**壊れても捨てられるものは、自由に試してください。

---

## 6. 返してほしいもの

### 6-1　★ 走らせる前に

```
何が起きると予想するか（★ 確率を添えて）
何が観測されたら 設計が失敗だと判断するか
```

★ **数値を見てから判定量を選ばないためです。**我々はこの規範を持っています。

### 6-2　走行後

```
台帳の sha256 と world_hash ／ .done の内容
per_def の 生の欄（★ 加工前）
実行したコマンドそのまま
6-1 の予想と実際の対照
```

★ **結論の要約ではなく、生の数値と引用を返してください。**

★ 我々は本日、解析出力の欄名を内容の保証として使ったことで 17 件の撤回を出しました。
  **設計・実装・走行・解釈を一者が担うと、誰も検算しません。**

### 6-3　機構記述への指摘

§3 に誤りがあれば、**行番号つきの引用で**示してください。

---

## 7. 我々が既に排除した選択肢（★ 再提案を避けるため）

```
「種を大きくしない」          m_alloc_true 中央 4 では 4→2 の二段しかない
「関係の本数を増やす」         同一述語の二本目は弾かれる（abstraction.py:163）
「塔を共通にしたまま深くする」  誤対応が起きる（4-3 で実測）
「θ′ 格子を種から計算する」     new_slots が設計できない
「per_def の m_alloc を厚さとして使う」  ★ 回転で膨らんだ累積行数だった
```

---

## 8. 添付

```
U-011_seed_v1.json           現行の種
U-011_seed_v2a.json          ★ 構造のみ有効。数値は無効
seed_gen.py / verify.py      種の生成器（★ v1 を完全に再現する）
gen_v2a.py                   v2a の生成器
台帳追記7_2026-09-03_rev4.md  本日の確定事項 16 件・撤回 17 件
引き継ぎ_2026-09-03.md        現状と次の手順
試験 59 箇所の一覧
```
