# v3.1-slotdump の確かめ（2026-09-26）

## 足したもの
- **tools/v3_run.py に `--dump-slot-history`**（既定オフ）
  - 走行末に、全定義の slot_history（墓石の席も含む）を side の最後の行に書く。欄は `slot_history_end`（定義 → 位置 → 述語の回数）。
  - あわせて、各定義の行を `constituents_end`（[位置, 登録試行, 述語, 生死] の並び）として書く。
  - 記録だけ。台帳（ledgers/）には何も書かない。各試行の削除の後の状態を指しておき、最後の一つだけを書き出す。
- **tools/l2scan_spoke_v6.py**：クラウドの Code の tools/l2scan_spoke.py 版 5（md5 95c98e43…）に、列を二つ足した。
  1. 出どころ × 旧の内外：源投影旧内_* など。
  2. 出どころ × 四分割（版 5）：源投影五見話_*・五見未話_*・五未見未話_*・五未見話_*（源生充・源墓充も同じ）。
  - 版 5 の数え方は変えていない。

## 確かめ
- **旗オンの一本**：v3.1a・NSIM 0.8・hide・seed001・θ′2.1（`--nohash --vt 0.3842 --greedy --extend-rule profit --charge1 d32 --nsim 0.8 --dump-slot-history`）をこのマック（Apple M5）で走らせた。238 秒・最大 809 MB。
  - クラウドから持ち帰った指紋（fp_v31a_n80_hide_s1.json.gz、c7a.16xlarge で走らせた台帳）と比べた。
  - 全 1,740 試行の `agent_state_snapshot_hash`・行ごとの sha256 の頭 16 桁・見出しを除いた本体の sha256・見出しの全欄が**一致**。
  - side の最後の行には、走行末の 33 定義すべての slot_history と行が入った（墓石の行 236）。
- **版 6 の走査**：b2_hide_s21・seed001 の一本（`--limit 1`）を、版 5 の手元の写しの出力と比べた。
  - 足した列を除いて、61 定義すべて一致。
  - 出どころ × 四分割の主張の合計 973 ＝ 四分割の主張の合計。

## 使い方の例（リポジトリの根で）
```
python3.12 tools/v3_run.py config/sweep_b2_hide_s1_2026-09-22.json <出力の根> --nohash --vt 0.3842 --greedy --extend-rule profit --charge1 d32 --nsim 0.8 --dump-slot-history --seeds 1,2,3,4,5 --workers 6
python3.12 tools/l2scan_spoke_v6.py <腕名> <走行根> seeds/U-011_seed_v3a2.json <出力json> [workers]
```
