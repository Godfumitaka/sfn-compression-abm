# v3.3 の確かめ（2026-09-26 夜、このマック）

ブランチ v3.3-work（v3.2-main f915152 から）。abm/ は変えていない。どの旗も、切れば今の動き。

## 穴埋めの同点で投影を捨てない（--proj-first、tools/projfirst.py）
- agent_runtime の project・fill_missing_slots を包み、投影が一本出ていて穴埋めが同点なら、穴埋めの結果の同点の印だけを外す（predict の中身は写さない）。
- 旗なし：本番の段 1 の今の同化（seed001・002 × 4 セル）が、試し 24 本の同じ台帳と一字一句同じ（8 本）。
- 旗あり：最頻のセルで、最初に違う試行はどれも「前：穴埋めの同点で棄権 → 直し：投影」（試行 5）。穴埋めの同点での棄権は 118→104 回など（投影が出ていなかった所は今と同じく棄権）。sample のセルは同点が無く変わらない。

## 直し②を予測と会計にも（--fix2-full、tools/fix2.py）
- 選んだ定義の写しとして、直し②の写しをそのまま返す（投影・穴埋め・会計が同じ写しを使う）。--fix2（選び方だけ）は残した。
- 小さな例（墓石 T：x(a,b)、親 P：P(t,c)、Q：Q(a,c)、U：U(b,c)、席 0 の履歴 {y:1, x:3}）。手の答えとすべて一致した。

| 場面 | 旗なし | --fix2（選び方だけ） | --fix2-full |
|---|---|---|---|
| 見える子・履歴にある（y） | 支持 2・会計で P は判定不能・T を x で埋める | 支持 3・判定不能・T を x で埋める | 支持 3・充足・T は埋めない |
| 見える子・履歴に無い（z） | 支持 2・判定不能・T を埋める | 同じ | 同じ |
| 見える子・回数 0 | 支持 2・判定不能・T を埋める | 同じ | 同じ |
| 伏せられた子 | 支持 3・充足・T を x で埋める | 同じ | 同じ |

- --fix2-full を足さなければ、試し 24 本の同じ台帳（--fix2・--fix-order）と一字一句同じ（8 本）。
- 走査の版 7 に同じ直しを入れた（flag.json の fix2_full）。作り直した発話と台帳の発話は、次のとおり全部一致した。
  - --fix2-full ＋ --proj-first ＋ --fix-order の今の同化 4 本：709・926・672・891。
  - 新しい同化 400 試行：223。
  - --proj-first の 8 本：すべて一致。

## 本番の流れの台本（tools/prod_run.sh ほか）
- 基準 0.7。デスクトップ：hide で 主（ρ＝0.5）→ ρ だけ 0 → 今の同化 → ρ＝1.0（並列 14）。マック：主で f00 → hide → f10（並列 6）。
- 直しの旗（既定）：--fix2-full --fix-order --proj-first（名前の順番の直し・案 1 は、届いてから足す）。

## 名前・番号に依らない写し（--fix-order2、案 1、調べの係の fixorder2-2026-09-26 37377dc）を取り込んだ（3ac965e）
- --fix-order の代わりに使う（両方は付けない）。走査の版 7 と数え（tools/prod_readcounts.py）も、flag.json の fix_order2 に合わせて同じ写しを使う。

## 最終の確かめ（コミット b157abd、20:43〜20:54、このマック）
| 確かめ | 結果 |
|---|---|
| 旗をすべて切った v2 seed001（--no-public-history なし） | runs/ と一字一句同じ |
| 旗をすべて切った v3.1b seed001 | マックの前の台帳と一字一句同じ |
| v3.1a seed001 ＋ --ident-shadow | 一字一句同じ。1,739 回の同定すべてで、書き直した同定が元と同じ答え |
| 本番の設定（主 ρ＝0.5・基準 0.7・--fix2-full --fix-order2 --proj-first）の台帳一本、--rename-check | 同定 1,739 回で、名前を付け替えて判断が変わった回 0（逆さ読み）・0（sha 付け）。比が変わった回 0・0、共通構造が変わった回 0・0。付け替えなしのやり直し 300／300 同じ |
| 本番の設定の今の同化 4 本・主 400 試行 → 走査の版 7 | 作り直した発話が台帳と全部一致（634・859・634・859・218、不一致 0） |
| 本番の台本（25 試行・種 1 と 3・主と今の同化） | 腕ごとに解析 8・控え 8・残した台帳 4・まとめ・図・README |
- 同じ日の d63abaf（案 1 の前）で、旗をすべて切った v3.1b も一致。v2・v3.1a・v3・shadow は走行中にタグを付けた（v3 の道筋は b157abd でも変わっていない）。
