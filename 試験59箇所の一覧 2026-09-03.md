# 種を変えると影響する試験の一覧（全 59 件）

作成 2026-09-03 ／ 出所：HEAD `05c36bdc605b13a44a1fe98f325e54e7987af892` での grep 実測

★ 「落ちるか」の判定は**していません。**v2 の内容に依存するためです。
  **場所の網羅のみ**を目的としています（規範11d：数え上げは grep で全部洗い出す）。

---

## 1. ファイルごとの件数

```
tests/test_b1_acceptance.py       24
tests/test_verbatim_theta.py      15
tests/test_b0b2_t7.py             10
tests/test_world_b0b2.py           5
tests/test_b3_beta.py              3
tests/test_b1_base_written_at.py   1
tests/test_b1_triple_key.py        1
                              合計 59
```

★ **加えて `abm/seed.py:64` に `len(constituents) != 24` の検査がある。**
  試験ではないが、種を変えると必ず引っかかる。

---

## 2. ★ 種の値の複写（★ 二重化に注意）

**同じ定数が 2 ファイルに複写されている。**片方だけ直すと落ちる。

### `tests/test_b1_acceptance.py:39-48`

```
39	MOTIFS = {
40	    "M1": (("hold", ("a", "b")), ("push", ("b", "a")), ("require", ("core1", "core2"))),
41	    "M2": (("carry", ("a", "b")), ("lift", ("a", "b")), ("cause", ("core2", "core1"))),
42	    "M3": (("break", ("a", "b")), ("cut", ("a", "b")), ("cause", ("core2", "core1"))),
43	    "M4": (("push", ("a", "b")), ("turn", ("b", "c")), ("cause", ("core1", "core2"))),
44	}
45	LIVE_IDS = frozenset(("core1", "core2", "higher", "tower"))
46	PI_A = {"M1": 0.060701, "M2": 0.081710, "M3": 0.130073, "M4": 0.265404}
47	PERIPHERAL = {"M1": "carry", "M2": "cold", "M3": "carry", "M4": "hard"}
48	GLUE = ("near", "above", "below", "beside", "behind", "inside")
```

### `tests/test_b0b2_t7.py:14-23`　★ 上と同一内容の複写

```
14	MOTIFS = { ... }        （★ 39-44 と同一）
20	LIVE_IDS = frozenset(("core1", "core2", "higher", "tower"))
21	PI_A = { ... }          （★ 46 と同一）
22	PERIPHERAL = { ... }    （★ 47 と同一）
23	GLUE = ( ... )          （★ 48 と同一）
```

### `tests/test_b1_base_written_at.py:17`

```
17	THETA_PRIMES = (0.0410, 0.1432, 0.2637, 0.3842)
```

---

## 3. ★ 数値を表明している箇所（★ 種を変えると値が変わる）

### `tests/test_b1_acceptance.py:336-347`　m_live の到達点

```
337	    ("motif", "expected_live"),
338	    (("M1", 4), ("M2", 2), ("M3", 2), ("M4", 4)),
...
347	    assert deleted.definitions["R"].m_live == expected_live
```

### `tests/test_b1_acceptance.py:362` / `:438`　m_alloc の上限

```
362	    assert all(definition.m_alloc <= 6 for definition in state.definitions.values())
438	    assert all(definition.m_alloc <= 6 for definition in state.definitions.values())
```

### `tests/test_b1_acceptance.py:447-452`　枚数の収束

```
448	    for theta_prime, expected in ((0.1432, 49), (0.2637, 14), (0.3842, 7)):
451	        assert expected * 0.5 <= sum(sizes[-20:]) / 20 <= expected * 1.5
452	        assert max(sizes[-20:]) - min(sizes[-20:]) <= expected * 0.5
```

### `tests/test_b1_acceptance.py:476-478`　m_alloc の下限

```
478	    assert any(definition.m_alloc >= 4 for definition in state.definitions.values())
```

### `tests/test_b1_acceptance.py:441-444`　m_live が単調増加でない

```
444	    assert any(current < previous for previous, current in zip(values, values[1:]))
```

### `tests/test_world_b0b2.py:28-33`　モチーフの件数と並び

```
31	    assert Counter(motifs) == {motif: 2_000 for motif in MOTIF_ROWS}
33	        assert set(motifs[start : start + len(MOTIF_ROWS)]) == set(MOTIF_ROWS)
```

★ **モチーフ数を 4 から変える場合に効く。**

### `abm/seed.py:64-65`　構成素の件数

```
64	    if len(constituents) != 24:
65	        errors.append(f"constituents は24件ではない: {len(constituents)}")
```

---

## 4. θ′ の値を直接指定している箇所（★ 格子を置き直すと影響）

```
tests/test_b1_acceptance.py:344        theta_prime=0.2637
tests/test_b1_acceptance.py:395        _longitudinal_run(0.2637)
tests/test_b1_acceptance.py:401-404    0.0410 / 0.1432 / 0.2637 / 0.3842
tests/test_b1_acceptance.py:442        _longitudinal_run(0.3842)
tests/test_b1_acceptance.py:460        parametrize (0.0410, 0.1432, 0.2637, 0.3842)
tests/test_b1_acceptance.py:477        theta_runs[0.2637]
tests/test_b1_acceptance.py:520, 537   theta_runs[0.3842]
tests/test_b3_beta.py:139, 214, 336    theta_prime=0.1432
tests/test_b1_triple_key.py:33         theta_prime=0.1432
tests/test_verbatim_theta.py:14, 23-24, 27, 34-35, 39-42, 50, 60-63   （★ 15 件）
```

★ `tests/test_verbatim_theta.py` は θ′ と vt の**組み合わせと cell 名の生成**を試験している。
  **格子を置き直すと cell 名の期待値がすべて変わる。**

---

## 5. ★ 落ちないと判断できるもの

```
tests/test_ledger_b0b2.py:42-45   台帳の欄数（38 / 26 / 24 / 8）
                                  ★ 種と無関係。台帳の欄定義に依存する
```

---

## 6. 注意

★ `tests/test_b1_acceptance.py` と `tests/test_b0b2_t7.py` の複写は**内容が同一**である。
  **片方だけ更新すると、もう片方で落ちる。**

★ 前例：`ARM_DESCRIPTOR_FIELDS` を数える試験が 3 ファイルにあり、
  2 つだけ更新して pytest が 1 件落ちた（規範11d の由来）。

★ 本一覧は**場所の網羅**であり、**修正方法を含まない。**
