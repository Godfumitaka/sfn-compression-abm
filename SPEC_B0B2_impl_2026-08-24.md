<!-- 自動生成。手で編集しない。原本は SPEC_B0B2_full.md、
     さらにその原本は節分割 SPEC v4（§1〜§7）。食い違ったら上流が正 -->

# SPEC B0+B2 — 装置と世界（実装用）

起草: 2026-08-23 / 起草者: Claude
対象: 実装者（Codex）／ red team（Fable）
原本: `SPEC_B0B2_full.md`。本書は `strip_sources.py` の自動生成物。手で編集しない

---

## §B.0 この文書の位置づけ

### 範囲

**エージェントが一体もいなくても走る部分**を作る。

```
入る   種の読み込み・場面の生成・伏せ辺・世界列とハッシュ・ループの骨組み
       オラクル・f・走行台帳
入らない  def(R) の会計（§3）・照合（§4）・充填（§4）・削除（§3.5）・伝達（B4）
```

範囲外の部分は**フックだけ置き、呼ばれたら `NotImplementedError` を投げる**。

### この文書が守る基本原則

```
原則1  世界列はエージェント状態・予測・正誤・掃引パラメータに依存しない
       run seed から事前生成でき、そのハッシュが全掃引腕で一致する      [§2.2.3 / D6]

原則2  台帳はループから write-only。同期・追記のみ。read API を作らない  [§1.5.5 / D1 / D23]

原則3  ID は不透明。連番・モチーフ名・層名・演算子名・来歴を埋め込まない [§2.2.3 / D3(a)]

原則4  f を定数で書かない。f(agent, t) の署名で研究者側に持つ           [§3.8 / D8(d)]

原則5  predict は AgentState を変えない                                 [§2.0 / D9(b)]
```

---

## §B.1 成果物

| モジュール | 責務 |
| --- | --- |
| `abm/seed.py` | `U-011_seed_v1.json` の読み込み・検証・ハッシュ |
| `abm/world.py` | 場面の生成・伏せ辺・世界列の事前生成 |
| `abm/oracle.py` | O1〜O3 の採点と開示 |
| `abm/feedback.py` | `f(agent, t)` と共通乱数 |
| `abm/ledger.py` | 走行台帳のスキーマと追記 |
| `abm/loop.py` | 一試行のループの骨組みと B1 フック |

---

## §B.2 種の読み込み（B2-1）

### §B.2.1 入力ファイル

- 入力は `U-011_seed_v1.json` ただ一つ。実行時にコーパスへアクセスしない
- 種のハッシュを run 記録に残す
- 種は読み取り専用。実行中に書き換えない

**ハッシュの正準形**

```
1  読み込んだ辞書から "sha256" キーを除く
2  json.dumps(d, sort_keys=True, separators=(',', ':'), ensure_ascii=False)
3  その UTF-8 バイト列の sha256
→ ddc0e4ae928c08ce252c6f9456eb74e9e8044716a4b4e4c84d6c36daa974d858
```

- 起動時にこのハッシュを計算し、`sha256` 欄と一致しなければ**起動を中止する**

### §B.2.2 種の検算（起動時に必ず走らせる）

| # | 検算 | 期待 |
| --- | --- | --- |
| 1 | `a_q = (ℓ_q + c_q) / ℓ_q` が `a` 欄と一致（誤差 5e-4） | 24/24 |
| 2 | `c_q = 1 + 3·arity − 3·new_slots` が `c` 欄と一致 | 24/24 |
| 3 | `ℓ_q = −log₂(rate_q / Z)`、`Z = 8.134472` が `ell` 欄と一致 | 24/24 |
| 4 | `Σ p̂ = 1.000000`（`marginal` 欄の総和） | 一致 |
| 5 | 媒介・周縁A の `(a,e)` 型で `c = 4` | 一致 |

---

## §B.3 場面の生成（B2-2）

### §B.3.1 モチーフ表

```
              M1 支える           M2 風が運ぶ         M3 割れる           M4 押して動かす
核 一階1      hold(a,b)           carry(a,b)          break(a,b)          push(a,b)
核 一階2      push(b,a)           lift(a,b)           cut(a,b)            turn(b,c)
核 高階       require(核1,核2)    cause(核2,核1)      cause(核2,核1)      cause(核1,核2)
媒介          袋から一様に1語 (a,e)                   同左                同左
塔            allow(媒介,核1)     allow(媒介,核1)     allow(媒介,核1)     allow(媒介,核1)
周縁A         carry(a,e′)         cold(a,e′)          carry(a,e′)         hard(a,e′)
役割ユナリー  supported(b)        carried(b)          broken(b)           moved(b)
```

- 一階関係の引数型は `(entity, entity)`、高階・塔は `(relation, relation)`
- `e` と `e′` はその関係だけに現れる新規実体。**媒介と周縁A で共有しない**
- M4 のみ実体が 3 つ（`turn(b,c)`）。意図した非対称
- 役割ユナリーは **def(R) の外**。`n_t` にも入らないが `p̂` には入る
- 高階構成素は `Relation.arguments` に他の `relation_id` を入れて表現する。新しいグラフ型を作らない

### §B.3.2 生成手順

| 手順 | 内容 |
| --- | --- |
| W1 | S=4 種のモチーフを均等に混ぜる。同一モチーフの基準提示間隔は S |
| W2 | 周縁A の有無を `P(あり) = π_A` で抽選する（不在型） |
| W3 | 媒介の役割スロットは常に埋まる。中身を袋（N 語）から**一様に**引く（置換型） |
| W4 | 実体を新規生成し、役割に束縛する。**場面ごとに全部入れ替える。族内で同じ実体名を再利用しない** |
| W5 | glue を **1〜3 本、一様分布**で生成する。モチーフの関係を指さず、モチーフの述語と重ならない |
| W6 | 役割ユナリーは属性ではなく**関係として**場面に書く。**1 場面 1 本** |
| W7 | 核・塔の参加率は 1.0。常に場面にある |
| W8 | 場面の実体表に、可視関係から到達できない実体を残さない |

**π_A と袋の枚数（種から読む。ハードコードしない）**

```
        π_A         袋の枚数 N     周縁A の述語
M1      0.060701    15            carry
M2      0.081710    14            cold
M3      0.130073     7            carry
M4      0.265404     4            hard
```

### §B.3.3 glue

- 語彙は種の `glue` 欄（12 語）
- 本数は 1〜3 の一様分布。**場面ごとに変える（一定にしない）**
- 引数は**場面の実体から重複なく一様に 2 つ**選ぶ。関係を指さない
- モチーフの全述語・媒介の袋・役割ユナリーのいずれとも重ならない

### §B.3.4 実体と ID

- ID は不透明。**世界列から決定的に生成した乱数トークン**
- 連番・モチーフ名・層名・演算子名・来歴を埋め込まない
- 生成手順は `blake2b(run_seed ‖ trial_index ‖ 役割名, digest_size=8).hexdigest()`

---

## §B.4 伏せ辺（B2-3）

### §B.4.1 抽選規則

- 場面にある**二項以上**の関係から一様に一本
- **高階関係は対象にしない。**塔も核高階も除く
- 役割ユナリー（一項）は対象にならない
- 隠した辺を指す塔は**可視部に残す**。推移的除去を層B の hold-out に使わない
- 一試行に一本。`k=1` は採点窓の広さであり、予測本数の上限ではない

したがって抽選の母集団は次のとおり。

```
核一階1  核一階2  媒介  周縁A（π_A で有無）  glue（1〜3 本）
```

### §B.4.2 検算 — h̄ の再現

```
h̄ = E[ 1/n ]      n = 2（核一階） + 1（媒介） + Bernoulli(π_A) + Uniform{1,2,3}

        計算値      種の値      差
M1      0.796612    0.796612    3.4e-07
M2      0.797363    0.797363    3.4e-07
M3      0.799090    0.799090    9.1e-08
M4      0.803923    0.803923    1.6e-07
```

- 実装は `1 − h̄` を上の式で計算し、種の `one_minus_h` と 1e-6 以内で一致することを起動時に確認する

### §B.4.3 伏せ辺の表現と F-1

**場面の側には何も置かない。**隠した関係は取り除くだけで、印も札も残さない。
エージェントは、**自分の def(R) の行に相手がいない**ことで気づく。

- 伏せ辺は可視グラフから**除去**する。標識・スタブ・置換述語を置かない
- 隠した辺を指す塔は可視部に残す

**ただし現行コードのままではこれが成立しない。**除去すると、それを指していた核高階と塔の
引数が宙に浮き、`sme.py` が実体型と誤判定して候補を却下する。核を隠すと 1/4 しか残らず、
def(R) がそもそも採択されない。

これは **F-1** として既に決定済みの修正である。

- **F-1 を全実装の先頭に置く。**吸収より前
- F-1 なしでは充填が発生せず、窓という概念が成立しない
- `_systematicity_score` は未観測スロットへの対応を体系性に数えない（中身が見えないため）
- **未観測スロットへの対応は `relation_pairs` に登録する。読み飛ばしてはならない**
- F-1 は **target 側のみ**に適用する。base 側の墓石は関係ノードとして残るので第三分岐に入らない
- F-1 と「高階受理時の `entity_mapping` 伝播パッチ」は**別物**である。後者は実装しない

**登録が要る理由**（2026-08-24 の実測。この一行を落とすと T7 が再現しない）

```
未観測スロットの対応を登録せず読み飛ばす実装   M4 の OA  0.000 → 0.280 ✗
原本パッチどおり relation_pairs に登録する実装  M4 の OA  0.000     ✓

機構  M4 が過剰適用しないのは cause(核1,核2) と cause(核2,核1) の引数順が矛盾するため。
      その矛盾は 塔経由で登録された 核1 ↔ 核1 の対応が存在して初めて検出される。
      登録しない実装では、核一階1 が伏せられた試行でこの対応が消え、矛盾が検出されなくなる。

要点  F-1 は「制約を緩める」修正ではなく「制約を保つ」修正である。
```

**F-1 適用後の実測**（m=4、毎試行伏せ辺あり、8,000 場面、τ=0.67）

```
                F-1 なし                F-1 あり           伏せ無し（T7）
M1  採択/OA     0.150 / 0.000          0.250 / 0.000      0.250 / 0.000
M2              0.345 / 0.574          0.500 / 0.500      0.500 / 0.500
M3              0.349 / 0.564          0.500 / 0.500      0.500 / 0.500
M4              0.248 / 0.397 ✗        0.250 / 0.000      0.250 / 0.000

核の充足率（M1・M4）  ―                0.788 / 0.810      1.000
                                        ＝ (1−h̄) = 0.797。自分の核が隠れた試行では充足しない
```

- 既存 pytest 84 件は F-1 適用後も全通過する
- F-1 適用後、伏せ辺の有無で採択率・OA が変わらない

### §B.4.4 ②/③ の判定規則（F-1 と対になる）

F-1 は「未観測スロット」という判定を照合器の内部に作る。
**この判定を下流へ漏らさないための一行。**

```
禁止   写像に載っているかどうかで ②/③ を判定する
       → 隠された核は載る（未観測スロットとして）、無い周縁A は載らない → 区別できてしまう

採用   可視部に、述語まで一致する相手が実在するかで判定する
       → 隠された核も、無い周縁A も、等しく「相手なし」→ 区別できない
```

- ②/③ の判定は「可視部に述語まで一致する相手が実在するか」で行う。**写像への掲載の有無では判定しない**
- 充填（P4）は未観測スロットの印を使って挙動を変えない。全スロットを一様に埋める

---

---

## §B.5 世界列（B0-1）

- W1〜W8 のすべてを、エージェント状態・予測・正誤・掃引パラメータに依存させない
- 世界列は run seed から**事前に**生成でき、そのハッシュが全掃引腕で一致する
- f のコインは共通乱数方式 `u[agent, t] < f(agent, t)`。索引は `(agent_id, trial)`
- f を掃引軸にするので `Bernoulli(f)` を**直接引かない**（world_hash が腕間で変わる）
- 場面の関係本数・glue 本数を一定にしない

**world_hash の正準形**

```
1  世界列を試行順に並べ、各試行を
   {trial, motif, relations:[{id,predicate,arguments}], entities:[id],
    held_out_relation_id, u_coins:{agent_id: float}}
   の辞書にする
2  json.dumps(seq, sort_keys=True, separators=(',', ':'), ensure_ascii=False)
3  その UTF-8 バイト列の sha256
```

- `world_hash` を run 記録の先頭に書く
- 種の正準形と同じ規則を使う（二種類の正準形を持たない）

---

## §B.6 一試行のループの骨組み（B0-2）

```
W  世界     場面を出す・伏せ辺を選ぶ                    B0+B2
P  predict  P1 整列 → P2 展開 → P3 採択(τ) → P4 充填
            → P5 ②/③ 判定 → P6 出力 → P7 PendingState   B1（フック）
O  オラクル O1 採点 → O2 f 判定と開示 → O3 台帳         B0
U  update   U1〜U8                                       B1（フック）
L  台帳     追記                                         B0
```

- `predict` は `AgentState` を変えない。変えてよいのは `update` だけ
- エージェントは「どのスロットが伏せられたか」を知らない
- `predict` の署名・`AgentInput` に `query` / `target_slot` / `held_out` 相当を持たない
- フィードバックは**予測確定後にのみ**、伏せ辺一本分だけ渡す
- B1 のフックは抽象基底クラスで定義し、未実装は `NotImplementedError` を投げる
- ループの各試行末に台帳の `append` が一回以上呼ばれる

---

## §B.7 オラクル（B0-3）

| 手順 | 内容 |
| --- | --- |
| O1 | 予測集合から、伏せ辺に**内容一致**するものを探す。あれば的中、なければ失敗 |
| O2 | `u[agent,t] < f(agent,t)` なら `RevealedEdge`（正解の中身）を返す。そうでなければ `None` |
| O3 | 研究者側台帳に `u_t`・`f_setting`・`f_fired`・伏せ位置・判定を記録する |

- 採点対象は伏せ辺一本のみ。展開全体・充填全件を採点しない
- `correction_mode = RevealedEdge` に固定。`CorrectnessBit` は初期実装で生成しない
- 予測の結末は四分類で記録する。**的中 / 失敗 / 保留（f が発火しなかった）/ ④未解決**
- オラクルの正誤をエージェント側の型が受け取れないよう、`OracleView` を DTO で分離する

---

## §B.8 f（B0-4）

```python
def f(agent_id: str, t: int) -> float: ...
```

- f を定数で書かない。この署名で研究者側に持つ
- 初期実装は定数を返してよい。必要なのは**署名と記録**だけ
- 時間的非一様（干ばつ）と空間的非一様（群ごとの f ＝ 空間 dominance）を、後から差し込める形を保つ
- 干ばつは f だけを落とす。場面の供給・参加率・提示間隔・伝達を変えない
- 主掃引範囲は 0〜0.5 の 8 点。`f=0` セルを退化点として除外しない

---

## §B.9 走行台帳（B0-5）

### §B.9.1 物理形式

- JSONL。1 試行 1 行。追記のみ
- 台帳はループから **write-only**。read API を作らない
- 同期・追記のみで書き、集計・後処理をしない
- 台帳を無効化するフラグを作らない
- 台帳の型・スキーマはエージェント側 DTO と別モジュールに置く
- **再走行なしで通過率・AUC・干ばつ判定が台帳だけから再計算できること**

### §B.9.2 欄（B0 が全部立てる。B1 の欄も null で立てる）

**(a) 層A から継承（38 欄）** — `abm/logging_schema.py` の `LOGGING_SCHEMA_FIELDS` をそのまま `[§1.5.1]`

- 欄名はコードから読む。SPEC に書き写した一覧をハードコードしない
- 原本 §1.5.1 は「37 欄」と書いているが、**実数は 38 欄**である。一覧は正しく、数の表記だけが誤り

| 注意 |
| --- |
| `threshold`（既存）は SME の写像受理閾値であり、削除閾値 `θ′` **ではない**。`theta_prime` を別欄で足す |

**(b) D23 が必須とする欄（取り直しがきかない）** `[§1.5.2]`

| 欄 | 型 | 内容 |
|---|---|---|
| `u_t` | float | 共通乱数の引き |
| `f_setting` | float | その試行の f の設定値 |
| `f_fired` | bool | 実際にフィードバックを返したか |
| `feedback_content` | dict\|null | 返した内容（開示辺） |
| `held_out_content` | dict | 伏せ位置（内容とスロット） |
| `expansion_and_filling_all` | list | 展開・充填の全件 |
| `constituent_reason_123` | dict | 各構成素の ①②③（研究者側でのみ計算） |
| `type2_fired` | list | ② の発火（エージェント側の事象） |
| `predictions_all_slots` | list | 予測（全スロット） |
| `oracle_verdict` | str | 的中 / 失敗 / 保留 / ④未解決 |
| `registration_event` | dict\|null | ΔL を `L(H)` / `L(D\|H)` に**分けて** |
| `deletion_event` | dict\|null | 項目種・原因タグ・V の三成分 `P·ΔL` / `ρ·C_hold` / `w·embed` |
| `deletion_ell` | float\|null | 削除された項目の符号長 |
| `tie_event` | dict\|null | 同点イベント |
| `dangling_ref_onset` | bool | 宙吊り参照の発生 |
| `dangling_ref_duration` | int\|null | 宙吊り参照の存続時間 |
| `R_used` | bool | R の使用有無 |
| `def_R_diff` | dict\|null | def(R) の内容（差分） |
| `agent_state_snapshot_hash` | str | 毎試行 |
| `state_snapshot` | dict\|null | 登録・削除・停留判定時 |
| `transmission_event` | dict\|null | 伝達イベント（B4。当面 null） |
| `scene_G_star_ref` | str | 場面の G* 参照 |

- ①②③ の**分類**は台帳側でのみ計算し、エージェント側に持ち込まない
- ③ は実行中に誰も触れないので、後から再計算できない。必ず同期記録する

**(c) 層Bで追加（機構側）** `[§1.5.3]`

```
m_alloc, m_live, theta_prime, tau, constituent_states, exception_bits_charged,
M051_balance, matcher, abstain_reason, n_tie_candidates, candidate_distribution,
enumeration_version, V_vocab, merit_event_times, outcome_category
```

**(d) 2026-08-23 に追加**

| 欄 | 型 | 内容 |
| --- | --- | --- |
| `charge_source` | list[dict] | 課金ごとに `{slot_index, source}`。**単数にしない**（一試行で ①② が併発し、複数構成素が同時に課金される） |
| `verbatim_written` | bool | `RevealedEdge` から verbatim を書いたか |
| `world_hash` | str | 世界列のハッシュ |
| `seed_hash` | str | 種のハッシュ |
| `support_at_adoption` | float\|null | **採択判定に使った支持量。捨てる前に書く** |

**(c′) 毎試行 non-null の義務**

- 入力欄（`exception_bits_charged`・`constituent_reason_123`・`oracle_verdict`・`m_live`・`support_at_adoption`）は**毎試行 non-null** で書く
- 累積量（`V_vocab`・`M051_balance` 等）は変化時のみでよい。会計定義から再生できる
- **AUC のプローブ時点を走行後に自由に取るなら、毎行が安い**

**(d″) 掃引腕の記述子（run ヘッダ。2026-08-23 追加）**

| 欄 | 型 | 内容 |
| --- | --- | --- |
| `arm_alpha` | float | 充填の功績割引率 α（S1 の会計選択） |
| `arm_beta` | float | R外使用の重み β（G の会計選択） |
| `arm_w` | float | embed の重み |
| `arm_kappa` | float | fan-in 飽和 κ |
| `arm_repair_scope` | str | 修復圧の塔方向の射程 |
| `arm_holdout_repr` | str | 伏せ辺の可視グラフ上の表現（§B.4.3） |
| `arm_f_profile` | str | f の与え方（定数・干ばつ・群別） |

- **`V` を台帳だけから再計算するには会計腕の設定値が要る。**`θ′`・`τ`・`matcher` は (c) にあるが、α・β・w・κ・射程は欠けていた
- run ヘッダに一度書く。毎行に複写しない

**(d′) 欄の総数と重複**

```
(a) 層A          38
(b) D23 必須     22
(c) 層B 追加     15
(d) 8/23 追加     5
(d″) 掃引腕       7   run ヘッダ。毎行には持たない
(e) 研究者側      5
重複             −2   agent_state_snapshot_hash は (a) と (b) に、
                      abstain_reason は (a) と (c) に、それぞれ二度現れる
合計             83   ＋ run ヘッダ 7
```

- 重複する 2 欄について、台帳に列を二本作らない
- **口座のキーは述語（語）ではなく構成素**である。`carry` が M2 の核一階1 と M1・M3 の周縁A に同時に現れるが、口座は混ざらない
- `V_vocab` は生存語彙**数**であって口座キーではない
- 起動時に欄数が 83 であること、run ヘッダに腕記述子 7 本があることを確認する

**(e) 研究者側でのみ計算（エージェントは読まない）** `[§1.5.4]`

```
p1, p0, Sel, OA, f_realized
```

- `p1` と `p0` を**別欄**で保存する。`Sel` だけにしない

---

## §B.10 受け入れ試験

**この 7 件が全部緑になるまで B1 へ進まない。**

| # | 試験 | 期待 |
| --- | --- | --- |
| T1 | 種の検算 5 点（§B.2.2） | 不一致 0 件 |
| T2 | 種のハッシュが `sha256` 欄と一致 | 一致 |
| T3 | `1 − h̄` の逆算（§B.4.2）が種の `one_minus_h` と 1e-6 以内で一致 | 4/4 |
| T4 | 世界列のハッシュが、掃引パラメータを変えても一致 | 一致 |
| T5 | **再ラベル不変テスト。**全 ID を置換した世界列で、生成される場面の構造がビット同一 | 一致 |
| T6 | 台帳の欄が **83 本**すべて存在し、1 試行 1 行で追記されている | 83/83 |
| T7 | **検算B 問い1 の独立再現**（下記） | 表と一致 |

### T7 の詳細

```
条件  実物の abm/sme.py ＋ F-1 ／ 8,000 場面 ／ τ = 0.67
      def(R) を各モチーフの m=4 状態（核一階1・核一階2・核高階・塔）に置く

      墓石   媒介は墓石とする。墓石は 関係ノードとしてグラフに残す。
             参照 ID だけを残す実装は採らない（下の「墓石の表現」を参照）。
             墓石は m_live に入れない。採択の分母にも充足の対象にもしない。
             塔の第一引数は墓石を指したまま relation 参照として保つ。

      採択   「一致した生存構成素の本数 ÷ m_live ≥ τ」。
             ここでの「一致」は 写像の成立（Alignment.relation_mapping に載ること）であって、
             構成素の充足（述語まで一致すること）ではない（U-078）。
             充足で判定するとこの表は再現しない（下の「採択の一致とは何か」を参照）。

期待  R    採択率    OA      核一階1 の充足率   核一階2 の充足率
      M1   0.250     0.000   1.000              1.000
      M2   0.500     0.500   0.500              0.500
      M3   0.500     0.500   0.500              0.500
      M4   0.250     0.000   1.000              1.000

      核高階・塔の充足率は四モチーフとも 1.000
      毎試行伏せ辺ありで走らせる場合、核一階の充足率は (1−h̄) = 0.797〜0.804 になる
```

- **墓石は関係ノードとしてグラフに残す。**参照 ID だけを残す実装は採らない
- 墓石は `m_live` に入れない。採択の分母にも充足の対象にもしない
- 採択の「一致」は**写像の成立**。構成素の充足ではない
- 墓石の中身（述語）は照合に影響しない

**墓石の表現**（2026-08-24 の実測）

```
関係ノードとして残す         T7 期待表を完全再現            ✓
参照 ID だけ残す             M4 の 採択/OA が 0.347/0.280 ✗
塔の第一引数を実体扱いに落とす  採択率 0.715・塔の充足率 0.000 ✗
```

**採択の一致とは何か**（2026-08-24 の実測）

```
def(M2) が M3 の場面に当たるのは、cause の高階対が受理されて relation_pairs 経由で
下位一階が relation_mapping に登録されるためである。
そのとき下位一階の述語は lift ≠ cut・carry ≠ break で 一致していない。

→ 充足（述語一致）で採択を判定すると M2・M3 の OA が 0.500 にならない。
→ U-078 の分離は、課金の側だけでなく 採択の側でも実効的である。
```

---

## §B.11 この文書で決めた小さな既定（6件）

| # | 内容 | 既定値 | 変えたときの波及 |
|---|---|---|---|
| 1 | glue の引数 | 場面の実体から重複なく一様に 2 つ。関係を指さない | 無し（照合に効かない。D19(c) で述語が重ならないため） |
| 2 | 役割ユナリーの本数 `k` | `k = 1` | θ′ の絶対値のみ。階段構造は不変。`seed_build.py` の `K_ROLE` を変えて種を再計算（N-2） |
| 3 | 不透明 ID の生成 | `blake2b(run_seed ‖ trial ‖ 役割名, 8B)` | 無し。T5 を通れば何でもよい |
| 4 | 正準形 | 種と世界列で同一（`sort_keys=True, separators=(',',':')`） | ハッシュ値のみ |
| 5 | 台帳の物理形式 | JSONL・1 試行 1 行・追記のみ | 解析側の読み込みコード |
| 6 | 台帳の各欄の型 | §B.9.2 に明記 | 解析側 |

---

## §B.12 範囲外（B1 でやること。ここでは実装しない）

```
§2.3.1  P1 整列（SME の呼び出し）
§2.3.2  P2 R の同定と def(R) の展開
§2.3.3  P3 採択判定（τ）          ← τ > 1−1/m_live の扱いが未決
§2.3.4  P4 充填                   ← U-079（引数ソートを何が与えるか）が未決
§2.3.5  P5 ②/③ の判定
§2.5.x  U1〜U8（会計・登録・削除）
§3      会計の式すべて            ← 2・3・6 が未決
B4      伝達
```

- これらのフックは抽象基底クラスで定義し、未実装は `NotImplementedError` を投げる
- **B0+B2 の段階で `P` を計算しない。**§1.2.3 と §3.3.2 が食い違っており、まだ決着していない

---

## §B.13 未決（B0+B2 の範囲に残るもの）

| 番号 | 内容 | 重さ |
|---|---|---|
| N-2 | 役割ユナリーの本数 `k`。既定 1 で進めるが確定ではない | 軽 |
| U-024 | `anti_analogy × redact` 相互作用。「現状を腕として残す」で閉じる。触らなければ B0+B2 は進む | 中 |
| U-035 | `error_direction_prototype_consistent` を残すか ① 的中率時系列へ置換するか。欄は残す | 中 |
| ~~F2a~~ | ~~伏せ辺の表現~~ → **F-1 で解消**（§B.4.3）。残るのは §B.4.4 の判定規則を §4 で本文化すること | 軽 |
| F2b | Abstain のまま失敗した試行で、誰に課金するか（D8(c) が未定義） | 中 |
| F1b | α > 0.30 で崖が消える。賭けC の報告を S1 腕ごとに分けるか | 中 |

