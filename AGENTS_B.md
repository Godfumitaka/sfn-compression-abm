# AGENTS_B.md — 層B 実装契約（B0 装置・B2 世界）

> 2026-08-24 追加。`AGENTS.md`（層A 契約）と併せて読む。
> **本書と `AGENTS.md` が食い違ったら、層B の作業については本書が優先する。**
> `AGENTS.md` §1〜§7 の規律（言語規約・滑りの禁止・leakage 禁止・PR 規律）は層B でも生きている。

---

## 1. 真実源

| 対象 | 真実源 |
|---|---|
| 層A（既存の `abm/` 一式・関門A/B/C） | `SPEC_v3_0625.md`。**変更しない** |
| 層B（B0 装置・B1 エージェント・B2 世界・B3 測定） | **SPEC v4 系** |

**SPEC v4 系の内訳**

```
SPEC_B0B2_impl_2026-08-24.md          今回の実装仕様。これを主に読む
SPEC_v4_s1_datatypes_ledger_rev2.md   §1 型と台帳。impl が §1.2.3・§1.5.1〜5.5 を参照
SPEC_v4_s2_trial_loop.md              §2 一試行のループ。impl が §2.0・§2.2.3・§2.3.1〜3.5・§2.5 を参照
SPEC_v4_s3_accounting.md              §3 会計。impl が §3.3.2・§3.5・§3.8 を参照
SPEC_v4_s4_matching_filling.md        §4 照合と充填。参照用（B1 の仕様。今回は作らない）
```

**今回着手するのは B0（装置）と B2（世界）だけである。**

- **層A のコードを層B の都合で書き換えない。**唯一の例外が F-1（§3）で、これは既存 pytest 84 件の全通過を条件とする。
- したがって本作業は**関門A/B/C の判定に影響しない。**`AGENTS.md` §4 の「層B は関門Cが緑になるまで実装しない」は、層A のコードや関門の指標に触れる作業を指す。B0・B2 は独立した新規モジュールであり、これに当たらない。
- 層A と層B で規律が衝突したら**止まって報告する。**自分で優先順位を決めない。

---

## 2. `AGENTS.md` §3 の 6 規律は層B でも生きている

特に層B で破りやすい三つ。

```
1  正解挙動を stipulate しない
   「m=2 の def はこう振る舞え」を書かない。薄化の帰結は会計に内生させる

2  if(target_type) を書かない
   モチーフ（M1〜M4）で分岐しない
   役割ユナリー・袋サイズ・π_A・canon による地域の実質復元も禁止

3  leakage を構造で禁じる
   エージェント側は OracleView / G_star / held_out_edge / Feedback を引数に取らない
   伏せ辺の中身をエージェント側の型が持てないことを 型レベルで検査する
```

---

## 3. F-1 — 唯一の層A 改変。これは「滑り」ではない

**`F-1_patch_2026-08-23.diff` を全実装の先頭で適用する。**吸収より前。

### なぜ滑りでないか（読まずに拒否しないこと）

```
現行の abm/sme.py:277–286 は、引数を「関係」か「実体」の二分類でしか判定していない。
伏せ辺で隠された関係／削除された構成素（墓石）を指す引数は、そのどちらでもない。
現行実装はこれを「実体」と誤判定して候補を却下する。

Gentner の SME では、target 側の高階構造が未観測部分を制約することが
candidate inference の駆動源である。現行実装はこれを構文的に禁じている。
簡略化であって理論的要請ではない（2026-08-08 §1-22 で決定、2026-08-13 再確認）。

F-1 は 正解の埋め込みではなく 型判定の誤りの修正である。
```

### 実装上の急所（2026-08-24 の実測。外すと T7 が再現しない）

```
(a) 未観測スロットへの対応は relation_pairs に 登録する。読み飛ばさない
    読み飛ばす実装では M4 の OA が 0.000 → 0.280 になる

    機構  M4 が過剰適用しないのは cause(核1,核2) と cause(核2,核1) の
          引数順が矛盾するためだが、その矛盾は 塔経由で登録された
          核1 ↔ 核1 の対応が存在して初めて検出される。
          登録しない実装では、核一階1 が伏せられた試行でこの対応が消える。

(b) F-1 は target 側のみに適用する

(c) F-1 と「高階受理時の entity_mapping 伝播パッチ」は 別物。後者は実装しない（U-051）

要点  F-1 は「制約を緩める」修正ではなく「制約を保つ」修正である
```

**適用後、既存 pytest 84 件が全通過すること。**落ちたら止まって報告する。

---

## 4. 受け入れ試験（T1〜T7）は「誤答方向の assert」ではない

`AGENTS.md` §5 の「誤答方向を assert しない／望ましい結果を合格条件にしない」は**研究結果**についての規律である。**受け入れ試験はこれに当たらない。**

```
禁止されているもの   反証対象の予測を合格条件にすること
                     例：「低 threshold なら鋳型方向に外すはず」を assert する

T7 が測るもの        固定した def(R) と固定した種に対する 照合器の機械的挙動
                     設計の決定論的帰結であって、反証対象の予測ではない
                     数値が変われば 実装かモチーフ表のどちらかが壊れている
```

**T7 は pytest として書く。数値を assert してよい。**

### T7 の急所

```
墓石   関係ノードとしてグラフに残す。参照 ID だけを残す実装は不可
       参照 ID だけ残すと M4 が 0.347/0.280 になる
       塔の第一引数を実体扱いに落とすと 採択率 0.715・塔の充足率 0.000 になる

採択   「一致」は 写像の成立（Alignment.relation_mapping に載ること）であって、
       構成素の充足（述語まで一致すること）ではない（U-078）
       充足で判定すると M2・M3 の OA が 0.500 にならない
```

---

## 5. スコープ境界

| ブロック | 今回 | 備考 |
|---|---|---|
| **B0 装置** | 作る | 種の読み込み・検証・台帳・オラクル・f の器 |
| **B2 世界** | 作る | 場面生成・伏せ辺・世界列の事前生成 |
| **B1 エージェント本体** | **作らない** | §1 rev3 と未決 2 件（U-084・U-086）待ち |
| **B3 測定** | **作らない** | 主張階層（U-030）が未決 |

**B1 に手を出さない。**impl が §1〜§3 を参照しているのは、型と欄の定義を引くためであって、エージェント本体を作る指示ではない。

---

## 6. SPEC v4 §1〜§3 に既知の古い記述が残っている

**2026-08-24 に決定が変わったが、§1〜§3 への反映が済んでいない箇所がある。**
下記を読んでも**実装しない**こと。B1 の範囲なので今回は触らないが、参照時に混乱しないよう明記する。

| 箇所 | 古い記述 | 正しい記述 |
|---|---|---|
| §1.2.3 | `P = S/(1+S)` | **削除。**def(R) の行の P は比率型。`P = (照合参加 + α×充填) ÷ R の適用回数` |
| §1.2.3 / §2.5.4 | `S_q = 1.0×照合参加 + α×充填 + β×R外使用` | **β を外す。**β は V の別項 `(1−p_R)·β·P_ext·ΔL`（M-019） |
| §2.5.4 | verbatim は「正規化なし。σ をそのまま」 | `P = σ/(1+σ)`（M-012） |
| §3.9.1 | U-081（θ′ 格子の延長） | **撤回。**θ′ は 4 点のまま |

---

## 7. f の署名は初期実装でも保持する

```
f は f(agent, t) の署名で持つ。定数を返す実装でよい
実際に経験した f を 研究者側台帳に毎試行記録する
共通乱数 u[agent,t] < f(agent,t) の形で引く。Bernoulli(f) を直接引かない
  （直接引くと world_hash が腕間で変わる。U-007）
```

**後から変えると 世界列生成・オラクル・台帳スキーマ の三箇所に波及する。**
定数だからといって署名を単純化しない。

---

## 8. 未決に手を出さない

SPEC 中に `🔺` が付いた行（impl では記号が除去されているので、判断に迷う箇所）、および下記は**未決**である。**自分で決めない。止まって報告する。**

```
U-084  採点されない充填の扱い
U-085  スロット履歴集合の減衰
U-086  F2b（棄権のまま失敗した試行の課金先）
U-030  主張階層
```

---

## 9. 停止条件

次のいずれかに当たったら**手を止めて報告する。**

```
既存 pytest 84 件のいずれかが落ちた
T7 の数値が再現しない
SPEC の記述が二通りに読め、どちらでも実装できてしまう
層A の規律と層B の SPEC が衝突した
未決（§8）を実装する必要が出た
```

**穴が出たらコードで埋めずに SPEC へ戻す。**実装の目的は結果ではなく仕様の穴出しである（`AGENTS.md` §11-11）。

---

## 10. 着手順序

```
1  F-1 パッチを適用する
   §3 を先に読む。これは「滑り」ではない
   適用後、既存 pytest 84 件が全通過することを確認する

2  T7 を pytest として書く
   §4 を先に読む。数値を assert してよい
   期待値は SPEC_B0B2_impl §B.7

3  種の読み込みと検証（abm/seed.py）
   sha256 の正準形は SPEC §3.2.2
   内部整合：a=(ℓ+c)/ℓ・c=1+3·arity−3·new_slots・ℓ=−log₂(rate/Z)・Σp̂=1.000000

4  場面生成と伏せ辺（abm/world.py）
   モチーフ表は SPEC_B0B2_impl §B.3.1
   伏せ辺の候補は 核一階1・核一階2・媒介・周縁A(π_A)・glue(1〜3の一様)
   核高階・塔・役割ユナリーは 伏せ辺の対象外
   h̄ = E[1/n] であって 1/E[n] ではない

5  f(agent, t) と共通乱数（abm/feedback.py）
   §7 のとおり

6  走行台帳（abm/ledger.py）
   欄は SPEC §1.5
   層A 38 ＋ D23 22 ＋ 層B 15 ＋ 追加 5 ＋ 研究者側 5 − 重複 2 = 83 欄 ＋ run ヘッダ 7
```

---

## 11. PR の書き方

`AGENTS.md` §7 のとおり。加えて PR 本文に次を書く。

```
1  対応する SPEC 節
2  追加した pytest が検査するもの
   （構造的性質か、受け入れ試験か。両者を区別して書く）
3  AGENTS.md §3 の 6 規律に滑りがないことの自己点検
4  実装中に見つけた SPEC の穴（あれば。無ければ「なし」と書く）
```

**4 は必須項目である。**2026-08-24 に Claude が独立実装を試みたとき、SPEC の穴が 3 件出た
（F-1 の登録要件・墓石の表現・採択の「一致」の定義）。**まだ残っている可能性が高い。**
