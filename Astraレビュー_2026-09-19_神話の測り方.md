# Astra レビュー 2026-09-19 — 神話の測り方の設計レビュー

★ 原文のまま。★ 2026-09-19 に リポジトリ直下へ保存（.gitignore:11 の analysis_*/ で
  追跡対象から外れることを避けるため）。

---

**決裁は条件付き承認です。§3の核は使えます。ただし、三分割の誤答率の差をそのまま「神話の量」とする案は採りません。**

測れるのは、**現在の定義から具体的に取り出せる主張のうち、過去に採点されず、世界と食い違うもの**です。「有用だから訂正されずに残る」という機構の証明には、もう一段必要です。

以下の確率は、実測した発生率ではなく、設計判断への主観的確信度です。**修正後の測定成立には90%、原案に修正が必要という判断には99%**です。

§2の原本を先に読み、本命走行に対応するコード・台帳・現行compactを確認しました。コード変更・再走行・PRは行っていません。

まず、§3で直すべき点を挙げます。

1. **「辺なので真偽が付く」は、命題の意味を固定した場合に成立します。**

   主張＝どの世界事例について、どの対象間の、どの関係を述べているかが確定した命題です。

   今回は次の二つを区別してください。

   | 真理の定義 | 判定する内容 |
   |---|---|
   | 世界真偽 | 「この辺が、この完全世界に存在する」 |
   | 課題真偽 | 「この辺が、今回伏せられた正解である」 |

   別の可視辺として存在する関係を答えれば、世界真偽は真でも、課題真偽は偽です。この違いは、棄却記録でも既に確認されています。analysis_astra/judgments.md:49

   **§3の構成素展開には世界真偽を主に使い、最終回答には課題真偽も併記する**のが適切です。両者を混ぜて採点群と未採点群を比較してはいけません。

   また、写像には実行時と同じ可視部分だけを渡し、完全世界は採点にだけ使います。対象や参照先が確定しない式は「不明」に残し、偽に数えません。〔確信度99%〕

2. **NSIMによる識別は、実際の予測条件と違います。**

   NSIM＝定義と場面の構造対応の得点を、定義自身との対応得点で正規化した値です。

   本命ではNSIM閾値は **0.95**、予測時の採択は生存行の支持率 **0.67** です。NSIMは同化先の識別に使われ、予測時には別の選択処理が動きます。abm/agent_runtime.py:204

   したがって、§3の手順3を必須条件にすると、**実際には出せる主張を測定から落とす可能性があります。** NSIM通過域に限定した構造診断としては成立しますが、「Rの主張域全体」とは呼べません。

   主測定では実行時の採択条件を使い、NSIM通過／不通を別列にしてください。〔確信度99%〕

3. **「採点済みだから正しい」は成立しません。**

   採点されたのは過去の回答です。正解を受け取ったことと、定義が修正され、現在も正しいことは別です。実装も、開示された誤答に例外費用を課すのであって、完全訂正を保証していません。abm/loop.py:291

   さらに、本命のように発話後、独立な確率 f で開示するなら、**当時の回答を採点あり／なしに分けても、期待上の誤答率は同じ**です。

   凍結評価＝ある時点の定義・記憶を固定し、学習を進めずに再評価することです。

   比べるべきなのは、**同じ凍結時点での正誤を、そこまでの採点履歴で分けた値**です。「(i)は当たる」は、その評価で検証する仮説として残してください。〔確信度99%以上〕

4. **内部候補を発話に数えると、三分割が壊れます。**

   内部候補＝生成されたものの、最終回答には選ばれていない辺です。

   台帳の `predicted_edge` は発話一本、`predictions_all_slots` は診断用の内部候補です。コードにも区別が明記されています。abm/loop.py:454

   よって、内部充填候補はそのまま(ii)に入りません。「生成経験あり」という別の印を付けます。また、確率抽選で候補になり得ることを、候補全部への同時の断定と扱ってはいけません。〔確信度99%〕

5. **誤答率の差は、神話の量ではありません。**

   未採点の偽主張が1個でも1,000個でも、誤答率差は同じになり得ます。採点群と未採点群が両方90%誤りなら、差はゼロです。

   **偽主張そのものの量を主指標にし、率の差は「採点履歴による偏り」の補助指標にする**べきです。さらに、その差だけでは「有用性が神話を守った」という因果関係までは分かりません。〔確信度99%〕

採用する測定仕様は、次の形を勧めます。

**R条件付き出力＝あるRを選択対象として固定し、他のRとの選択競争だけを外して、現在の実装で得る回答または棄権です。**

- 評価時点 t までに、実発話したことのあるRを対象にする。
- 同じ凍結状態へ、正解を隠した評価場面を与える。
- 各Rの採択条件・投影条件・充填規則・同点棄権を保って、R条件付き出力を求める。
- 得られた主張に世界真偽・課題真偽と、過去の発話・採点履歴を付ける。
- 別表で、写像可能な構成素を展開した主張を評価する。
- 何も出ない場合は、写像不能・採択条件不通・出力不能などの理由を残す。

構造展開＝実際の回答選択を離れ、定義の構成素を場面に対応させて内容を読む外部評価です。

**R条件付き出力と構造展開は両方必要ですが、別の測定対象です。** 後者まで調べることで§3の意図を保てます。ただし、その全件を「エージェントが信じている」と認定することには留保が要ります。

三分割の履歴にも、単位の指定が必要です。同型＝不透明なIDを付け替えるだけで、述語・引数順・参照構造などが一致することです。

具体辺のID完全一致だけで履歴を付けると、新しい実体の場面では、十分に試された規則まで全部(iii)になります。そこで、

- **具体的な主張への直接採点歴**
- **同型の可視条件・同じ質問位置・対応する結論についての採点経験**

を別に保存してください。後者の経験があっても、新しい個別事例の正しさが保証されるわけではありません。

また、**未採点は未観測ではありません。** 可視入力で見た辺、沈黙中に開示された辺、他のRを通じて得た情報もあります。三分割は「自己出力への採点歴」として使い、可視観測・開示経験を別列にします。高階が出題されないことも、高階を一度も見ていないことを意味しません。

主指標の一例は、Rごとの次の量です。

    M_R(t) = Σ_s μ(s) Σ_q π_t(q|R,s) · 1[世界で偽] · 1[履歴が(ii)または(iii)]

ここで、μ(s) は評価場面の重み、π_t(q|R,s) はR条件付き出力が主張 q を出す確率です。

これは「Rに基準分布の問題を与えた場合の、未採点の偽主張の確率量」です。**主張数、各群の誤り率、出力率、(i)との差も併記**します。R別の量を足したものは定義別の延べ量なので、個体の異なる主張数を出す際は同一場面・同一命題を重複除去します。分母ゼロは欠測です。

§4の四点については、次の判断です。

| 論点 | 判断と具体案 |
|---|---|
| **4-1 モチーフと機構** | 分離する方針に賛成です。**生成モチーフ × 投影／生存行充填／墓石充填 × 推論方向**を出してください。推論方向は、研究者側で原因・結果の役割を定め、原因が見えて結果を問う場合と、その逆を区別します。充填だから逆推論、とは数えません。〔95%〕 |
| **4-2 場面標本** | 標本で十分です。主表は生成頻度に対応する重み、補助表はモチーフ別を等しく扱う重みにします。稀な場面を多めに採るなら、主表では抽出割合を補正します。完全世界と可視入力の組を保持し、同じ可視入力から複数の正解が生じる場合を潰しません。〔95%〕 |
| **4-3 投影条件** | 主測定では適用。構造展開は別表。ただし四条件を一括撤去しません。高階を除く条件だけを緩める診断は可能ですが、引数の対応は必須です。既に見えている辺の再掲も分離します。両表の差は「神話の取りこぼし量」とは断定できません。〔97%〕 |
| **4-4 発話数 > 0** | 終点評価なら全走行で一度でも発話、でよいです。途中時点では**その時点までに一度でも発話**とします。「今その場面で話したR」には限定しません。過去に話したが現在は出力不能のRも対象一覧に残し、測定不能の件数を出します。〔97%〕 |

投影条件は、既に対応した可視辺の除外、高階行の除外、全引数の写像、既存内容の除外です。全部外すと、既知の真辺を大量に再掲して正答率を上げることもできます。abm/sme.py:350

**時間的回収については、現時点では撤回すべき読みです。** 近い処理の `pending_claims` は本命では無効です。有効にしても、後日に述語集合が共起したことを見る処理で、同じ人物の過去と現在を結ぶものではありません。abm/domains.py:176、abm/loop.py:171。本命にその意味の時間的回収を帰属できない、という確信度は98%です。

また、数値の前提を二つ修正します。

- **4,160は旧375場面の評価集合に含まれる辺の和集合で、生成世界全体の大きさではありません。** 現行四階の375事例では、保存された世界辺は7,153出現でした。analysis_astra/rext_port_worlds_handoff.md:3
- **一時点の375主張という上限は、375問題へ一本ずつ答える評価の上限です。** 全R・全構成素へ広げる今回の案には適用できません。

実装経路は、**現行四階用の台帳・compact・保存状態を再利用した外部解析で作れる見込みが高い**です。再走行必須とは判断しません。〔90%〕

compact＝原台帳から解析用の項目を抜き出した縮約記録、checkpoint＝選定時点の状態を復元するための保存物です。

| 必要な情報 | 現状と追加事項 |
|---|---|
| 実発話と開示 | `predicted_edge`、`R_used`、`f_fired`から取得可能。`R_used`だけでは発話を意味しません。 |
| 任意のR×場面の主張 | 保存状態から再計算が必要。既存出力列の集計だけでは作れません。 |
| λ=1の充填 | 現行四階用復元器は局所回数を保持しています。履歴を集合化する旧復元器は使えません。 |
| 主張の履歴接合 | 評価時点、定義の版、構成素の登録世代、場面、質問位置、命題の意味、発話・開示時刻を解析表に追加します。 |
| 実際に経験した f と経路 | 原台帳の `f_realized`、`prediction_path`を追加取得します。現行compactでは落ちています。 |
| 任意時点の完全な状態 | 選定checkpoint以外は原台帳の差分復元が必要です。 |

現行移植はλと履歴回数を保持しています。analysis_v3a2cf_2026-09-17/api_current.py:14。ただし、保存状態には引数順をそのまま逆変換できない箇所があり、登録イベントと接合する既存の復元手順が必要です。analysis_astra/judgments.md:37

R名も構造の識別子にはなりません。述語多重集合だけで命名されるためです。abm/abstraction.py:255。**同じ名前のRが昔一度採点された、という理由で現在の全構成素へ採点歴を継承しないでください。** 版は再現のために保持し、履歴を継承するかは、前提と命題が保たれているかで判断します。

今後再走行するなら、構成素の一意ID、定義の変更版ID、順序を失わない状態保存、候補ごとの出所・選択理由・確率をまとめて記録すると、接合の曖昧さを減らせます。

計算量は、走行数 N、評価時点数 K、対象定義数 D、場面数 S、一組の写像・出力・採点費用 C、原記録の読取量 L として、概ね

    O(L + N·K·D·S·C)

です。世界の辺を場面別に検索できる形へ準備すれば、毎回4,160や7,150要素を総当たりする必要はありません。

| 仮置きする規模 | R×場面の評価組数 |
|---|---:|
| 120走行 × 53定義 × 375場面 × 1時点 | **238.5万** |
| 同じ条件で3時点 | **715.5万** |
| 全99.3定義を対象に1時点 | **約447万** |
| 53定義を全1,740時点へ適用 | **約41.5億** |

一組1msなら238.5万組で約40分、10msなら約6.6時間、100msなら約66時間です。これは実測予測ではなく、仮の単価による計算です。墓石・充填候補・高階参照の費用があるため、生存6.25行だけでは所要時間を決められません。

**初版は終了時点を主評価、事前に固定した少数時点を補助評価**とするのが妥当です。120台帳は3つのθ′×2つの充填方式×20 seedなので、6条件を分け、推定の不確かさは走行seed単位で扱います。全辺を独立な標本と数えてはいけません。

最後に、§2の棄却が再適用される範囲を明示します。

- **「受理しただけでは何が偽か分からない」という棄却は、具体的な命題と真理規則を固定すれば回避できます。** 写らず何も出ない場合には、そのまま残ります。
- **生成範囲外の真理の問題は、生成範囲内だけを評価すれば回避できます。** その代わり、生成されない場面についての誤表象は測っていません。
- **構造を外部展開した結果を、無条件に本人の信念と呼ぶ場合には、帰属の問題が残ります。**
- **生成範囲内で全く同じ正しい予測をする二つの説明は、この測定では区別できません。** 「正しく理解している」の全体を測った、とは言えません。

9月の測定は、この修正版で進めてよいと判断します。「有用性が未採点の誤りを守る」という説明には、同じR内での有用な出力と偽主張の持続を追い、保持への寄与を別に検証する必要があります。

10月に向けては、**低い f のため採点されなかった主張と、回答に選ばれず採点機会がゼロだった主張を必ず分けてください。後者は、移入先の f が高くても、それだけでは訂正されません。**
