# 夜間の依頼 — 墓石からの発話を止めたら何が失われるか、そして探索

宛先 Codex ／ 発行 2026-09-05 深夜
対象 `GODfumitaka/sfn-compression-abm`（HEAD `05c36bdc605b13a44a1fe98f325e54e7987af892`）

---

## 0. この依頼の性格

**発行者は就寝します。朝まで応答できません。**

★ **止まって聞かないでください。**判断が要る場面では、
  **可逆で安価なほうを選び、選んだ理由と選ばなかった案を記録して進めてください。**

★ 止まってよいのは **§6 の保護対象を壊しうるときだけ**です。

★ 前半（§2）は指定された測定、後半（§4）は自由な探索です。
  **前半が終わったら、後半は好きな順で構いません。**

★ **時間を使い切ってください。**長く回して構いません。

---

## 1. 今日わかったこと（★ 前提。誤りがあれば行番号つきで指摘してください）

前回の探索（`exploration_20260905/REPORT.md`）に加えて、SPEC を引いた結果です。

### 1-1　墓石からの発話は仕様違反である

```
SPEC_B1_impl_2026-08-28.md §C.3.4 P4（充填）
    for constituent in definition.constituents:
        if not constituent.alive:
            continue

同 1182 行  予-2「予測は def(R) の充填からのみ出す」
同 判定順 9  「全充填スロット集合を予測として返す」
```

★ **仕様は「生存構成素の充填からのみ予測を出す」と定めています。**

★ 一方、実装は `agent_runtime.py:222` で墓石を含むグラフを `project` に渡し、
  その出力が採られる経路があります。**前回の実測で、f=0.5 の失敗 108 件中 97 件が墓石由来でした。**

★ 会計側の `loop.py:229-230` の alive ガードは**仕様どおり**です。
  **投影側が仕様から外れています。**

### 1-2　★ 「水位の命題(i)」が SPEC に既に書かれている

```
SPEC_B1 1182 行
  枚からの投影を予測としてではなく研究者側の欄として残す。
  走行後に「逐語から予測していたら何を言っていたか」を再構成でき、
  ★「反証材料は手元にあったが、誤りとして記帳されなかった」を数値で示せる（水位の命題(i)）。
```

★ **これが本研究の中心的な問いです。**そして測るための欄が既に用意されています。

```
verbatim_baseline_prediction ／ verbatim_baseline_hit   （台帳・試行ごと）
accuracy_verbatim ／ def_beats_verbatim ／ verbatim_rescues ／ verbatim_rescue_rate （per_run）
```

★ **`/home/claude/ld` 相当の ldecouple 走行（640 本、vt 4 水準 × θ′ 4 水準）が既にあります。**
  ★ **この命題を正面から測った形跡がありません。**

---

## 2. 錨となる測定 — 投影に生存ガードを入れる

### 2-1　変更する箇所（★ 最小に）

```
現行  agent_runtime.py:222
      relations=tuple(row.relation for row in definition.constituents)
検証  relations=tuple(row.relation for row in definition.constituents if row.alive)
```

★ **注意：`_definition_graph` は照合にも使われます。**
  SPEC 314・468 行は「墓石は関係ノードとしてグラフに残り、照合器から見える」と定めています。
  **照合用のグラフは絞らないでください。投影に渡すグラフだけを絞ります。**

★ 実装の形は任せます。**config の腕として切り替えられる形が望ましい**が、
  難しければ別ブランチでも構いません。

### 2-2　★ まず seed 36 一本

```
seed 36 ／ θ′ = 0.0410 ／ f = 0 ／ T = 1740 ／ abstain_charge = false
（★ 原報告の再現条件と同じ）
```

現行とガードありを比べて、次を出してください。

```
発話 / 的中 / 失敗 / 精度 / 被覆率 / 全試行的中率
★ 失われた的中 と 失われた誤答 の 件数と比
棄権理由の内訳（★ no_projectable_relation が増えるはず）
```

### 2-3　★ 走行前に置く判定（★ 数値を見てから変えない）

```
誤答が的中より 明確に多く減る   → 墓石発話は害。仕様どおりに直すのが正しい
的中のほうが 多く減る           → 墓石発話は有用。SPEC の側を見直す論拠になる
両方ほぼ同率で減る               → 発話が減っただけ。命題の材料にならない
```

★ **「明確に」の基準を、走行前に自分で決めて記録してください。**

### 2-4　広げる条件

seed 36 で**いずれかの方向に振れたら**、広げてください。

```
seed 10〜20 本 × θ′ 2 水準 × f 2 水準
```

★ 1 走行 328 秒（1 コア）。**`--calibrate` で費用を実測してから点数を決めてください**
  （`sweep.py:304-334`）。

★ **振れなかった場合は広げず、§4 へ進んでください。**

---

## 3. 前回の探索で残った宿題

前回のご自身の提案です。**優先度は §2 の次。**

```
1  生死を出力制御に反映させた場合、墓石由来の誤答と的中はどちらもどれだけ失われるか
   → ★ §2 がこれに当たります
2  独立した 90% 世界と、履歴から更新する明示的な確率出力を用意した場合、
   確率の校正が進むか
   → ★ §4 の探索候補として扱ってください
```

---

## 4. 開かれた探索（★ 好きな順で。全部やる必要はありません）

### 4-1　★ 最有力 — 水位の命題(i) を既存データで測る

§1-2 のとおり、SPEC が「反証材料は手元にあったが記帳されなかった」を
測れると書いており、欄も走行も既にあります。

★ **どう測るかは任せます。**思いつく形を挙げますが、より良い形があればそちらで。

```
逐語ベースラインが的中し、def が外した試行の数
その誤りが f によって記帳されたか、されなかったか
vt を短くすると その差はどう動くか（★ ldecouple 640 走行が既にある）
```

★ **注意：`verbatim_lifetime` は `floor(1/θ′²)+1` という θ′ の関数であり、
  枚の実数ではありません**（実測で確認済み）。**枚の実数が要るなら台帳から数えてください。**

### 4-2　仕様と実装の食い違い（★ 今日見つかった三件）

```
new_slots        種に宣言があるが 実行時は def ごとに再計算される（一致率 27.22%）
dangling_ref_*   ledger.py:19 に欄名があるが 代入する行が無い。全試行 None
R_identified     agent_runtime.py:110 で trace に入るが 台帳に写されない
```

★ **三つ目が効きます。**`R_identified` が台帳にあれば、
  **「同定されたが発話しなかった」試行が数えられ、def 単位の発話率が初めて出ます。**
  一行足せば取れるはずです。

### 4-3　種の拡張（★ 優先度は低い）

前回「この問いの前提にはしなかった」と判断されています。**その判断を維持して構いません。**

案は `docs/.../探索依頼_Codex_誤り訂正の限界_2026-09-05.md` の §5 にあります。
写像器での検証は済んでいます。

### 4-4　自由枠

**上のどれでもない筋を見つけたら、そちらを追ってください。**
「この模型では中心の問いは測れない」という結論も、同じ価値があります。

---

## 5. 迷ったときの規則（★ 発行者は寝ています）

```
可逆で安価         → 選んで進む。選んだ理由と 選ばなかった案を記録
非可逆または高価   → 選んで進むが、記録を厚くする
★ 保護対象を壊しうる → ★ そこだけは止まって、朝まで待つ
```

★ **「聞くために止まる」を避けてください。**夜が無駄になります。

★ **仮説が途中で変わってよいです。**変わった経緯を残してください。

---

## 6. 保護対象（★ これ以外に制限はありません）

```
1  runs/ 以下の既存走行を 変更・削除しない
2  analyze_rev6.py を 書き換えない（★ 複製する）
3  U-011_seed_v1.json を 書き換えない（★ 新ファイルを作る）
4  main に 直接コミットしない
5  exploration_20260905/ の 既存成果物を 上書きしない（★ 新しいディレクトリを作る）
```

---

## 7. 返してほしいもの

```
1  何を測ったか（★ 生の数値・実行したコマンド・台帳の sha256）
2  ★ 走らせる前に何を予想したかと、実際との対照
3  ★ 途中で判断した箇所と、選ばなかった案
4  何が分かったか。何が分からなかったか
5  §1 の前提に誤りがあれば、行番号つきの引用で指摘
```

★ **結論の要約より、生の数値と引用を返してください。**

★ `per_def` と `per_run` は二次記録です。
  「模型が何をするか」の一次記録は `abm/` 以下の実装、
  「何を決めたか」の一次記録は SPEC と台帳です。**両者が食い違う場合、その食い違い自体を報告してください。**

★ **単一 seed の結果を一般化しないでください。**前回の 6 走行はすべて seed 36 でした。
